$ eslint .

<repo>/src/app/AppShell.tsx
  125:30  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/AppShell.tsx:125:30
  123 |
  124 |   useEffect(() => {
> 125 |     if (mode === "database") setDatabaseVisited(true)
      |                              ^^^^^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  126 |     else if (mode === "files" || mode === "ade") { lastWorkMode.current = mode; setCheckoutTool("files") }
  127 |     if (mode === "git") { setGitVisited(true); setCheckoutTool("git") }
  128 |   }, [mode])  react-hooks/set-state-in-effect

<repo>/src/app/panels/DatabasePanel.tsx
  55:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/app/panels/PreviewPanel.tsx
  133:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/panels/PreviewPanel.tsx:133:5
  131 |
  132 |   useEffect(() => {
> 133 |     setUrlDraft(null)
      |     ^^^^^^^^^^^ Avoid calling setState() directly within an effect
  134 |     setUrlError(null)
  135 |   }, [workspace])
  136 |  react-hooks/set-state-in-effect

<repo>/src/app/panels/SftpPanel.tsx
  167:3  warning  Error: Cannot access refs during render

React refs are values that are not needed for rendering. Refs should only be accessed outside of render, such as in event handlers or effects. Accessing a ref value (the `current` property) during render can cause your component not to update as expected (https://react.dev/reference/react/useRef).

<repo>/src/app/panels/SftpPanel.tsx:167:3
  165 |   const [hover, setHover] = useState<SftpHoverTarget | null>(null)
  166 |   const dragRef = useRef<SftpDragState | null>(null)
> 167 |   dragRef.current = drag
      |   ^^^^^^^^^^^^^^^ Cannot update ref during render
  168 |   // Pressing a file row selects it (row highlight); a stale path simply stops
  169 |   // matching after navigation, so no explicit clearing is needed.
  170 |   const [selected, setSelected] = useState<{ pane: SftpPaneKind; path: string } | null>(null)                                                                                                                                        react-hooks/refs
  262:6  warning  React Hook useEffect has missing dependencies: 'acceptRemoteToLocalDrop' and 'hostId'. Either include them or remove the dependency array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            react-hooks/exhaustive-deps
  485:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/panels/SftpPanel.tsx:485:5
  483 |
  484 |   useEffect(() => {
> 485 |     setCwd(nativeWorkspacePath)
      |     ^^^^^^ Avoid calling setState() directly within an effect
  486 |     setEntries([])
  487 |   }, [nativeWorkspacePath])
  488 |                                                            react-hooks/set-state-in-effect
  494:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/panels/SftpPanel.tsx:494:5
  492 |     if (!cwd) return
  493 |     let cancelled = false
> 494 |     setError(null)
      |     ^^^^^^^^ Avoid calling setState() directly within an effect
  495 |     void listDir(cwd)
  496 |       .then((nodes) => {
  497 |         if (!cancelled) setEntries(nodes)  react-hooks/set-state-in-effect

<repo>/src/app/workbench/ContextMenu.tsx
  17:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/app/workbench/DatabaseNavContent.tsx
  1201:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/workbench/DatabaseNavContent.tsx:1201:5
  1199 |
  1200 |   useEffect(() => {
> 1201 |     setTransportChallenge((current) => {
       |     ^^^^^^^^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  1202 |       if (!current) return current
  1203 |       if (
  1204 |         current.transportMode === transportMode  react-hooks/set-state-in-effect

<repo>/src/app/workbench/LogsSection.tsx
  157:6  warning  React Hook useEffect has a missing dependency: 't'. Either include it or remove the dependency array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         react-hooks/exhaustive-deps
  177:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/workbench/LogsSection.tsx:177:5
  175 |
  176 |   useEffect(() => {
> 177 |     setSource(initialSource ?? "")
      |     ^^^^^^^^^ Avoid calling setState() directly within an effect
  178 |   }, [initialSource, openNonce])
  179 |
  180 |   useEffect(() => {                                                   react-hooks/set-state-in-effect
  182:7  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/workbench/LogsSection.tsx:182:7
  180 |   useEffect(() => {
  181 |     if (!isValidIsoTimestamp(since) || !isValidIsoTimestamp(until)) {
> 182 |       setLoading(false)
      |       ^^^^^^^^^^ Avoid calling setState() directly within an effect
  183 |       return
  184 |     }
  185 |     let alive = true  react-hooks/set-state-in-effect
  211:6  warning  React Hook useEffect has a missing dependency: 't'. Either include it or remove the dependency array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         react-hooks/exhaustive-deps

<repo>/src/app/workbench/SettingsDialog.tsx
  238:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/workbench/SettingsDialog.tsx:238:5
  236 |   useEffect(() => {
  237 |     if (!open) return
> 238 |     setQuery("")
      |     ^^^^^^^^ Avoid calling setState() directly within an effect
  239 |     setJumpTarget(null)
  240 |     const match = SECTIONS.find((s) => s.id === initialSection)
  241 |     if (match) setSection(match.id)  react-hooks/set-state-in-effect

<repo>/src/app/workbench/SqliteLocationFields.tsx
  73:5   warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/app/workbench/SqliteLocationFields.tsx:73:5
  71 |       if (leasedUri) await releaseRemoteWorkspace(leasedUri)
  72 |     }
> 73 |     setBusy(true); setError(false)
     |     ^^^^^^^ Avoid calling setState() directly within an effect
  74 |     void (async () => {
  75 |       if (!connection) throw new Error("disconnected")
  76 |       const uri = await registerRuntimeWorkspace(connection.owner, workspace.canonicalPath, () => useHostStore.getState().hosts[workspace.hostId]?.connection === connection)  react-hooks/set-state-in-effect
  84:48  warning  The ref value 'generation.current' will likely have changed by the time this effect cleanup function runs. If this ref points to a node rendered by React, copy 'generation.current' to a variable inside the effect, and use that variable in the cleanup function                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               react-hooks/exhaustive-deps

<repo>/src/components/ui/dialog.tsx
  253:3  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/hooks/useResizableDialogSize.ts
  72:5  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/hooks/useResizableDialogSize.ts:72:5
  70 |
  71 |   useEffect(() => {
> 72 |     setPreference(loadDialogSizePreference(resizeId))
     |     ^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  73 |     setTransientSize(null)
  74 |     dragRef.current = null
  75 |     setIsResizing(false)  react-hooks/set-state-in-effect

<repo>/src/workbench/AskpassHost.tsx
  90:9  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/AskpassHost.tsx:90:9
  88 |     // for one prompt can never leak into the next.
  89 |     useEffect(() => {
> 90 |         setValue("")
     |         ^^^^^^^^ Avoid calling setState() directly within an effect
  91 |     }, [current?.id])
  92 |
  93 |     function advance(id: number, response: string | null) {  react-hooks/set-state-in-effect

<repo>/src/workbench/ExternalChangeResolver.tsx
  33:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/workbench/FolderPickerHost.tsx
  59:5   warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/FolderPickerHost.tsx:59:5
  57 |
  58 |   useEffect(() => {
> 59 |     setListing(null); setPath(recent?.hostId === activeHostId ? recent.path : "."); setError(null); setBusy(false)
     |     ^^^^^^^^^^ Avoid calling setState() directly within an effect
  60 |     return () => { browseGeneration.current++ }
  61 |   }, [activeHostId, session?.sessionId, recent, location, access])
  62 |  react-hooks/set-state-in-effect
  60:37  warning  The ref value 'browseGeneration.current' will likely have changed by the time this effect cleanup function runs. If this ref points to a node rendered by React, copy 'browseGeneration.current' to a variable inside the effect, and use that variable in the cleanup function                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     react-hooks/exhaustive-deps

<repo>/src/workbench/ImageView.tsx
   16:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      react-refresh/only-export-components
   63:9   warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/ImageView.tsx:63:9
  61 |     useEffect(() => {
  62 |         let disposed = false
> 63 |         setByteSize(null)
     |         ^^^^^^^^^^^ Avoid calling setState() directly within an effect
  64 |         setLoadError(false)
  65 |         setDims(null)
  66 |         setRemoteImage(null)  react-hooks/set-state-in-effect
  125:5   warning  Error: Cannot access refs during render

React refs are values that are not needed for rendering. Refs should only be accessed outside of render, such as in event handlers or effects. Accessing a ref value (the `current` property) during render can cause your component not to update as expected (https://react.dev/reference/react/useRef).

<repo>/src/workbench/ImageView.tsx:125:5
  123 |     // the listener always sees the current zoom without re-attaching.
  124 |     const zoomByRef = useRef(zoomBy)
> 125 |     zoomByRef.current = zoomBy
      |     ^^^^^^^^^^^^^^^^^ Cannot update ref during render
  126 |     useEffect(() => {
  127 |         const container = containerRef.current
  128 |         if (!container) return                                                                                                                                                                                                                                                                                      react-hooks/refs

<repo>/src/workbench/MarkdownPreview.tsx
  184:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 react-refresh/only-export-components
  188:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 react-refresh/only-export-components
  206:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 react-refresh/only-export-components
  213:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 react-refresh/only-export-components
  250:9   warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/MarkdownPreview.tsx:250:9
  248 |     useEffect(() => {
  249 |         let disposed = false
> 250 |         setLoadError(false)
      |         ^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  251 |         void getDocument(sourcePath)
  252 |             .then((entry) => {
  253 |                 if (disposed) return  react-hooks/set-state-in-effect

<repo>/src/workbench/SvgSplitView.tsx
  28:10  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         react-refresh/only-export-components
  30:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         react-refresh/only-export-components
  56:9   warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/SvgSplitView.tsx:56:9
  54 |     useEffect(() => {
  55 |         let disposed = false
> 56 |         setLoadError(false)
     |         ^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  57 |         setRenderError(false)
  58 |         setContent(null)
  59 |         void getDocument(path)                                        react-hooks/set-state-in-effect
  98:13  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/SvgSplitView.tsx:98:13
   96 |     useEffect(() => {
   97 |         if (content === null) {
>  98 |             setUrl(null)
      |             ^^^^^^ Avoid calling setState() directly within an effect
   99 |             return
  100 |         }
  101 |         const next = URL.createObjectURL(new Blob([content], { type: "image/svg+xml" }))  react-hooks/set-state-in-effect

<repo>/src/workbench/git/BranchPopover.tsx
  264:13  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/git/BranchPopover.tsx:264:13
  262 |     useEffect(() => {
  263 |         if (!activeItems.some((item) => item.name === selectedRef)) {
> 264 |             setSelectedRef(activeItems[0]?.name ?? "")
      |             ^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  265 |         }
  266 |     }, [activeItems, selectedRef])
  267 |  react-hooks/set-state-in-effect

<repo>/src/workbench/git/DiffModal.tsx
  290:13  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/git/DiffModal.tsx:290:13
  288 |     useEffect(() => {
  289 |         if (!source || !activeRow) {
> 290 |             setLoadState(null)
      |             ^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  291 |             return
  292 |         }
  293 |         const identity = activeIdentity  react-hooks/set-state-in-effect

<repo>/src/workbench/git/DiffView.tsx
  172:8  warning  React Hook useEffect has a missing dependency: 'langExt'. Either include it or remove the dependency array  react-hooks/exhaustive-deps

<repo>/src/workbench/git/GitRollbackDialog.tsx
  89:9  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/git/GitRollbackDialog.tsx:89:9
  87 |
  88 |     useEffect(() => {
> 89 |         setDeleteUntrackedOrAdded(false)
     |         ^^^^^^^^^^^^^^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  90 |         setSubmitting(false)
  91 |     }, [pending?.requestId])
  92 |  react-hooks/set-state-in-effect

<repo>/src/workbench/git/LocalChangesTab.tsx
  229:13  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/git/LocalChangesTab.tsx:229:13
  227 |     useEffect(() => {
  228 |         if (!repositoryRoot || !selectedPath || !selectedRow) {
> 229 |             setLoadState(null)
      |             ^^^^^^^^^^^^ Avoid calling setState() directly within an effect
  230 |             return
  231 |         }
  232 |         const identity = loadIdentity  react-hooks/set-state-in-effect
  251:8   warning  React Hook useEffect has a missing dependency: 'selectedRow'. Either include it or remove the dependency array                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          react-hooks/exhaustive-deps

<repo>/src/workbench/git/LogTab.tsx
  189:9  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/git/LogTab.tsx:189:9
  187 |         }
  188 |         useGitLogStore.getState().reset({ preserveAuthorSession: true })
> 189 |         setQuery("")
      |         ^^^^^^^^ Avoid calling setState() directly within an effect
  190 |         firstHead.current = true
  191 |         // Depend on hasStatus, not the whole status object — ordinary refresh
  192 |         // must not wipe the log list/selection  react-hooks/set-state-in-effect
  194:9  warning  Unused eslint-disable directive (no problems were reported from 'react-hooks/exhaustive-deps')
  205:9  warning  Unused eslint-disable directive (no problems were reported from 'react-hooks/exhaustive-deps')

<repo>/src/workbench/git/VirtualizedGitChangeList.tsx
  34:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/workbench/git/fileRows.tsx
  25:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components
  49:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components
  60:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components
  73:17  warning  Fast refresh only works when a file only exports components. Use a new file to share constants or functions between components  react-refresh/only-export-components

<repo>/src/workbench/search/useWorkspaceSearch.ts
  39:13  warning  Error: Calling setState synchronously within an effect can trigger cascading renders

Effects are intended to synchronize state between React and external systems such as manually updating the DOM, state management libraries, or other platform APIs. In general, the body of an effect should do one or both of the following:
* Update external systems with the latest state from React.
* Subscribe for updates from some external system, calling setState in a callback function when external state changes.

Calling setState synchronously within an effect body causes cascading renders that can hurt performance, and is not recommended. (https://react.dev/learn/you-might-not-need-an-effect).

<repo>/src/workbench/search/useWorkspaceSearch.ts:39:13
  37 |     useEffect(() => {
  38 |         if (!workspacePath || trimmed.length < MIN_QUERY_LEN) {
> 39 |             setEvents([])
     |             ^^^^^^^^^ Avoid calling setState() directly within an effect
  40 |             setLoading(false)
  41 |             // Leaving/clearing search: fire an empty search so the Rust generation
  42 |             // advances and any still-running query sees a stale generation and stops  react-hooks/set-state-in-effect

✖ 49 problems (0 errors, 49 warnings)
  0 errors and 2 warnings potentially fixable with the `--fix` option.

