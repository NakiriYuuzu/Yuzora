# LOOP RUN — m3-wave3

- 日期：2026-07-04
- 狀態：DONE
- 任務原文：
  > （沿使用者指示「繼續進行下一個 wave，直到 m3 完結」；三約束：opus subagent／git 唯讀／無 worktree）
- **跨 run 指令備忘**：M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示）。
- **使用者裁決（跨 run 適用）**：P4 對抗 review 用 ultracode Workflow（fresh opus reviewer fan-out＋雙反駁驗證＋loop-until-dry）；實作 fan-out 可經 Workflow 併行（檔案白名單零交集前提）；修復回派原 implementer；M3 完結後最終 review 派 fable agents。

## P1 提問

**需求（可驗證陳述）**
- R1: T7 code actions——src/lsp/codeActions.ts＋test。codeActionsFor（request context.diagnostics 必含 message）／applyWorkspaceEdit（changes＋documentChanges→CM6 changes dispatch，共用 T9 映射）／lspLintSource（@codemirror/lint Diagnostic.actions）。plan :491-533。
- R2: T8 semantic tokens——src/lsp/semanticTokens.ts＋test、src/editor/editor.css。decodeSemanticTokens（delta 五元組解碼）／semanticTokensExtension（Decoration.mark 疊 Lezer baseline，無 provider 降級空 extension）；A5 完整對照 22 token type＋10 modifier 的 cm-st-* class 深淺雙主題。plan :535-559。
- R3: T9 diagnostics pull＋rename compat——src/lsp/diagnosticsPull.ts＋test、src/lsp/renameCompat.ts＋test。diagnosticsPullExtension（capabilities.diagnosticProvider→docChanged debounce ~500ms pull，無 provider 空 extension）／pullDiagnostics／workspaceEditToChanges（changes＋documentChanges 兩格式正規化，供 T7 共用）。plan :561-581。
- R4: 五件套不退化：vitest ≥421（48 檔）、tsc 0、build ✓、cargo 132＋1 ignored、fmt clean。
- R5: P4 dry pass（ultracode fresh opus reviewer fan-out）。

**已查資料**
- plan T7/T8/T9（:491-581）全讀。types.ts 現有 LSP 型別：LspLanguage／LspProcessStatus／LspServerInfo／LspConfig／LspDisplayState／LspInstallProgress——**無** LspDiagnostic／WorkspaceEdit／TextEdit／Range／Position／CodeActionItem／SemanticTokensLegend／DecodedToken。
- lspExtensions.ts 已留 T7/T8/T9 placeholder（:23-25），明文 T10 wire、三任務不共編此檔。
- @codemirror/lint@6.9.7 已在 wave 2 確認為 transitive（可 import Diagnostic.actions）。
- 相依表 wave 3 note（plan）：T7 applyWorkspaceEdit「共用 T9 renameCompat 的映射」。

**假設（非 BLOCKING）**
- A1: 各任務所需 LSP protocol 型別（LspDiagnostic／WorkspaceEdit／TextEdit／Range／Position／CodeActionItem／SemanticTokensLegend／DecodedToken）在**自身模組**定義（避免並行寫 types.ts 衝突；沿 wave 2 慣例）；T10 若需可再 consolidate。
- A2: **分工＝2 並行 implementer**（非 plan 假想的 3），因 T7 import T9 的 workspaceEditToChanges，合併同一 agent 消除 import race，且更耐 opus 基礎設施不穩（Round 12b 教訓）：
  - **Agent A（T9＋T7）**：renameCompat.ts（WorkspaceEdit/TextEdit 型別＋workspaceEditToChanges）→ diagnosticsPull.ts → codeActions.ts（import renameCompat）。白名單：src/lsp/{renameCompat,renameCompat.test,diagnosticsPull,diagnosticsPull.test,codeActions,codeActions.test}.ts。
  - **Agent B（T8）**：semanticTokens.ts＋test、editor.css。白名單：src/lsp/{semanticTokens,semanticTokens.test}.ts、src/editor/editor.css。
  - 零檔案寫交集、零跨 agent import。

**BLOCKING 待問**：無（worktree 沿三約束不用；範圍為新增檔為主，風險低）。

**Gate G1**：✅

## P2 規劃

**Baseline battery**：wave 2 P5 終驗（本 session 親跑）＝本 run baseline：vitest 421/421（48 檔）、tsc 0、build ✓ 1.05s、cargo 132＋1 ignored、FMT CLEAN。

**步驟**
1. 並行派工 2 opus implementer（Agent A=T9+T7 內部序 renameCompat→diagnosticsPull→codeActions；Agent B=T8）→ verify：各回報後 controller 親跑對應測試。
2. controller 五件套整合 → 全綠不退化。
3. P4 ultracode workflow（fresh opus reviewer fan-out＋雙反駁，loop-until-dry）。
4. P5 終驗＋P6 收尾（LEDGER，下接 wave 4）。

**預計變更檔案**
- Agent A: src/lsp/{renameCompat,renameCompat.test,diagnosticsPull,diagnosticsPull.test,codeActions,codeActions.test}.ts
- Agent B: src/lsp/{semanticTokens,semanticTokens.test}.ts、src/editor/editor.css

**不碰範圍（non-goals）**
- 不動 lspExtensions.ts（T10 wire）、不動 types.ts（型別自備）、不動 EditorPane/StatusBar/SettingsDialog、不動 src-tauri/**、不改 spike（唯讀對照）、git 唯讀。

**風險**
- LSP protocol 型別自備可能與未來 types.ts consolidation 分歧／守門：命名比照 LSP spec，T10 consolidate 時對齊。
- semantic tokens delta 解碼跨行累加 off-by-one／守門：TDD 對 spike 範例陣列驗多 token 跨行。
- jsdom 無 layout，Decoration 著色像素／pull debounce 實效不可測／守門：斷言 mark 存在、fake timers 驗 debounce；著色/真 server 歸 T15。

**驗收證據清單（實作前固定，只可加嚴）**
- E1: Agent A 測試綠（workspaceEditToChanges 兩格式／pullDiagnostics provider 分支＋debounce fake timers／codeActionsFor context 含 message／applyWorkspaceEdit 真 EditorView doc 變更）。
- E2: Agent B 測試綠（decodeSemanticTokens 多 token 跨行 delta／semanticTokensExtension 有 provider 產 .cm-st-* mark、無 provider 無 mark／非標準 legend 名安全忽略）＋editor.css 22 type＋10 modifier 雙主題 class。
- E3: 五件套終跑（vitest ≥421／0／✓／≥132+1／CLEAN）。
- E4: P4 dry pass 紀錄。

**Gate G2**：✅

## P3 執行

**Agent B（T8 semantic tokens）— 回報完成、controller 親驗 ✅**
- 變更檔：
  - `src/lsp/semanticTokens.ts`（decodeSemanticTokens delta 累加＋legend 越界安全忽略；semanticTokensExtension：StateField＋ViewPlugin，初次即時＋docChanged 500ms debounce，無 provider/legend 降級空 extension）
  - `src/lsp/semanticTokens.test.ts`（6 測：多 token 跨行/同行累加＋modifier bitmask、type index 越界忽略、非標準名不 throw；真 EditorView 斷言 .cm-st-* mark、無 provider 無 mark、非標準名無 mark）
  - `src/editor/editor.css`（9 role palette 變數雙主題＋23 type class `cm-st-<type>`＋10 modifier class `cm-st-mod-<modifier>`）
- controller 親跑 `bunx vitest run src/lsp/semanticTokens.test.ts` → **Test Files 1 passed / Tests 6 passed**（10:00）。
- tsc full-run 僅 codeActions.test.ts（Agent A 撰寫中）報錯，semanticTokens 檔零錯 —— Agent B type-level clean。
- 假設待裁：modifier class 前綴採 `cm-st-mod-*`（plan 只定 type 為 `cm-st-<type>`，未定 modifier 前綴；避免與 type「modifier」→`cm-st-modifier` 混淆）；程式與 CSS 一致。→ **controller 裁定：接受**（命名清晰、無衝突，T10 wire 時對齊）。

**Agent A（T9＋T7）— 回報完成、controller 親驗 ✅**
- 變更檔：
  - `src/lsp/renameCompat.ts`（自備 Position/Range/TextEdit/WorkspaceEdit/TextDocumentEdit 型別＋`workspaceEditToChanges`：正規化 changes＋documentChanges 兩格式，documentChanges 優先；防禦性過濾無 edits 的 resource ops）
  - `src/lsp/renameCompat.test.ts`（4 案：兩格式映射／documentChanges 優先／空 edit）
  - `src/lsp/diagnosticsPull.ts`（`pullDiagnostics`＋`diagnosticsPullExtension`：偵測 diagnosticProvider，mount 初拉＋docChanged 500ms debounce，setDiagnostics 渲染出口；共用 offsetOf/toSeverity/toLintDiagnostic）
  - `src/lsp/diagnosticsPull.test.ts`（4 案：request 送出／unchanged 回空／無 provider inert／fake-timers debounce 合流）
  - `src/lsp/codeActions.ts`（`codeActionsFor` context.diagnostics 帶 message／`applyWorkspaceEdit` 共用 renameCompat 映射真 view dispatch／`lspLintSource` pull-based lint source＋每 Diagnostic 附 quick-fix action）
  - `src/lsp/codeActions.test.ts`（5 案：message 契約／action 映射／changes＋documentChanges 真 view dispatch／lspLintSource actions）
- controller 親跑三檔 → 併入下方整合 battery（13 測全過）。
- 假設待裁：①diagnosticsPullExtension 補「mount 初拉」（plan 只寫 docChanged debounce，但 pull 模型首開檔無初拉則不顯診斷）→ **接受**（測試已斷言 mount called×1，語意正確）；②位置轉換自備 offsetOf 而非 LSPPlugin.fromPosition（讓裸 view 可測）→ **接受**（生產語意一致）；③applyWorkspaceEdit 單 view 只套符合 uri 的 edits，跨檔屬 workspace 職責 → **接受**（符合分層，留 T10 wire）。

### 整合 battery（controller 親跑，10:01）
- vitest：**Test Files 52 passed / Tests 440 passed**（baseline 421 → +19＝B 6＋A 13，只增）
- tsc --noEmit：**TSC_EXIT=0**
- build：**✓ built in 1.06s**（tsc && vite build）
- cargo test：**132 passed / 1 ignored**（running 133）
- cargo fmt --check：**FMT_EXIT=0**
→ 五件套零退化，E3 銷帳。P3 完成，進 P4。

## P4 對抗 review

**Round 1（ultracode workflow `w7aen2w6b`，10:02 啟動、進行中）**
- 7 lens fresh opus reviewer fan-out：semantic-decode／workspace-edit-order／diagnostics-timing／codeactions-contract／lifecycle-race／css-theme-parity／scope-orphans。
- 每 finding 雙角度反駁（repro＋spec），survives＝至少一反駁者 confirmed；killed＝雙反駁。
- 全數 model:opus（鐵律 2）；reviewer 與撰寫者分離（fresh context）。
- **workflow script bug**：內層 `parallel([agent(...),agent(...)])` 傳 promise 而非 thunk，Verify 階段同步拋錯 → pipeline 把 item 丟 null → 回報假 `survives:0`。依 LEARNINGS「dryPass 可能只代表已完成 agents 中 0 findings」，**不採信此 0**；從 journal.jsonl 撈回 7 reviewer 的 **25 條 raw findings**（5 refuter 完成、全 confirmed，其餘 45 因 bug 崩）。
- 25 raw → **去重 11 cluster**（controller）：
  - **C1 async 回應 staleness（無 generation/cancel guard）**〔major〕：diagnosticsPull.run(:97-103)＋semanticTokens fetch(:167-176)。慢的舊回應覆蓋新的，持續到下次編輯；含 C1a 順序 race＋C1b doc-drift（回應對 resolve-time doc 映射、無 sync()/unsyncedChanges 補償→offset 位移）。
  - **C2 'unchanged' report 清空診斷**〔major〕：diagnosticsPull.ts:70/100。pullDiagnostics 對 unchanged 回 []，caller 清掉全部，與自身 comment「keep what rendered」相反。
  - **C3 applyWorkspaceEdit .find 丟同 uri 多個 TextDocumentEdit**〔minor〕：codeActions.ts:67。
  - **C4 applyWorkspaceEdit 反向/重疊 range → RangeError 未捕捉**〔minor〕：codeActions.ts:76，offsetOf 不 reorder from/to。
  - **C5 semanticTokens 負 delta → RangeSetBuilder.add throw**〔minor〕：semanticTokens.ts:109，與「malformed never throws」註解相反。
  - **C6 lspLintSource 挑錯 action（無 context.only=quickfix）**〔minor〕：codeActions.ts:94/96，apply 第一個帶 edit 的 action 可能是無關 refactor。
  - **C7 lspLintSource 對 resolve-deferred action（無 inline edit）靜默 no-op**〔minor〕：codeActions.ts:98。
  - **C8 CSS modifier additive stacking 壞掉**〔minor〕：editor.css:97/42。deprecated(line-through)＋modification(underline) 同 specificity 互蓋，與「stack as additive」註解相反。
  - **C9 lspLintSource 測試假綠**〔minor/test〕：codeActions.test.ts:103，只斷言 actions.length===1、從不觸發 apply，mock 對 codeAction 回 [] 故 apply 跑了也 no-op still pass。
  - **C10 orphan：toSeverity export 無外部消費**〔minor〕：diagnosticsPull.ts:43。
  - **C11 i18n：'Quick fix' 英文 label**〔minor〕：codeActions.ts:94，違 UI 文字中文慣例。
- → 對 11 cluster 跑雙反駁 refutation workflow（`wk21ngeo8`，22 opus refuter），survives=至少一 confirmed。

**Round 1 refutation 結果：9 survives / 2 killed**（controller 逐條親驗讀碼確認）
- **KILLED（drop，不修）**：
  - **C4** 反向 range：唯一 caller lspLintSource 用 `void ...then().catch(()=>{})` 包住，RangeError 被吞非 uncaught crash；反向 range 非 server 真實輸入。親驗 codeActions.ts:95-101 屬實。
  - **C7** resolve-deferred action：不可達——@codemirror/lsp-client 預設 clientCapabilities 不宣告 resolveSupport.properties 含 edit，server 恆回 inline edit。
- **SURVIVES（9，全修，親驗屬實）**：
  - **C1**〔major〕diagnosticsPull.run()（:97-108）＋semanticTokens.fetch()（:167-182）皆無 generation/in-flight guard、對 resolve-time current doc 映射。→ 修：加 generation counter（docChanged ++／fetch 捕捉／resolve 比對不符即 drop），一次解 stale-wins＋doc-drift。**A（diagnosticsPull）＋B（semanticTokens）分修**。
  - **C2**〔major→實為 latent〕pullDiagnostics（:70）unchanged 回 []→run()（:100）setDiagnostics 清空，違自身註解「keep rendered」；且 :66-69 從不送 previousResultId 故 spec 上現不可達（dead branch＋誤導註解）。→ 修：unchanged 回 null、run()/lspLintSource 遇 null 跳過 dispatch（keep rendered），並正註解。**A**。
  - **C3**〔minor〕applyWorkspaceEdit（:67）`.find` 只取首個 uri，documentChanges 同 uri 多 TextDocumentEdit 其餘丟失（spec 合法）。→ 修：filter＋flatMap 全 edits。**A**。
  - **C5**〔minor〕buildDecorations（:95-112）per-token 檢查 range 但未檢查跨 token from 單調遞增，負 delta 逆序 token → RangeSetBuilder.add（:109）throw，違 :93-94「never throws」。→ 修：track lastFrom、逆序 token skip，正註解。**B**。
  - **C6**〔minor〕codeActionsFor（:39-53）codeAction request 無 `context.only=['quickfix']`＋lspLintSource（:98）取首個帶 edit 的 action，可能套到無關 refactor。→ 修：加 only:['quickfix']。**A**。
  - **C8**〔minor〕editor.css :97 deprecated(line-through)＋:100 modification(underline) 同 specificity 互蓋，違 :42「additive」。→ 修：加 `.cm-st-mod-deprecated.cm-st-mod-modification{text-decoration:underline line-through}` combinator。**B**。
  - **C9**〔minor/test〕codeActions.test.ts:103 lspLintSource 測只斷言 actions.length===1、從不觸發 apply，mock 對 codeAction 回 [] 故假綠。→ 修：mock 回帶 edit 的 action、觸發 apply、斷言 view doc 變更。**A**。
  - **C10**〔minor/orphan〕toSeverity（:43）export 僅內部消費（grep 證實）。→ 修：移除 export。**A**。
  - **C11**〔minor/i18n〕lspLintSource label "Quick fix" 英文，違 UI 中文慣例。→ 修：改中文（走 src/lib/i18n.ts 字串表）。**A**。
- 派修：SendMessage 回原 implementer（A=a6c7469008ea9eb72／B=a030cec4ed86e03d8）；修畢 controller 親驗＋五件套 → 全新 reviewer 整輪重跑（Round 2）loop-until-dry。

**修復回報（Round 1 → fix）**
- **Agent B ✅（C1b/C5/C8）**：semanticTokens.ts 加 generation guard（docChanged ++／fetch 捕捉／dispatch 前比對丟 stale）＋buildDecorations lastFrom 單調守衛（逆序 token skip、兌現 never-throws、正註解）；editor.css 加 `.cm-st-mod-deprecated.cm-st-mod-modification{text-decoration:underline line-through}`。TDD red（2 測失敗）→green。controller 親跑 `bunx vitest run src/lsp/semanticTokens.test.ts` → **8 passed（6→8）**（10:41）。tsc 自身檔零錯、vite build exit 0。C8 視覺疊加 jsdom 測不到 → 併入 T15 gui-acceptance。
- **Agent A ✅（C1a/C2/C3/C6/C9/C10/C11）**：i18n.ts 加 `strings.lsp.quickFix="快速修正"`；diagnosticsPull.ts generation guard（gen 捕捉＋null-skip＋gen-skip）＋pullDiagnostics 回 `[]｜null`＋toSeverity 去 export；codeActions.ts only:["quickfix"]＋applyWorkspaceEdit filter+flatMap 全 edits＋lspLintSource forEachDiagnostic 保留前次＋中文 label。TDD red（7 紅）→green。
  - **計畫外必要改動（Agent A 正確攔截、controller 裁定接受）**：C2 原指示 lspLintSource `return null`，但已安裝 `@codemirror/lint@6` 的 LintSource 型別不含 null，且執行期 `annotations.reduce(concat)` 會把 null 塞進診斷陣列致 `null.from` 崩潰。改採安全等價：unchanged 時 `forEachDiagnostic` 讀回現有診斷回傳（語意＝保留前次，型別/執行期皆安全）；自控 dispatch 的 diagnosticsPullExtension 仍照 null→skip。controller 讀碼親驗無虞、接受。

### 整合 battery（controller 親跑，10:43，兩 agent 修畢）
- vitest：**52 passed / 447 passed**（baseline 421 → +26 只增：R1 實作 +19、fix +7）
- tsc --noEmit：**TSC_EXIT=0**
- build：**✓**（僅 chunk-size warning）
- cargo test：**132 passed / 1 ignored**
- cargo fmt --check：**FMT_EXIT=0**
→ 五件套零退化。Round 1 修復完成，進 **Round 2 全新 reviewer**（loop-until-dry）。

## P4 對抗 review（續：Round 2 dry pass）

**Round 2（workflow `wu523jl5f`，4 全新 opus lens：fix-correctness／test-mutation／fresh-full／integration-regression）＋雙反駁**
- 內層 parallel 改正確 thunk 語法（Round 1 的假 0 bug 已修）。
- 結果：**4 reviewer 全完成、各 findings=0、0 error、無 failures**；refutations 16/12/16/17（共 61 條「試圖打破但通過」）。journal 親核 4 lens 皆真完成回空——**真 dry pass**（非假 0）。
- **Gate G4 ✅**：dry pass＋反證≥3（61）＋每 finding 有裁決（R1：9 修＋2 killed；R2：0）。

## P5 驗收

**E1（Agent A 測試綠）✅**：diagnosticsPull.test＋codeActions.test 併入整合，14 passed（含 generation guard stale-wins、unchanged→null 保留、applyWorkspaceEdit 同 uri 多 edit、only:quickfix、lspLintSource 全 apply 鏈）。
**E2（Agent B 測試綠）✅**：semanticTokens.test 8 passed（含 generation guard 丟 stale、out-of-order token 不 throw）；editor.css 23 type＋10 modifier＋combinator additive class。
**E3（五件套終跑，controller 親跑 11:00）✅**：
- vitest：**52 passed / 447 passed**（baseline 421 → +26 只增）
- tsc --noEmit：**TSC_EXIT=0**
- build：**✓ built in 903ms**
- cargo test：**132 passed / 1 ignored**
- cargo fmt --check：**FMT_EXIT=0**
**E4（P4 dry pass 紀錄）✅**：Round 2 真 dry pass（上記）。
**Gate G5 ✅**：全 E 有貼證據且 PASS。

## P6 收尾

- **狀態：DONE**
- **最終變更檔案**（wave 3 全部）：
  - Agent A：src/lib/i18n.ts（+lsp.quickFix）、src/lsp/renameCompat.ts＋.test、src/lsp/diagnosticsPull.ts＋.test、src/lsp/codeActions.ts＋.test
  - Agent B：src/lsp/semanticTokens.ts＋.test、src/editor/editor.css
- **證據摘要**：五件套 447/0/✓/132+1/clean（baseline 421/132 只增）；P4 兩輪對抗 review——R1 撈 25 raw→11 cluster、9 修 2 killed；R2 4 全新 lens 真 dry pass（61 反證）。
- **需使用者確認的假設**：A1 型別自模組定義（T10 可 consolidate）；A2 分工 2 implementer（非 plan 假想 3）；C2 偏差採 forEachDiagnostic 重發（因 @codemirror/lint@6 LintSource 型別不含 null）——皆 controller 裁定接受。
- **殘留事項**：
  - C8 CSS modifier additive 視覺疊加、semantic token 真著色、pull/semantic drift 於真 server 的實效 → **T15 gui-acceptance 實機驗**。
  - doc-drift 完整補償（client.sync()＋unsyncedChanges mapPos，比照官方 push path）本輪以 generation guard（doc 變即丟棄 stale 回應並重拉）覆蓋主要風險；完整 sync 整合留 **T10 wire**（autoSync/serverDiagnostics 掛載時）。
- **自我改進三問**：
  1. 哪個閘門沒攔到？→ ultracode workflow 內層 parallel 傳 promise（非 thunk）致 Verify 全崩、回報假 dryPass:0——無閘門攔，靠 LEARNINGS「dry-pass 須核所有 lens 完成」＋讀 journal 才識破。→ 追加 LEARNINGS [trap]。
  2. 哪條規則浪費時間？→ 無；反而「核 dry-pass 完成度」這條這輪直接救了假 0，不放寬。
  3. 學到陷阱/慣例？→ (a) workflow 內層 parallel 必用 thunk；(b) 派修 brief 授權 implementer「指示若不安全須停下回報」奏效（Agent A 攔 C2 型別不安全）。→ 追加 LEARNINGS。
- **下接 wave 4**：T10 EditorPane 整合＋symbols＋focus（brief 必載 4 硬性需求：①rename/WorkspaceEdit 套用前預載所有 target URI；②split-pane per-view refcount / LSP plugin 掛載政策；③tooltip anchor 點擊攔截於 editor mount 層；④background-file 升格 stale doc/version 調和）。
