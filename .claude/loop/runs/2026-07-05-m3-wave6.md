# LOOP RUN — m3-wave6

- 日期：2026-07-05
- 狀態：DONE
- 任務原文：
  > （沿使用者指示「繼續進行下一個 wave，直到 m3 完結」；三約束：opus subagent／git 唯讀／無 worktree；P4 純 opus subagent fresh reviewer）
- **跨 run 指令備忘**：M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示）。

## P1 提問

**需求（可驗證陳述）**
- R1（T14 lsp_download.rs）：`lsp_install_server(language)` 第 10 個 handler——七個 curated adapter 一鍵安裝三路徑（binary×3：rust-analyzer .gz→flate2／marksman／markdown-oxide 裸 binary，官方 release 下載至 ~/.yuzora/servers/＋SHA256＋chmod+x＋macOS xattr 去 quarantine；npm×3：偵測 npm→私有 prefix ~/.yuzora/servers/npm；pip×1：pylsp 偵測 python3→venv ~/.yuzora/servers/pyenv）；背景執行緒（A2）；全程 emit `lsp:install-progress`（phase download/verify/unpack/npm/pip/done/error＋percent）；成功 emit `lsp:server-status`；失敗明確錯誤（T12 顯示 manual fallback）。plan :698-723。
- R2（T14 測試邊界）：**不觸網不跑 npm**——URL 組裝（platform 對照）、SHA 比對、解壓分流、quarantine 指令、npm/venv+pip 指令與 bin 路徑組裝、無 npm/無 python 錯誤分支，全抽純函式 cargo 測。
- R3（wave 5 殘留契約，T14 必含）：①每路徑必以 done/error 終態收尾（可測處加契約測試）；②emit error 必伴 command Err；③per-language in-flight guard（防 double-install）；④無 npm→「需先安裝 Node.js」/無 python3→「需先安裝 Python」明確錯誤；⑤成功後 emit server-status 補償 redetect。
- R4（T15）：`fixtures/gen-lsp-fixtures.ts`（fixtures/out/lsp-demo 四語言最小專案：ts-app 跨檔＋型別錯＋unused import／py-app 型別錯／rs-app cargo package 跨檔＋型別錯／md-doc）＋`docs/superpowers/checklists/m3-manual-checklist.md`（plan :739-750 全項＋各 wave 累積 T15 實機清單合併）。
- R5：五件套不退化：vitest ≥530 無 Errors、tsc 0、build ✓、cargo ≥132+1、fmt 0。
- R6：P4 dry pass（純 opus fresh reviewer，loop-until-dry）。

**已查資料（本 session 親核）**
- plan T14（:698-723）/T15（:727-758）/自動 vs 手動清單（:762-775）/風險表（:777-792）/A2/A9/A9' 裁決（:797,:804-805）全讀。
- lib.rs `pub mod lsp_download;` 已於 wave 0 預註冊（stub 檔存在）；lib.rs 增 handler 本 wave 僅 T14 一人動，無競改。
- types.ts:90 `LspInstallProgress` phase enum 前端已定（download/verify/unpack/npm/pip/done/error）；ipc.ts lspInstallServer wrapper 已定（wave 5 mock 測）；SettingsDialog install listener（A-F1 終態語意）已就緒等 producer。
- lsp_service.rs:298 emit 模式（app.emit("lsp:server-status", info)）可援用；which() 解析序（~/.yuzora/servers 優先）T4 已配合。
- wave 5 P6 殘留六項已列 R3。
- baseline（12:18 P5 終驗＝本 run baseline）：vitest 530 無 Errors／tsc 0／build ✓ 1.06s／cargo 132+1／fmt 0。

**假設（非 BLOCKING）**
- A1：T14 與 T15（Step1-3）**並行派工**——檔案零交集（src-tauri/* vs fixtures/*＋docs/checklists/*）；T15 Step 4 收尾全綠由 controller 於 T14 落地後執行。
- A2：Rust 端 LspInstallProgress 型別落點（lsp_download.rs 或共用處）由 implementer 依現有慣例定，serde camelCase 與 types.ts:90 對齊。
- A3：真實下載/npm/pip 執行列 T15 實機（gui-acceptance），CI 永不觸網（cargo build 抓 crates.io 依賴除外）。
- A4：worktree 不用（沿全 M3 慣例，同 repo 分檔並行）。

**BLOCKING 待問**：無（範圍全由 A9/A9'/A2 既決，plan 已核准）。

**Gate G1**：✅

## P2 規劃

**Baseline battery**（wave 5 P5 終驗＝本 run baseline，controller 親跑 12:18）：
```
vitest：59 files / 530 passed（無 Errors 行）
tsc --noEmit：EXIT=0
build：✓ built in 1.06s
cargo test：132 passed / 1 ignored
cargo fmt --check：EXIT=0
```

**步驟**
1. 並行派 T14（opus implementer，TDD red→green）＋T15 Step1-3（opus implementer）。
2. controller 親驗各自證據；T14 落地後跑 T15 Step 4 收尾全綠（五件套）。
3. P4 純 opus fresh reviewer fan-out（T14 Rust 域＋T15 fixture/checklist 域）→ 裁決 → 回派修 → loop-until-dry。
4. P5 終驗＋P6 收尾（LEDGER；M3 全 wave 完結 → 最終 review 派 fable）。

**預計變更檔案**
- T14：src-tauri/src/lsp_download.rs（新）、src-tauri/src/lib.rs（第 10 handler）、src-tauri/Cargo.toml（+ureq+sha2+flate2）。
- T15：fixtures/gen-lsp-fixtures.ts（新）、docs/superpowers/checklists/m3-manual-checklist.md（新）。

**不碰範圍（non-goals）**
- 不動 src/（前端 wave 5 定稿；install UI 已就緒等事件）、不動 lsp_service.rs/lsp_adapters.rs/lsp_config.rs（僅讀）、git 唯讀、CI 不觸網。

**風險**
- Cargo 新依賴編譯時間/版本相容／守門：cargo test 全綠＋fmt。
- lib.rs handler 增量與既有 9 handler 相容／守門：cargo test＋tsc（前端 wrapper 已 mock 測）。
- 進度事件契約與前端 listener 對齊（phase 字串、camelCase）／守門：serde 測試對照 types.ts:90。
- fixtures TS 檔進 tsc 範圍／守門：tsc 0（沿既有 gen-*fixtures.ts 慣例）。

**驗收證據清單（實作前固定，只可加嚴）**
- E1：T14 cargo 測綠（7 adapter 分流／URL platform 對照／SHA 比對／解壓分流／quarantine 指令／npm+venv 指令與 bin 路徑／無 npm/python 錯誤／終態契約／in-flight guard）。
- E2：T15 fixture 產生器可重複執行、四語言目錄齊備（`bun run fixtures/gen-lsp-fixtures.ts && ls`）；checklist 涵蓋 plan :739-750 逐項＋各 wave 累積實機清單。
- E3：五件套終跑（vitest ≥530 無 Errors／tsc 0／build ✓／cargo ≥132+1 含新測／fmt 0）。
- E4：P4 dry pass 紀錄（每 finding 有裁決）。

**Gate G2**：✅

## P3 執行

**並行派工（12:3x）**：w6-t14-install（lsp_download.rs＋lib.rs 第 10 handler＋Cargo.toml；brief 含 A2/A9/A9' 契約全文＋wave5 殘留三契約＋純函式測試邊界＋serde camelCase 對照＋lsp_service harness 模式指引＋SHA 策略授權）；w6-t15-fixtures（gen-lsp-fixtures.ts＋m3-manual-checklist.md；brief 含 plan :727-758 逐項＋「讀 wave2-5 run files 合併累積實機殘留」指令）。等回報。

**T15 Step1-3 回報＋controller 親驗 ✅（12:5x）**
- gen-lsp-fixtures.ts：四語言最小專案（ts-app 跨檔＋TS2345 型別錯＋TS6133 unused import——implementer 以獨立 tsc -p 佐證兩診斷真觸發；py-app pyright 型別錯；rs-app 獨立 Cargo package 跨檔型別錯——已核不被 ancestor workspace 吸收；md-doc）。controller 親跑：冪等重跑 8 檔無堆疊、四目錄齊備、repo 級 tsc 0、fixtures/out gitignored 確認。
- m3-manual-checklist.md（202 行 28 節）：環境準備（debug bundle→/Applications＋fixture 前置）／自動已驗引用（wave5 基線 530/132+1＋自動≠手動邊界聲明）／12 主項（MVP-2/3、features 十項、四語言、Settings、大檔、退避、preview、一鍵安裝三路徑對齊 T14 契約、雙主題、A1 跨檔 rename、截圖存證）／歷史累積 A-H 章（W2-W5 殘留逐項對照收錄：contain:paint、CSS additive、semantic 著色、Radix 雙 dialog、focus、cold-start、五態色票、install 進度視覺、trace 實效、reload-missing 實機流等）。
- IDE 單檔 TS2591（node:fs 無 @types/node）＝fixtures/ 不在 tsconfig include 的既有待遇（同 gen-fixtures 等三檔），repo 級 tsc 0 為準。
- T15 Step 4（收尾全綠＋tauri:dev smoke）依計畫待 T14 落地後 controller 執行。
- 等 w6-t14-install 回報。

**T14 回報＋controller 親驗 ✅（12:5x）**
- 落地：route_for 七 adapter 三路徑分流表；binary 路（ureq 下載→SHA256 記錄→gz/裸分流→~/.yuzora/servers/→chmod 0755→macOS xattr）；npm 路（which 偵測→私有 prefix）；pip 路（venv ~/.yuzora/servers/pyenv）；`finalize()` wrapper 結構性保證終態契約（do_install 只 emit 非終態；done/error 僅 wrapper 產、error⟺Err 同 match 臂——兩契約各有測）；InflightGuard（OnceLock<Mutex<HashSet>>，RAII drop）；成功 emit server-status（status=Stopped、path=which 落點）；lib.rs:91 第 10 handler；Cargo.toml +ureq/sha2/flate2。29 新測（TDD red 27 failed→green）。
- **與 plan/官方不符（implementer 記錄，待 P4 覆核）**：①RA Windows 官方是 .zip 非 .gz——URL 照官方、Windows RA 誠實 Err 導引手動（macOS 驗收平台不影響）；②SHA256 採「下載時計算記錄」＋`expected_sha256()` pin 硬化掛勾（上游無穩定 checksum manifest，NIST 向量測比對函式）；③三 binary pin 現行版本常數；④install 無 workspace 參數→resolve_active 走 global default→adapter default，**不吃 workspace override（記限制，P4 須驗 UI 端會不會裝錯 profile）**；⑤`lsp:server-status` emit 會被 LspBridge workspace 過濾（空字串 workspace）——前端實際更新走 command 回傳→handleInstall setServerInfo，emit 保留 best-effort（**P4 須覆核此鏈**）。
- controller 親驗：cargo **161 passed／1 ignored**（132→161 只增）＋fmt 0；vitest 530 無 Errors；tsc 0；build ✓ 1.02s；NUL 掃描（**含 src-tauri/src**）乾淨；spot-read finalize/InflightGuard/route_for/pin hook/handler/deps 全部吻合回報。
→ P3 完成（T15 Step 4 的 tauri:dev smoke 移至 P5 終驗執行，覆蓋最終碼），進 P4。

## P4 對抗 review（更新）

**Round 1（2 全新 opus reviewer 並行，13:0x 派出）**
- Lens A（w6r1-rust）：lsp_download.rs 全檔對抗——finalize 逃逸路徑（panic/early-return 繞過終態？unwrap/expect 盤點）、InflightGuard 競態與 poison、route/URL 對照 lsp_adapters＋官方慣例、SHA 策略、which 落點對齊、**④install 不吃 workspace override→UI 裝錯 profile 情境**、**⑤emit 被 LspBridge 過濾的宣稱與前端更新鏈**、lib.rs/Cargo.toml、29 測 mutation。
- Lens B（w6r1-fixtures）：fixture 四語言正確性（rs-app cargo check 須 E0308 型別錯而非語法/manifest 錯；py 錯誤 pyright 真會報）、checklist 對 plan :739-750 完備性＋累積殘留對照 run files 真實性、指令/路徑正確性。

**w6r1-fixtures 回報（2 findings＋8 反證 hold；六檢點餘皆乾淨）— controller 親驗裁決（13:2x）**
- fixture 本體全乾淨（實跑：ts-app TS2345+TS6133 命中；rs-app cargo check --offline 唯一 E0308 跨檔指向 math.rs 非 manifest 錯；冪等兩跑逐位元組相同；四 wave 彙整清單 22 項全收、抽驗 11 項語意未失真；phase 序列與 types.ts/T14 InstallPhase 逐一對齊；log/npm bin/lsp.json 路徑對齊）。
- **W6B-F1〔major〕checklist :12 `bun run build # 產 debug bundle` 假宣稱 — 確認修（親驗 package.json:8=tsc+vite build；gui-acceptance SKILL:25-26 正解）**：改 `bun run tauri build --debug --bundles app`＋ditto 兩步。
- **W6B-F2〔minor〕H 節漏收 wave4 H4（background-file 升格 stale version 窗口）— 確認修（親驗 wave4:39/:97 原文在、checklist :194-196 無）**：H 節補一列。
- 派修 → w6-t15-fixtures（僅 checklist md，與 rust 域審查零交集，即時派）。等 w6r1-rust。

**W6B-F1/F2 修復回報＋controller 親驗 ✅（13:3x）**：:14-15 改正 tauri build --debug --bundles app＋ditto 兩步（無 bun run build 假宣稱殘留）、/mcp reconnect 精確化、:206 footer 刪除提醒（SKILL:29 慣例）；:198 H4 補列語意忠實 wave4:39。

**w6r1-rust 回報（2 major＋2 minor＋2 nit；偏差②③⑤覆核 holds；finalize/InflightGuard/which/route 對齊全 hold）— controller 親驗裁決（13:3x）**
- **W6A-F1〔major/breaks-requirement〕install 無視 workspace override（偏差④判不可接受）— 確認修**。親驗：resolve_active（:124-129）只讀 cfg.defaults→adapter default，從不讀 workspaces；handler（:660-663）與 ipc.ts:212 無 workspace 參數；而 `lsp_config::resolve_server`（:39-45，override→defaults）現成有測。情境成立：scope 預設 workspace、override python=pylsp、卡顯示 pylsp、點安裝→裝 pyright→pylsp 永遠 Missing＝死路，違反 A9'。修（兩半，簽名契約 controller 定死）：前端 `lspInstallServer(workspace: string|null, language)` 傳 workspacePath；Rust handler +`workspace: Option<String>`，canonical 化（與 set_server 寫入側同一 helper，防 raw-vs-canonical miss）後走 resolve_server→adapter default。
- **W6A-F2〔major〕handler 非 async 跑 main thread，違反 A2 — 確認修**。親驗 :659-663 `#[tauri::command] pub fn`＋註解「off the UI thread」與 Tauri v2 事實相反（non-async command 在 main thread）；body 含 blocking ureq＋npm/pip 子行程（分鐘級）→ 安裝期間全部 IPC 餓死。既有全 codebase sync command 慣例屬實，但 A2 為明列裁決且量級不同。修：async fn＋`tauri::async_runtime::spawn_blocking`（官方文件為準）。
- **W6A-F3〔minor〕binary 就地 truncate 執行中 binary — 確認修**（:553 fs::write 直寫 dest）：temp＋rename 原子換位。
- **W6A-F4〔minor〕download 無 timeout/size cap — 確認修**（:459-491 無 agent timeout、buf 無上限）：ureq agent timeout＋讀取上限常數，超出 Err。
- **W6A-F5〔nit〕Windows RA 先下載才 Err — 順手修**：unpack_kind=Zip 於下載前短路 Err。
- **W6A-F6〔nit〕server-status emit workspace="" 恆被 LspBridge 濾（偏差⑤ harmless 成立）— 隨 F1 修**：handler 有 workspace 後 Some 時填實；null 維持（回傳值直寫仍為主通道）。
- 派修分工（檔域零交集並行）：Rust 半（F1-Rust/F2/F3/F4/F5/F6）→ w6-t14-install；前端半（F1-前端：ipc.ts 簽名＋handleInstall 傳參＋mock 測）→ w5-t12-settings（settings 檔域原 owner）。

**F1 前端半回報（w5-t12-settings，13:5x）— 待整合驗**：lspInstallServer(workspace, language) 新簽名＋handleInstall 傳 `workspacePath ?? null`（grep 核唯一 prod 呼叫者）；ipc.test＋settingsDialog.lsp.test 各補 workspace/null 兩案（TDD 4 紅→綠）。自證三檔 72/72、tsc 0、全量 532 無 Errors。等 Rust 半。

**Rust 半回報（w6-t14-install，13:2x-14:2x）＋controller 整合驗 ✅**
- F1：resolve_active 收 Option<&str> canonical → `lsp_config::resolve_server` → adapter default；`canonical_key()` 與 set_server 寫入側同 helper；canonical 一致性測用 tempdir（macOS symlinked tmp 使 raw≠canonical）證 raw 傳入命中 canonical 寫入。F6：emit info.workspace 填 raw（LspBridge 以 raw 比對）。F2：async fn＋spawn_blocking（guard 不跨 await、JoinError→Err 收斂、context7 核官方寫法）。F3：sibling .tmp→chmod→xattr→rename（running server 留舊 inode）。F4：AgentBuilder connect 30s/read 60s＋`download_too_large()` 雙檢（Content-Length 預檢＋串流累積，300MB）。F5：Zip 下載前短路 Err、match 臂 unreachable。
- TDD red 4（F1 assertion pyright≠pylsp ×2＋binary_temp/download_too_large unimplemented）→green 33（29→33）。
- controller 親驗：cargo **165 passed／1 ignored**（161→165 只增）＋fmt 0（IDE E0061 確為中間態殘影）；vitest **532/532 無 Errors**（530→532）；tsc 0；build ✓ 1.01s；NUL（含 src-tauri）乾淨；spot-read 六修全部吻合（:125/:140/:230/:475/:498-500/:569-575/:606/:690/:728/:733）。
→ R1 全部 8 修（Rust 6＋前端 1＋checklist 2 併計）落地，進 Round 2（全新 reviewer，dry-pass 候選輪）。

**Round 2（1 全新 opus reviewer w6r2-dry，14:3x 派出）**：驗證 R1 修復——F1 端到端契約（invoke camelCase↔snake_case、canonical 測試品質、resolve 鏈）、F2（spawn_blocking 正確性、guard Send/跨 await、panic 收斂）、F3（chmod/xattr 於 temp 後 rename 的 inode 語意）、F4（雙檢邏輯邊界）、F5（測試隨短路更新）、F6（raw workspace 與 LspBridge 比對真的 match）＋新測 mutation＋回歸快掃。零新 finding 即 dry。

## P4 對抗 review

（待填）

**w6r2-dry 回報（14:3x）：零新 finding，dry pass**。六修全判落地正確——F1 端到端契約匹配＋canonical 同 helper 逐字相同＋**同源確認（install 的 resolve 鏈與 lsp_service 啟動 resolve 鏈完全相同，「裝的==啟動會用的」）**；F2 唯一 await、guard 不跨、panic 經 JoinError 收斂且 RAII 不洩漏；F3 同目錄 rename＋inode 附著語意＋Windows MOVEFILE 語意；F4 雙檢邊界（==max 放行）＋cap Err 走 finalize 終態；F5 下載前短路＋unreachable 真不可達；F6 raw 對 raw 同源匹配。4 新測 mutation 皆有鑑別力（macOS symlink tmp 實測）；反證 hold ×5。
**P4 收斂：R1（Rust 6＋前端 1＋checklist 2）→R2 dry。Gate G4 ✅**

## P5 驗收

**最終五件套（controller 親跑，13:36，dry pass 後零程式變更）**：
```
vitest：59 files / 532 passed（無 Errors 行）    ← baseline 530 → 532，只增
tsc --noEmit：EXIT=0
build：✓ built in 1.02s
cargo test：165 passed / 1 ignored               ← baseline 132 → 165，只增（T14 +29、R1 修 +4）
cargo fmt --check：EXIT=0
NUL 掃描（src＋src-tauri/src）：乾淨
```
**tauri:dev smoke（plan T15 Step 4，controller 親跑 13:4x）**：cargo dev build Finished 14.71s（含新依賴 ureq/sha2/flate2 全編譯）→ `Running target/debug/yuzora`、VITE ready 163ms、Local :1420、進程存活（pid 28079）、無 panic/error → **PASS**，隨後 TaskStop 收掉。
**驗收證據對照（P2 固定清單）**：E1 ✅（lsp_download 33 測：分流/URL/SHA/解壓/quarantine/npm/venv/無 npm/python/終態契約/in-flight/override resolve/canonical/temp/cap）；E2 ✅（fixture 冪等四語言＋checklist 12 主項＋歷史 A-H 章，controller 親跑親讀）；E3 ✅（上表）；E4 ✅（P4 兩輪每 finding 有裁決）。
**Gate G5 ✅**

## P6 收尾

- **本 wave 落地**：T14 七 curated server 一鍵安裝（binary/npm/pip 三路徑、A2 async 背景、終態事件契約、in-flight guard、workspace override resolve、temp+rename、timeout/cap）＋T15 fixture 產生器與 m3-manual-checklist（含歷史累積實機項 A-H）。P4 兩輪 9 修 dry pass（R1：W6A-F1 breaks-requirement override 死路／W6A-F2 A2 main-thread 違反／F3 SIGBUS／F4 無界下載／F5/F6 nit＋W6B-F1 checklist 假指令／F2 H4 漏收；R2 dry）。
- **殘留**：真實下載/npm/pip/xattr/rename 實機行為＋Windows rename 語意 → T15 實機清單（checklist 第 9 項）；Windows RA zip 解壓支援（延後）；markdown-oxide asset 命名精確性（T15 實機首驗）；wave 5 殘留之 lifecycle（stop 不 emit/initialized 無 per-language reset/profile 切換不重掛）**部分紓解**（install 成功 emit server-status 填 raw workspace）但 stop 側仍在 backlog。
- **LEDGER**：wave6 → DONE。**LEARNINGS**：+2（Tauri sync command main-thread 陷阱／雙端功能同源 resolve 原則）。
- **M3 自動側全數完結**（wave 0-6，15 tasks）。後續：①**最終 review 派 fable subagent（使用者指令）**；②M3 結案報告 html（opus subagent 撰寫）；③m3-manual-checklist 實機驗收需使用者配合 gui-acceptance（debug bundle→/Applications）。

- 狀態：**DONE**（2026-07-05 13:5x）
