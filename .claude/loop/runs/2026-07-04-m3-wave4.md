# LOOP RUN — m3-wave4

- 日期：2026-07-04
- 狀態：RUNNING
- 任務原文：
  > （沿使用者指示「繼續進行下一個 wave，直到 m3 完結」；三約束：opus subagent／git 唯讀／無 worktree）
- **跨 run 指令備忘**：M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示）。
- **使用者裁決（跨 run 適用）**：P4 對抗 review 用 ultracode Workflow（fresh opus reviewer fan-out＋雙反駁＋loop-until-dry）；修復回派原 implementer；M3 完結後最終 review 派 fable agents。

## P1 提問

**需求（可驗證陳述）**
- R1: T10 EditorPane LSP 整合——full-feature（result.kind==="full" 且 !hasVeryLongLine）檔 lazy 掛 LSP（`lspExtensionsForFile(path, grade)` 已備：grade≠full／非四語言／無 workspace／server missing→null 不掛不 didOpen）；view 先建無 LSP、extensions resolve 後 `reconfigure` 併入（不破 M1/M2 既有時序：save 攔截、conflictMarkers、pendingReveal、viewRegistry）。plan :585-620。
- R2: wire T7/T8/T9 進 `lspExtensions.ts::assembleLspExtensions`（codeActions／semanticTokens／diagnosticsPull 三 extension 依 client 能力降級）。plan :593、:440-447。
- R3: 跳轉聚焦——go-to-definition/references 導航後 `view.focus()`（jsdom try/catch）；未開啟檔沿 A1（YuzoraWorkspace 背景解析）＋displayFile/openTab/requestReveal。plan :602。
- R4: format-on-save gating＋save flush——save 閉包於 maybeInterceptSave 後、saveFile 前：(1) `flushPendingChanges`（client.sync()）flush pending didChange；(2) `shouldFormatOnSave(capabilities, enabled)` 真→送 textDocument/formatting 再存。plan :603。
- R5: document/workspace symbols（本 task owning）——`symbols.ts`：requestDocumentSymbols（DocumentSymbol[]／SymbolInformation[] 兩形狀→扁平 {name,kind,range,detail}）、requestWorkspaceSymbols（workspace/symbol query→{name,kind,uri,range}）；無 provider→空＋入口 disabled。plan :604-606。
- R6: SymbolPicker（cmdk 雙模式）——document 大綱（選取→revealLine＋view.focus()）／workspace 查詢（debounce→選取→openTab＋requestReveal）；CommandPalette 加「Go to symbol」「Workspace symbols」兩入口。plan :606。
- R7: 五件套不退化：vitest ≥447、tsc 0、build ✓、cargo 132+1、fmt clean；M1/M2 既有 EditorPane 測試不退化。

**已查資料**
- plan T10（:585-620）全讀；lspExtensions.ts wire 點（:19-27 三 placeholder）；EditorPane.tsx 現況（98→現 102 行，useEffect 內 getDocument().then 建 view、save 閉包、revealLine、m4 unregisterView(path,view) 帶 view 比對、pendingReveal 兩處）；lspManager.ts 匯出 `ensureClient`／`lspExtensionsForFile(path,grade)`／`shouldFormatOnSave(cap,enabled)`／`flushPendingChanges(managed)=client.sync()`／`assembleLspExtensions`／`sanitizeHtml`；CommandPalette.tsx（cmdk，props: open/onOpenChange/onSelectMode/onOpenSettings，⌘K，「Go to」group）。
- LEARNINGS 全讀（含本 session 追加 3 條：workflow 內層 parallel thunk、dry-pass 完成度核查、派修授權停下回報）。
- wave 3 DONE（T7/T8/T9 三 extension 就緒，各 export：codeActions 的 `lspLintSource`／T8 `semanticTokensExtension(client,uri)`／T9 `diagnosticsPullExtension(client,uri)`）。

**假設（非 BLOCKING，已選定預設值）**
- A1: 分工＝**2 並行 implementer**，零檔案寫交集：
  - **Impl A（EditorPane 整合）**：src/editor/EditorPane.tsx、src/lsp/lspExtensions.ts、src/editor/editorPane.lsp.test.tsx。掛 LSP（reconfigure）、wire T7/T8/T9、focus、format-save flush、大檔/單行守衛。
  - **Impl B（symbols 功能）**：src/lsp/symbols.ts＋.test、src/workbench/SymbolPicker.tsx＋.test、src/app/workbench/CommandPalette.tsx。symbols 純邏輯＋cmdk UI＋入口。
  - 依據：EditorPane 與 CommandPalette 雖同屬共用檔清單但為不同檔、不撞；symbols 鏈（symbols→SymbolPicker→CommandPalette）由單 agent 內部序列；SymbolPicker 讀 viewRegistry/workspaceStore 自足、不 import EditorPane。（沿 wave 3 慣例＋LEARNINGS opus 穩定性）
- A2: SymbolPicker 開關由 **CommandPalette 內部 state 管理**（選「Go to symbol」→關 palette、開 SymbolPicker），**不動 AppShell.tsx**（符 plan「獨立快捷鍵才動 AppShell」為可選；與 wave 5 T12 動 AppShell 跨 wave 序列）。
- A3: symbols 型別（DocumentSymbol／SymbolInformation／SymbolKind）於 symbols.ts 自模組定義（沿 wave 3 慣例，避免動 types.ts）。
- A4: LSP extension 掛載採「先建無-LSP view、`lspExtensionsForFile` resolve 後 `view.dispatch({effects: StateEffect.appendConfig / reconfigure})`」（plan :601 二擇一取最小侵入；appendConfig 較 reconfigure 不破既有 compartment）——實作者以最小侵入為準、差異記 report。

**已知整合風險（brief 須明列、P4 對抗 review 重點驗）**
- H1（rename 跨檔）：WorkspaceEdit 觸及多檔時 YuzoraWorkspace 須先載入所有 target URI 才套用（A1，T6 workspace.ts 職責）——T10 驗 rename 流程不破。
- H2（split-pane mount 政策）：同檔於兩 split pane＝兩 EditorPane／兩 view；ensureClient 快取單 client，但 assembleLspExtensions 每 view 建 extension、兩 LSPPlugin 對同 uri——查 @codemirror/lsp-client 是否 per-view 安全（雙 didOpen／sync 衝突）。
- H3（tooltip anchor 點擊）：hover/completion tooltip 的 sanitized HTML 內 `<a href>` 仍可能導航 webview；sanitizer 層擋不掉，須於 editor mount 層攔截點擊。
- H4（background-file 升格）：YuzoraWorkspace 背景載入的檔後被開成真 view 時，doc/version 須調和避免 LSP 收到 stale version。

**BLOCKING 待問**：無（worktree 沿三約束不用；EditorPane 為高風險檔但單 implementer 獨佔、有 M1/M2 既有測試守門，風險可控）。

**Gate G1**：✅

## P2 規劃

**Baseline battery**（wave 3 P5 終驗，本 session 親跑 11:00）＝本 run baseline：
```
vitest：52 passed / 447 passed
tsc --noEmit：EXIT=0
build：✓ built in 903ms
cargo test：132 passed / 1 ignored
cargo fmt --check：EXIT=0
```
`git status --short`：wave 3 變更（i18n.ts／lsp/*.ts／editor.css 等）untracked/modified，git 唯讀不動。

**步驟**
1. 並行派 2 opus implementer（A=EditorPane 整合／B=symbols 功能，TDD red→green）→ verify：各回報後 controller 親跑對應測試。
2. controller 五件套整合 → 全綠不退化。
3. P4 ultracode workflow（fresh opus reviewer fan-out＋雙反駁，重點驗 H1-H4，loop-until-dry）。
4. P5 終驗＋P6 收尾（LEDGER，下接 wave 5）。

**預計變更檔案**
- Impl A：src/editor/EditorPane.tsx、src/lsp/lspExtensions.ts、src/editor/editorPane.lsp.test.tsx
- Impl B：src/lsp/symbols.ts＋.test、src/workbench/SymbolPicker.tsx＋.test、src/app/workbench/CommandPalette.tsx

**不碰範圍（non-goals）**
- 不動 AppShell.tsx／StatusBar.tsx／SettingsDialog.tsx（wave 5）、不動 types.ts／ipc.ts、不動 src-tauri/**、不動 wave 3 已定稿的 T7/T8/T9 三 extension 檔（只在 lspExtensions.ts import wire）、git 唯讀。

**風險**
- EditorPane reconfigure 破壞 M1/M2 既有時序（save 攔截／conflictMarkers／pendingReveal／viewRegistry）／守門：既有 EditorPane.test.tsx 不退化＋新 editorPane.lsp.test.tsx 斷言掛載不影響既有行為。
- H2 雙 view 同 uri／守門：P4 review＋T15 實機。
- symbols 兩形狀映射 off／守門：symbols.test.ts 對 DocumentSymbol[]／SymbolInformation[] 兩形狀斷言。

**驗收證據清單（實作前固定，只可加嚴）**
- E1: Impl A 測試綠（full 檔掛 LSP／limited/單行不掛／focus spy／save flush＋format gating／wire 後 build）。
- E2: Impl B 測試綠（symbols 兩形狀映射＋降級／SymbolPicker document+workspace 雙模式選取導航＋debounce）。
- E3: 五件套終跑（vitest ≥447／0／✓／132+1／clean），M1/M2 EditorPane 既有測試不退化。
- E4: P4 dry pass（含 H1-H4 明確裁決）。

**Gate G2**：✅

## P3 執行

**派工（2 並行 opus implementer，10:xx）**
- **Impl A（ac34afd2d90bde9c4）**：EditorPane.tsx＋lspExtensions.ts＋editorPane.lsp.test.tsx。掛 LSP（appendConfig、guard disposed/view 替換）、managedRef 供 save 用、wire T7/T8/T9（含 T7 linter(lspLintSource) vs T9 diagnosticsPullExtension 雙診斷渲染的去重決策）、revealLine 末加 view.focus()、save flush（flushPendingChanges）＋format gating（localStorage key 預設 off）。H2/H3/H4 標注處置。
- **Impl B（a6a860ce475a74d00）**：symbols.ts（兩形狀 DocumentSymbol[]/SymbolInformation[] 攤平映射）＋SymbolPicker.tsx（cmdk 雙模式、經 requestReveal/openTab 導航）＋CommandPalette.tsx（兩入口、內部管 SymbolPicker 開關，不動 AppShell）。
- 兩者白名單零檔案寫交集（EditorPane/lspExtensions vs symbols/SymbolPicker/CommandPalette）。均授權「指示若不安全須停下回報」。
- 等回報 → controller 親驗各測試 → 五件套整合 → P4 ultracode（重點驗 H1-H4）。

**修復回報**
- **Impl B ✅（symbols）**：symbols.ts（requestDocumentSymbols 攤平 DocumentSymbol[]/SymbolInformation[] 兩形狀，DocumentSymbol range 取 `selectionRange ?? range` 讓跳轉落在名稱；requestWorkspaceSymbols）＋SymbolPicker.tsx（cmdk 雙模式，resolveActive 讀 workspacePath＋groups[activeGroupIndex].activePath→lspLanguageOf→ensureClient；capabilities cast 本地 SymbolCapabilities 讀 provider；document→requestReveal(activePath,line+1)，workspace→openTab＋requestReveal；無 provider→CommandEmpty「無符號」）＋CommandPalette.tsx（兩入口＋內部 useState 管 picker，fragment 併 render，AppShell 未動）。TDD red→green。controller 親跑 `bunx vitest run src/lsp/symbols.test.ts src/workbench/symbolPicker.test.tsx` → **7 passed**（11:17）。自身檔 tsc-clean、vite build exit 0。
  - 假設待裁：①DocumentSymbol range 取 selectionRange（落名稱）→ **接受**（跳轉語意更佳）；②「Go to symbol」無 activePath 時 picker 顯示空而非 disabled → **接受**（與 capability-empty 路徑一致）；③「無符號」字串 inline 未進 i18n（i18n.ts 不在白名單）→ **接受**（留待後續 i18n consolidation）。親驗了 workspaceStore requestReveal/openTab/activePath 真實簽名、無偏差。
- **Impl A ✅（EditorPane 整合）**：EditorPane.tsx（async LSP mount：no-LSP view 先 live、registerView/pendingReveal 同步完成後 IIFE guard disposed/view-swap→`appendConfig`；managedRef 供 save；save 閉包 `flushPendingChanges`→`applyFormatOnSave`(gated,await)→mark→saveFile；revealLine 末 `view.focus()`；`FORMAT_ON_SAVE_STORAGE_KEY` export 供 T12）＋lspExtensions.ts（wire `languageServerSupport＋semanticTokensExtension＋linter(lspLintSource)`，不 wire T9）＋editorPane.lsp.test.tsx（7 測）。TDD red→green。
  - **T7/T9 wiring 決策（controller 覆核接受）**：lspLintSource（經 linter）是 diagnosticsPullExtension 的超集（同 pull＋quick-fix），只 wire T7 避雙渲染；R2 要 code-action 只 T7 有；與 baseline serverDiagnostics（push）互補。diagnosticsPullExtension 未成孤兒（其 test 仍引用）。
  - **假設待裁（controller 接受）**：①用 assembleLspExtensions 直呼＋ensureClient 取 managed（非 lspExtensionsForFile，因 save 需 client；後者未成孤兒 lspManager.test 覆蓋）；②fileGradeOf(r,content)!=="full" 等價 R1 掛載判準；③format key `yuzora.lsp.formatOnSave.v1` 預設 false export 供 T12；④formatting tabSize:4。
  - **誠實標注的殘留整合風險（controller 記，排 T15）**：baseline serverDiagnostics（命令式 setDiagnostics）與 linter()（宣告式）在**雙模式 server** 可能互覆蓋/閃爍——解需動 baseline（移除 serverDiagnostics）超白名單。H2 split-pane（非 registry 記錄的 pane 編輯不 sync／unmount didClose，屬既有 m4 單-view 限制）、H3 tooltip anchor（tooltip DOM host 不確定、低成本攔截不成立）、H4 background-file 升格 stale 窗口（屬既有 workspace early-return 語意）——皆既有/out-of-scope，標 T15。
  - **計畫外發現（未改，建議 controller 開後續 task）**：測試 localStorage mock 的正確歸宿是 src/test/setup.ts（超白名單），gitStore.test.ts 亦各自 workaround——建議收斂。

### 整合 battery（controller 親跑，11:1x，兩 agent 完成）
- vitest：**55 passed / 461 passed**（baseline 447 → +14：B 7＋A 7 只增）
- tsc --noEmit：**TSC_EXIT=0**／build：**✓ 964ms**／cargo：**132+1**／fmt：**EXIT=0**
→ 五件套零退化。M1/M2 既有 EditorPane 測試不退化（含於 461）。P3 完成，進 P4。

## P4 對抗 review

**指令變更（使用者 2026-07-04）**：P4 起**不使用 ultracode Workflow**，改**純 opus subagent（Agent 工具）**派工。fresh reviewer／裁決／回派修復／loop-until-dry 的閉環結構不變，僅執行載體改變。（前一 session 於此處中斷：wave3 R2 workflow 完成紀錄遺失但 run file 已記帳；本 session 已親驗五件套 461/461、tsc 0、build ✓ 952ms、cargo 132+1、fmt 0——P3 狀態為真。）

**Round 1（3 fresh opus reviewer 並行，lens 分工，23:2x 派出）**
- Lens A（EditorPane 生命週期）：async mount／save 閉包順序／format gating／M1/M2 時序回歸。
- Lens B（symbols 鏈）：兩形狀攤平（含 DocumentSymbol 巢狀 children）／導航 off-by-one／cmdk 雙模式／i18n 雙主題。
- Lens C（整合＋scope＋測試 mutation）：T7-only wiring 決策核驗／雙診斷疊渲染／H1-H4 處置正當性／孤兒／假綠。
- 等回報 → controller 逐條裁決（讀碼親驗；無法裁者派 fresh opus refuter）→ 真 finding 回派原 implementer → 全新 reviewer 重跑至 dry pass。

**Lens B 回報（symbols，7 findings＋9 反證）— controller 已逐條親驗裁決**：
- **B1〔major/latent〕capabilities 未初始化誤顯「無符號」— 確認修**。親驗 lspManager.ts：`managed={…,capabilities:null}`＋fire-and-forget `void client.initializing.then(()=>managed.capabilities=…)`，ensureClient 於 transport 連上即 resolve；SymbolPicker 兩 effect `!caps?.documentSymbolProvider` 把「未 ready」當「無 provider」回空，deps [open,mode] 不重跑。修法：兩 effect 讀 caps 前 `await managed.client.initializing.catch(()=>{})`（同 promise 先註冊的 lspManager .then 先跑，await 返回時 capabilities 已填）＋cancelled 再查。
- **B2〔minor〕picker 開著按 ⌘K 疊開 palette — 確認修**。親驗 CommandPalette keydown 只看 `open`。修法：handler 先查 `symbolPicker !== null` → 關 picker 並 return（該按鍵不開 palette）。
- **B3〔minor/cleanup〕selectWorkspace 重複 openTab — 確認修**。親驗 requestReveal 內部 `get().openTab(path)`。修法：刪顯式 openTab 行。
- **B4〔minor〕畸形 truthy range `{}` 流過 `?? ZERO_RANGE` → onClick TypeError — 確認修**（防禦姿態與 codebase total-function 慣例一致化）。修法：symbols.ts 攤平/映射處驗 `typeof range?.start?.line === "number"` 否則 fallback ZERO_RANGE。
- **B5〔minor/test-gap〕capability guard 與無 range WorkspaceSymbol 零覆蓋（revert guard 7 測仍綠）— 確認修**。補測：capabilities 缺席→空＋不發 request；location 無 range→ZERO_RANGE。
- **B6〔minor/i18n〕SymbolPicker 內中英混用 — 部分修**。裁決：SymbolPicker 自身字串統一走 i18n.ts（strings.lsp.*，中文：placeholder／sr-only 標題／「無符號」併入）；CommandPalette 既有全英文 labels（含本 wave 兩個新入口）**不動**——palette 內部一致性優先，palette 全面 i18n 留待後續 consolidation（記殘留）。
- **B7〔minor/cleanup〕條件渲染使 reset effect 不可達 — 確認修**。修法：CommandPalette 改常駐渲染（pickerOpen＋pickerMode 分離 state），使 reset-on-close 可達、免 remount；或等效最小改法由 implementer 擇一回報。
- **jsdom 不可驗（記 T15 gui-acceptance）**：Radix 雙 dialog 交接（palette 關→picker 開的 pointer-events/focus-scope 陷阱）；view.focus() 實效。
- 派修：**等 Lens A/C 回報到齊後一批回派 Impl B**（白名單擴：+src/lib/i18n.ts）。

**Lens C 回報（整合/wiring，5 findings＋H 核驗＋8 反證 hold）— controller 已親驗裁決**：
- **F1〔major〕unchanged re-emit 用 stale offset — 確認修（Impl A）**。親驗 @codemirror/lint forEachDiagnostic(:933-943)：callback `f(d, mappedStart, mappedEnd)` 以參數傳 mapped 座標（d.from/d.to 為建立時原始值）；codeActions.ts:104-108 `kept.push(d)` 丟參數→unchanged 時 decoration 跳回舊座標（rust-analyzer 確定性 drift）。修：`kept.push({...d, from, to})`＋補「插行後 unchanged」測試（現 codeActions.test.ts:151 假綠：seed 與 pull 間無 doc change）。
- **F2〔major〕wiring 診斷通道殘缺 — 確認修（Impl A），且比 Lens C 框架更嚴重**。controller 親驗 lsp-client:1854-1862：`languageServerSupport` **不含 serverDiagnostics**——Impl A 自報「與 baseline serverDiagnostics 互補」不成立，push-only server（ts-ls 等）現況**零診斷渲染**＋un-gated pull 每 750ms 對無 provider server 發錯誤 request（lspLintSource :99 無 try/catch）。修：lspExtensions wiring 補 `serverDiagnostics()`（push 通道；有 unsyncedChanges 補償）；`linter(lspLintSource)` 僅於 `diagnosticProvider` 存在時掛（gate）；lspLintSource pull 包 try/catch（transient 錯誤回現況）；重寫 :18-24 錯誤註解（改為正確 rationale：push=serverDiagnostics／pull=gated linter＋quick-fix；both-capable server 雙寫風險記 known limitation → T15）。750ms/500ms 節奏差異接受（linter 設計值）。
- **F3〔minor/orphan〕diagnosticsPullExtension＋lspExtensionsForFile 皆無 production caller、EditorPane inline 重複 gating — 部分修（Impl A）**。裁決：`lspExtensionsForFile` 重構為回傳 `{managed, extensions}｜null` 的單一 gating 來源、EditorPane 改消費之（消孤兒＋消重複邏輯，lspManager.test 同步改）；`diagnosticsPullExtension` **保留**（tested 正確參考實作、plan T9 交付物；其檔頭補「未 wire 原因」正確版註解）——不刪測試（棘輪）。
- **F4〔major〕format-on-save await 後映射 current doc — 確認修（Impl A）**。親驗 EditorPane.tsx applyFormatOnSave：`const doc = view.state.doc` 在 await **之後**；無 docAtRequest 比對、無 view 存活檢查。修：request 前捕 `docAtRequest`（flush 後＝server 所見 doc）；await 後 `if (!isLive() || view.state.doc !== docAtRequest) return`（drift→跳過 format 照存未格式化內容）；isLive 由 save 閉包傳 `() => viewRef.current === view`。補「await 期間 doc 變更→不套用」測試。
- **F5＝B1（capabilities 初始化競態）合併**；另 controller 親驗**新增 A0〔major〕mount capability race（Impl A）**：mount IIFE `await ensureClient` 後直接 `assembleLspExtensions`（EditorPane.tsx:166-168），冷啟 server 時 serverCapabilities=null→semanticTokens 等 capability gate 對該 view 永久降級空。修：assemble 前 `await managed.client.initializing.catch(()=>{})`，之後**重查** `disposed || viewRef.current !== view` 再 dispatch。
- **H 處置核驗**：H1/H2/H4 維持（可接受）；**H3 翻案 — 確認修（Impl A）**：hoverTooltips 在 wiring 內（:1858）、sanitizer 保留 `<a href>`（移 target 反迫使 in-place 導航）——document 層 capture click listener 查 `closest('.cm-tooltip a[href]')` preventDefault 低成本可攔。原「不成立」理由改寫；Tauri navigation handler 實效仍記 T15。
- 其餘（i18n inline strings）併 B6 已裁。Lens C 之 failing-test 草案提議**婉拒**（角色分離：reviewer 不寫碼；由 implementer TDD）。

**派修執行**
- 原 wave-4 Impl B（a6a860ce475a74d00）resume 失敗：「No transcript found」——前一 process 結束時 in-flight/completed subagent transcript 隨之遺失（LEARNINGS /clear 條的同族現象）。**Fallback：派全新 opus implementer（w4fix-symbols）帶自足 brief 修 B1-B7**（brief 含完整根因＋修法，不依賴原作者 context）；白名單同 Impl B＋i18n.ts＋commandPalette.test.tsx（新測試檔）。派出 23:5x。
- Impl A 修復批次（F1／F2／F3 重構／F4／A0 mount race／H3 攔截）**等 Lens A 回報合併後**派（原 Impl A ac34afd2d90bde9c4 同樣不可 resume，屆時亦派 fresh implementer）。

**Lens A 回報（EditorPane 生命週期，5 findings＋8 反證 hold）— controller 裁決**：
- **A1〔major〕＝F4 合併**（同一 format 漂移；A1 補完整損毀鏈：ChangeSet.of 亂序 flush+compose 不 throw＋offsetOf clamp→靜默污染→markDirty(false) 寫盤；format-on-save 預設 off 為緩解）。
- **A2〔minor/test-gap〕flush→format→save 順序零覆蓋（reorder 仍綠）— 確認修**：補 call-order 斷言（sync→formatting request→saveFile）。
- **A3〔minor/test-gap〕lspExtensions wiring 零測試（使用處被 mock）— 確認修**：新增 src/lsp/lspExtensions.test.ts（與 F2 gate 測試合流：composition＝languageServerSupport＋semanticTokens＋serverDiagnostics＋gated linter）。
- **A4〔minor/scope-regression〕revealLine 無條件 focus 波及 M2 搜尋點擊 — 確認修**：pendingReveal 加 `focus?: boolean` **預設 true**（requestReveal 第三參數；SymbolPicker/displayFile/LSP 跳轉不需改檔），SearchResults.tsx 顯式傳 false 保留 M2「reveal 不搶焦點」；EditorPane 消費 `reveal.focus ?? true`；補兩測（search 不 focus／預設 focus）。修法選擇考量：避免動 SymbolPicker.tsx（修復批次 B 正在寫）。
- **A5〔minor/cleanup〕save 鏈與 mount IIFE 無 .catch — 確認修**：save 鏈補 `.catch`（保 dirty、console.error，完整錯誤 UX 記殘留）；mount IIFE 補防禦性 catch。
- 反證 hold ×8（destroyed-view dispatch 安全／appendConfig 不重掛／grade gating 完備／localStorage 每次重讀／同 view 單次 didOpen 等）；split-view、Radix focus 爭用、真 server round-trip → T15。
- **修復批次 A（fresh implementer w4fix-editor）＝F1＋F2(含A3)＋F3 重構＋F4(=A1,含A2)＋A0＋H3＋A4＋A5**。白名單與批次 B 零交集（確認：EditorPane/lspExtensions/lspManager/codeActions/diagnosticsPull 註解/workspaceStore/SearchResults vs symbols/SymbolPicker/CommandPalette/i18n）。

**修復批次 B 回報＋controller 親驗 ✅（2026-07-05 00:04）**
- B1-B7 全落地：symbols.ts isValidRange 守衛 ×3 點／SymbolPicker 兩 effect await initializing＋刪冗餘 openTab＋strings.lsp.*／CommandPalette pickerOpen+pickerMode 常駐渲染（B7 選 a）＋⌘K 關 picker 不疊窗／i18n.ts +4 鍵／commandPalette.test.tsx 新檔。TDD red 證據齊（B4 ×2 紅、B1 waitFor 逾時、B2 dialog 殘留）。
- controller 親跑：`bunx vitest run symbols+symbolPicker+commandPalette` → **3 files / 15 passed**；`bunx tsc --noEmit` → **EXIT=0**。
- **計畫外改動裁決（接受）**：B4 第三個 range 點（SymbolInformation 分支 `if (info.location.range)` truthy 即 push）同 bug class、白名單內、降級哲學一致、既有測試不回歸——非 drive-by。
- 其他自證：B1 排序保證非假設（lspManager .then 先註冊、Promise FIFO，冷啟測試重現）；B6 DialogTitle 收斂單一鍵；mock 補預設 resolved 值屬測試 infra。
- 等批次 A 回報 → 親驗 → 五件套整合 → Round 2 全新 reviewer。

**修復批次 A 回報＋controller 親驗 ✅（2026-07-05 00:18）**
- 8 項全落地（12 檔）：F1 mapped 座標（codeActions.ts:106 `(d,from,to)=>{...d,from,to}`＋插行 drift 測試取代假綠）；F2 gated linter＋lspLintSource try/catch＋註解重寫＋新檔 lspExtensions.test.ts（A3 銷）；F3 lspExtensionsForFile→`{managed,extensions}|null` 單一 gating 來源、EditorPane 消費；F4 docAtRequest＋isLive guard（3 測：真套用/drift 跳過/A2 順序）；A0 await initializing 收進 lspExtensionsForFile（:225，冷啟註解完整）；H3 per-pane document capture listener（:134-140）；A4 focus?: boolean（SearchResults false、其餘預設 true）；A5 三鏈補 .catch。TDD red 證據 ×7。
- **計畫外偏離 ×2（controller 裁決：均接受）**：①serverDiagnostics() 實為 LSPClientExtension（d.ts 證據）非 CodeMirror Extension——brief 的掛載點寫錯，implementer 正確改掛 `new LSPClient({extensions:[serverDiagnostics()]})`（lspManager.ts:142），以 initialize 請求含 publishDiagnostics.versionSupport 驗證；②searchResults.test.tsx（白名單外）機械性連動補 `focus:false` 斷言（A4 必然後果，帶說明註解）。A4 用 `focus?:` 而非參數預設值（等價、避免撞 workspaceStore.test 斷言）——接受；A0 不另立 EditorPane 端測試（理由合理）——接受。
- controller 親跑五件套（00:18）：vitest **479/479**（461→+18 只增）、tsc **0**、build **✓ 1.37s**、cargo **132+1**、fmt **0**。
- → **Round 2 全新 reviewer**（2 fresh opus lens，00:2x 派出：①修復正確性＋mutation＋兩批次交互（w4r2-fixes）②fresh 全審（w4r2-fresh）——交互 rubric 併入 ①，較原記 3 lens 收斂）。

**Round 2 回報（兩 lens 到齊）— 合併去重 8 條，controller 親驗裁決（2026-07-05 00:5x）**：
- **R2-1〔major/hygiene〕SymbolPicker.tsx :189/:205 JSX key 內嵌 NUL byte（0x00）×2 — 確認修（批次 C）**。controller 親驗：perl 計數 2、`file` 判 data、grep/rg 對整檔失明（tsc/vitest 照綠）；全 src 掃描僅此檔。極可能為批次 B subagent Write 夾帶（B6/B7 重寫區塊）。修：兩處換一般分隔字元。→ P6 記 LEARNINGS [trap]＋修復後 NUL 掃描慣例。
- **R2-2〔major〕format dispatch 在 try/catch 外，throw 吞掉整個存檔（兩 reviewer 交叉收斂：fresh-F1＝fixes#2）— 確認修（批次 D）**。controller 親讀 :150-178 確認：applyFormatOnSave reject → `formatted.then(...)` 跳過 → `.catch(()=>{})` 吞 → saveFile 不執行＝靜默存檔遺失，違自述「save the un-formatted text」意圖。inverted range（from>to）確定 throw（fresh 以真 @codemirror/state 驗）；overlap 是否 throw 兩 reviewer 說法分歧——修法不受影響：dispatch 包 try/catch（catch→console.warn＋return 照存未格式化）＋pre-filter from<=to。
- **R2-3〔minor〕applyWorkspaceEdit 同款 unguarded dispatch（caller 有 catch→良性 no-op）— 確認修（批次 D）**：offsetOf 後驗 from<=to，任一無效→整組不套用（atomic）＋測試。
- **R2-4〔minor〕SymbolPicker resolveActive 繞過 file-grade gate — 確認修（批次 C）**：non-full 檔開 picker 仍 ensureClient（可能啟動被 grade 拒絕的 server）＋查未 didOpen 的 URI。修：resolveActive 比照 EditorPane 用 getDocument＋fileGradeOf gate，non-full→空狀態。
- **R2-5〔info〕symbol requests 前無 client.sync()（built-ins 皆先 sync；autoSync 500ms 緩解）— 修（批次 C）**：requestDocumentSymbols/requestWorkspaceSymbols 開頭 client.sync()。
- **R2-6〔info〕workspace symbol 未驗 rec.name → 空白列 — 修（批次 C）**：非 string name 跳過該筆。
- **R2-7〔test-gap〕symbols.ts:65 SymbolInformation 分支 isValidRange 守衛無測試鎖（revert 仍綠）— 補測（批次 C）**。
- **R2-8〔test-gap〕workspace 模式 await initializing 無 cold-start 測試鎖（revert :113 仍綠）— 補測（批次 C）**。
- 反證 hold：fresh ×9＋fixes ×9（sync→format→save 順序、isLive 閉包、A0 no-LSP 窗口安全、B2 stale closure、A4 focus 全鏈保真、H3 text-node 安全、孤兒 import 清空等）。
- 派修：批次 C（symbols 側 6 項）＋批次 D（editor 側 2 項）fresh implementer 並行，白名單零交集。

**修復批次 D 回報＋controller 親驗 ✅（00:45）**：R2-2 pre-filter（任一 from>to 整組放棄）＋dispatch try/catch（warn＋照存未格式化）；R2-3 applyWorkspaceEdit atomic 驗證（任一無效整組不套）。TDD red 證據直證原 bug（saveFile 從未呼叫／RangeError 24 to 0）。controller 親跑兩檔 **22/22**（20→22）＋tsc **0**。計畫外改動：無。

**修復批次 C 回報＋controller 親驗（00:54）——6/6 落地但整合驗出 1 個 unhandled error → follow-up**
- 親驗通過項：NUL 歸零（perl 0、file 回 UTF-8 text、grep 恢復可搜、全 src 複掃乾淨）；兩檔 20/20；tsc 0；build ✓；cargo 132+1；fmt 0。R2-4 red→green、R2-7/R2-8 revert-demo 證據＋還原完整性 grep 齊。brief 筆誤（EditorPane 目錄）由 implementer 自行對照 sibling 解決、未越界。
- **controller 攔截（全量 vitest 才可見）**：`Errors 1`——commandPalette.test.tsx（B7 常駐渲染 SymbolPicker）未 mock documentRegistry，R2-4 新增的 getDocument 打到真 ipc → jsdom 無 Tauri bridge → unhandled rejection（487/487 照過，非乾淨綠）。同時暴露 production 缺口：resolveActive 的 getDocument reject（picker 開啟瞬間檔案被刪）→ effect async IIFE 無 catch。
- **Follow-up 派回 w4fix2-symbols（白名單＋commandPalette.test.tsx）**：①resolveActive getDocument 包 try/catch→null managed（空狀態）；②commandPalette.test.tsx 補 documentRegistry mock。驗收重點：全量 vitest 無 Errors 行。

**Follow-up 回報＋controller 親驗 ✅（01:0x）**：`.catch(() => null)`（沿檔內慣例）＋`!entry` 併早退守衛；commandPalette.test 補 documentRegistry mock（@/ alias）；symbolPicker.test 加 reject→空狀態鎖測（red-demo 證明有效）。controller 親跑：**vitest 488/488 無 Errors 行**、tsc 0、build ✓ 1.07s、cargo 132+1、fmt 0。Round 2 全部修復收斂（479→488 只增）。→ **Round 3 全新 reviewer（終局檢）**。

**Round 3 回報（w4r3-final：1 major＋2 minor＋2 nitpick＋13 反證 hold）— controller 裁決（01:2x）**
- **R3-1〔major〕確認修（symbols 側）**：SymbolPicker 兩 effect IIFE 無 .catch，requestDocumentSymbols/requestWorkspaceSymbols 的 reject（server error 如 rust-analyzer 索引期 ContentModified -32801／3s timeout／not-connected——compiled client :662/:707/:798）逸出 unhandled；Round 2 只補了 getDocument 一個 reject 源。修：兩 await 包 try/catch→空清單（與 capability-missing 路徑一致）＋鎖測。
- **R3-2〔minor〕確認修（symbols 側）**：workspace/symbol 跨檔查詢被 active 檔 grade gate 誤傷（tooLarge active 檔→整個 workspace 搜尋停用）。修：grade gate 只套 document 模式；workspace 模式仍要求 language/workspace/ensureClient。＋測試（tooLarge active＋workspace 模式→request 照發）。
- **R3-3〔minor/pre-existing〕確認修（editor 側）**：codeActionsFor 請求前無 client.sync()（內建 feature 全都 sync；「合法但錯位」誤套的前置）。修：codeActionsFor 開頭 client.sync()＋順序測試。
- **R3-4〔nitpick〕defer**：format tabSize:4 寫死——碼內已有 T12 過渡註解，記殘留、wave 5 Settings 接。
- **R3-5〔nitpick〕駁回（既決）**：palette 英文入口＝Round 1 B6 裁決（palette 內部一致性優先，全面 i18n 留 consolidation）；fresh reviewer 不知前輪裁決，不重啟。
- 反證 hold ×13（pre-filter 空 range 放行、overlap try/catch、並發雙 Ctrl+S drift guard、cold-start await 順序、sync 必要性、A→B→A handle、NUL 複掃零命中、focus 語意雙向、⌘K stale closure、save 鏈雙 catch 等）。
- 派修：R3-1/R3-2 → w4fix2-symbols（在線）；R3-3 → w4fix2-editor（在線）。→ Round 4 終局複檢。

**R3 修復回報＋controller 親驗 ✅（01:2x-01:3x）**
- R3-3（editor 側）：codeActionsFor 開頭 client.sync()（d.ts:394 佐證、同 R2-5 前例）＋sync→request 順序測試＋3 個流經 mock 補 sync spy。親驗 23/23、tsc 0。
- R3-1/R3-2（symbols 側）：兩 request `.catch(() => [])`（檔內 idiom，reject→空清單降級、cancelled guard 保留）；resolveActive(gradeGate) 參數化——document 傳 true、workspace 傳 false（跨檔查詢不受 active 檔 grade 拖累），:44-45 註解改寫；3 鎖測（red 直證 2 unhandled＋gate 誤傷）。
- controller 親跑五件套：vitest **492/492 無 Errors 行**（488→492 只增）、tsc 0、build ✓ 1.03s、cargo 132+1、fmt 0。
- → **Round 4 終局複檢**（fresh reviewer；0 finding 即 dry pass）。

**Round 4 回報（w4r4-dry：1 minor/latent＋16 反證 hold；reviewer 自跑 tsc 0＋492 全綠）— dry pass 判定（01:4x）**
- **F1〔minor/latent〕裁決：defer（記殘留）**：EditorPane.tsx:152 `getDocument(path).then` 無 .catch 為**既存**（M1 mount path、M2 SearchResults 同可達，非 wave-4 變更行）；wave 4 經 selectWorkspace 首度把 server 控制的 URI 餵進同一 mount（擴大可達面）。後果：stale/惡意 URI→unhandled rejection＋空 pane＋孤兒 tab，無資料遺失。正解需「開檔失敗 error-pane UX」設計（超出 T10）；`.catch(()=>{})` 只滅音不解決空 pane——不做半吊子修。記 P6 殘留＋backlog。
- R3 三修復逐項驗證通過（catch 後 setState 在 cancelled guard 下／gradeGate=false 不呼叫 getDocument／sync 先於 request＋mock 補齊）；16 反證 hold。
- **Gate G4 ✅：dry pass**（R4 僅 1 條裁決 defer 的既存 latent；四輪 R1 15＋R2 8＋R3 3＝26 修；累計反證 hold：R1 期 17＋R2 期 18＋R3 13＋R4 16）。

## P5 驗收

- **E1（Impl A 測試綠）✅**：editorPane.lsp／lspExtensions／lspManager／codeActions 測試全數含於整合 492（含 F4 drift 三測、H3 攔截、A4 focus、R2-2/R2-3、R3-3 順序）。
- **E2（Impl B 測試綠）✅**：symbols／symbolPicker／commandPalette 25 測（兩形狀＋遞迴、cold-start ×2、capability guard ×2、畸形 range ×3、⌘K、reopen 重置、reject 降級 ×3、gradeGate）。
- **E3（五件套終跑）✅**（controller 親跑 01:3x，R3 修復後零程式變更，Round 4 唯讀）：
  - vitest：**57 files / 492 passed，無 Errors 行**（wave baseline 447→461→492 只增；M1/M2 既有測試含於內，不退化）
  - tsc --noEmit：**EXIT=0**／build：**✓ 1.03s**／cargo test：**132 passed / 1 ignored**／cargo fmt --check：**EXIT=0**
- **E4（P4 dry pass＋H1-H4 裁決）✅**：dry pass（上記）；H1 rename 跨檔=T6 職責（維持）、H2 split-pane=既有 m4 限制（維持、T15）、H3 tooltip anchor=**已修**（document capture 攔截＋測試；Tauri navigation 實效 T15）、H4 background-file 升格=既有 workspace 語意（維持、T15）。
- **Gate G5 ✅**：全部 E 有貼證據且 PASS。

## P6 收尾

- **狀態：DONE**
- **最終變更檔案**（wave 4 全部）：src/editor/EditorPane.tsx＋editorPane.lsp.test.tsx；src/lsp/lspExtensions.ts＋lspExtensions.test.ts（新）＋lspManager.ts＋lspManager.test.ts＋codeActions.ts＋codeActions.test.ts＋symbols.ts（新）＋symbols.test.ts（新）＋diagnosticsPull.ts（註解）；src/workbench/SymbolPicker.tsx（新）＋symbolPicker.test.tsx（新）；src/app/workbench/CommandPalette.tsx＋commandPalette.test.tsx（新）；src/state/workspaceStore.ts；src/workbench/search/SearchResults.tsx＋searchResults.test.tsx；src/lib/i18n.ts。
- **證據摘要**：五件套 492/0/✓/132+1/clean（461→492 只增、無 Errors 行）；P4 四輪對抗 review 26 修 dry pass——含 2 個跨 reviewer 收斂的 major（unchanged stale 座標／format 靜默存檔遺失）、NUL byte 檔案腐壞、diagnostics wiring 通道殘缺（languageServerSupport 不含 serverDiagnostics 的事實糾正）、5 個 capability/grade/async 競態。
- **需使用者確認的假設**（皆 controller 已裁決，供追認）：A1-A4（P1 記載）；serverDiagnostics 掛 LSPClient config（LSPClientExtension 型別）；A4 focus 用 `focus?:` 形狀；C2/unchanged 用 forEachDiagnostic mapped 參數重發；B6 palette 入口維持英文（內部一致性）。
- **殘留事項**：
  - **F1**：EditorPane getDocument 鏈無 .catch（既存）——server 控制 URI／檔案被刪→unhandled rejection＋空 pane＋孤兒 tab；需 error-pane UX 設計（backlog）。
  - **T12（wave 5）**：format tabSize:4 寫死→接 Settings；formatOnSave key 已 export。
  - **consolidation**：palette 全面 i18n；「無符號」等已進 i18n 但 palette 入口英文。
  - **T15 實機**：Radix 雙 dialog 交接（pointer-events/focus-scope）、view.focus 實效、semantic token 著色/CSS additive、contain:paint、both-capable server 雙診斷通道、split-pane 同檔、H3 Tauri navigation handler 實效、cold-start 實測、pull/semantic drift 真 server。
- **自我改進三問**：①哪個閘門沒攔到？——修復批次的單檔驗證讓 unhandled rejection 與 NUL 腐壞溜過，靠 controller 全量整合＋全 src 掃描才攔到→LEARNINGS [tighten] ×2。②哪條規則浪費時間？——無（「全新 reviewer 每輪」在 R2 抓到 NUL、R3 抓到 catch 不完整，正是 fresh context 的價值）。③陷阱/慣例？——subagent Write 可夾 NUL；async IIFE 的 reject 源要窮舉；teammate idle 通知不帶回報須 SendMessage 索取→LEARNINGS。
- **下接 wave 5**：T12 Settings（先，建 openSettings API＋formatOnSave/theme 設定）→ T11 StatusBar（讀 displayFor 渲染「{Lang} · {serverDisplay} {state}」）。
