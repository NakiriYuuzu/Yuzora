# LOOP RUN — m3-wave1

- 日期：2026-07-03
- 狀態：RUNNING
- 任務原文：
  > A-p1的部分需要 i18n；繼續進行下一個 wave，直到 m3 完結
  > 1. subagent opus only ／ 2. 不使用 git add, commit ／ 3. 不使用 worktree

- **使用者追加指令（2026-07-03，M3 收尾適用，跨 run 有效）**：
  > 最後等所有任務完成的時候，請 fable-subagent 進行最後的 review
  - Controller 詮釋：M3 全部 wave（T1–T15）完成後的**最終 review 派 `model: fable` 的全新 reviewer subagent**——使用者明示覆寫鐵律 2 opus-only，僅適用該一次最終 review；各 wave 的 P4 對抗 review 照舊 opus。此指令須傳遞至後續每個 m3-wave* run file。

## P1 提問

**需求（可驗證陳述）**
- R1: T4 lsp_service.rs——process manager 依 plan Interfaces（:256-309）：frame/parse_frames/which 純函式、LspManager/LspState、9 個 lsp_* commands（含 lsp_set_trace A8 裁決：獨立 trace 檔 tee＋主 log 指標事件＋保留 5 份）、crash 退避 MAX_RESTARTS=3、Missing 不 spawn、單一 (ws,lang) server 保證；lib.rs 補 .manage＋9 handler（mod 行已預註冊）；測試依 plan 策略（framing 6+、which 2、lifecycle 4、trace 2，全部不需真 server）。
- R2: T5 types.ts＋ipc.ts——LSP 型別（camelCase 對齊 T4 serde）＋10 個 wrapper（lspStart Channel 模式）＋FileGrade/fileGradeOf；ipc.test.ts 轉發測試＋Channel 事件＋fileGradeOf 推導。
- R3: i18n 最小字串表（使用者裁決 2026-07-03「最小字串表」）——新 src/lib/i18n.ts 集中字串模組，BranchPopover placeholder 接上；其他既有文案不動（漸進遷移屬後續）。
- R4: 五件套全綠且不退化：vitest ≥325（42 檔）、tsc 0、build ✓、cargo ≥103＋1 ignored、fmt 乾淨。
- R5: P4 對抗 review dry pass（每輪全新 opus reviewer）。

**已查資料**
- wave 0 run（DONE）：基線推進至 325/103、fmt 乾淨；lib.rs 已有 4 mod 行；lsp_adapters/lsp_config 已實作（T4 依賴滿足）。
- plan 任務相依表：wave 1＝T4＋T5 併行（lib.rs 僅 T4 動、types/ipc 僅 T5 動）；T5 契約由 plan 固定，不等 T4。
- plan T4（:247-335）／T5（:339-406）Interfaces 已讀入。
- LEARNINGS 已讀（wave 0 新增三條在案：空轉派工／實測 baseline／stub 換行）。

**假設（非 BLOCKING）**
- A1: i18n 字串表命名與形狀由 implementer 依 repo 慣例擇定（純 TS 常數表＋型別，無框架），BranchPopover 為首個消費者（依據：使用者裁決「最小」）。
- A2: T4/T5/i18n 三工並行（檔案零交集；plan 明文 T4/T5 併行）。

**BLOCKING 待問**：無（i18n 形式已由 AskUserQuestion 裁決：最小字串表）。

**Gate G1**：✅

## P2 規劃

**Baseline battery**：wave 0 P5 終驗（17:32 親跑，其後零檔案變更）即本 run baseline：

```
bun run test → 325 passed (325)，42 檔
bunx tsc --noEmit → exit=0
bun run build → ✓ built in 756ms
cargo test → 103 passed; 0 failed; 1 ignored
cargo fmt --check → FMT CLEAN
git status --short → 同 wave 0（單 commit repo 全 untracked）
```

**步驟**
1. 並行派工 opus implementer：T4（lsp_service＋lib.rs）、T5（types+ipc）、i18n-min（i18n.ts＋BranchPopover）→ verify: 各回報後 controller 親跑對應測試。
2. controller 親跑五件套整合 → 預期全綠不退化。
3. P4 全新 opus reviewer 整輪 review → 裁決 → 修復迴圈 → dry pass。
4. P5 五件套終驗＋E 銷帳；P6 收尾（LEDGER、回覆「下接 wave 2」）。

**預計變更檔案**
- T4: src-tauri/src/lsp_service.rs（stub→實作）、src-tauri/src/lib.rs（manage＋9 handler）
- T5: src/lib/types.ts、src/lib/ipc.ts、src/lib/ipc.test.ts（或 types 對應測試檔）
- i18n: src/lib/i18n.ts（新）、src/workbench/git/BranchPopover.tsx、src/workbench/git/branchPopover.test.tsx
- 記帳：本 run file、LEDGER

**不碰範圍（non-goals）**
- 不動 lsp_download.rs（T14/wave 6）、不裝任何依賴（wave 1 無新依賴；@codemirror/lsp-client 屬 wave 2）、不做 i18n 全面遷移、不動 EditorPane/StatusBar/SettingsDialog（wave 3+）、git 唯讀、無 worktree。

**風險**
- lib.rs 為共用檔：wave 1 內僅 T4 動（plan 相依表保證）／守門：五件套＋review scope 掃描。
- lsp_service 測試 spawn 真 process（cat/sh）：時序脆弱風險 ／ 守門：plan 測試策略（trivial 指令＋純函式 framing）＋controller 復驗。
- trace 檔寫 ~/.yuzora/logs：測試須 tempdir 注入，不碰真目錄 ／ 守門：brief 明令＋reviewer rubric。

**驗收證據清單（實作前固定，只可加嚴）**
- E1: T4 cargo test lsp_service 輸出（framing/which/lifecycle/trace 全綠）＋lib.rs 9 handler 註冊。
- E2: T5 vitest ipc/types 輸出（10 wrapper＋Channel＋fileGradeOf）。
- E3: i18n.ts 存在＋BranchPopover 消費＋測試綠。
- E4: 五件套終跑尾段（≥325／0／✓／≥103＋1／CLEAN）。
- E5: P4 dry pass 紀錄（反證 ≥3、findings 全裁決）。

**Gate G2**：✅

## P3 執行

**派工紀錄**
- 17:38 並行派工 3 個 opus implementer（agentType=implementer, model=opus）：
  - T4 lsp_service——brief：plan :247-335 契約逐字（9 commands＋framing/which 純函式＋crash 退避＋trace tee tempdir）、lib.rs 僅 manage＋9 handler、零新依賴、Channel 測試可構造性擇最小方案記 report。回報待收。
  - T5 types+ipc——brief：plan :339-406 契約逐字（10 wrapper＋Channel 模式＋fileGradeOf）、mockIPC 轉發測試、僅動 src/lib 三檔。回報待收。
  - i18n-min——brief：使用者裁決最小字串表；新 src/lib/i18n.ts＋BranchPopover placeholder 接線＋測試 import 同一常數比對；其他文案不動。回報待收。
- 17:51 T5 回報收訖：types.ts +LSP 型別＋fileGradeOf（hasVeryLongLine 復用既有 MAX_LINE_LEN_SYNTAX_OFF=10_000 常數）；ipc.ts +10 wrapper；ipc.test.ts +15 測試（TDD red：15 failed → 43 passed）。content 省略時 fallback result.content（合理詮釋，記錄在案）。
  - **controller 復驗（親跑）**：`bun run test -- src/lib/ipc.test.ts` → `43 passed (43)`；tsc exit=0。✅ 綠。IDE stale 診斷再現（宣稱 lspStart 未 export 與 tsc 全量矛盾）——以 tsc 為準。
- 17:52 i18n-min 回報收訖：src/lib/i18n.ts（strings 常數＋型別，零依賴）；BranchPopover placeholder 接線；測試 import 同一常數斷言（非重複硬編碼）。顯示值不變（分支名稱…）。
  - **controller 復驗（親跑）**：branchPopover 測試 `12 passed (12)`；grep 證實三檔接線（i18n.ts:9 定義／BranchPopover.tsx:211 消費／test:133 同常數斷言）。✅ 綠。E3 PASS。
- 17:56 T4 異常＋收尾：agent 於收尾階段遭 API 連線中斷（failed 通知），但實作已落地——controller 親驗：lsp_service.rs 1042 行、lib.rs setup 內 manage＋恰 9 handler、`cargo test` 120 passed＋1 ignored（+17）、FMT CLEAN。SendMessage 續推補交正式報告，收訖。
  - T4 報告偏差 3 點：(1) LspProcessStatus 加 `rename_all_fields="camelCase"`（installHint 對齊 T5——正確必要）；(2) lsp_start 去掉 unused `app` 參數（tauri 注入項，前端 invoke 契約不變）；(3) which 候選目錄自定為 servers/bin、servers/npm/bin、servers/venv/bin。
  - **controller 裁決偏差 3**：與 plan T14 權威契約不符（:710-712——binary 落 servers/ 根、npm bin 在 servers/npm/node_modules/.bin、pip 在 servers/pyenv/bin）→ 17:58 回派 T4 修正（server_bin_dirs 對齊＋優先序測試）。偏差 1/2 接受（記錄在案）。

- 19:28 which 修正回報收訖：server_bin_dirs_from(base) 純函式（servers 根／npm/node_modules/.bin／pyenv/bin|Scripts）＋優先序測試（tempdir 三處佈局先中先贏）。
  - **controller 復驗（五件套親跑，19:28）**：vitest `340 passed (340)`；tsc 0；build ✓ 752ms；cargo `121 passed; 1 ignored`；FMT CLEAN。✅ 全綠；基線 325/103 → 340/121 只增不減。

**變更檔案**
- src-tauri/src/lsp_service.rs — T4 process manager 全實作（framing/which/lifecycle/crash 退避/trace tee＋18 測試）
- src-tauri/src/lib.rs — T4：setup 內 manage(LspState)＋9 個 lsp_* handler
- src/lib/types.ts — T5：LSP 型別＋FileGrade/fileGradeOf（復用 MAX_LINE_LEN_SYNTAX_OFF）
- src/lib/ipc.ts — T5：10 個 lsp_* wrapper（薄轉發）
- src/lib/ipc.test.ts — T5：+15 測試（轉發/Channel/fileGradeOf）
- src/lib/i18n.ts — i18n 最小字串表（新）
- src/workbench/git/BranchPopover.tsx — placeholder 接 i18n
- src/workbench/git/branchPopover.test.tsx — 斷言改 import 同一常數

**計畫修正**
- #1：T4 偏差 1（serde `rename_all_fields="camelCase"`——plan 契約字面缺此屬性但 T5 需要 installHint 上線鍵）——接受，屬契約補全非偏離。
- #2：T4 偏差 2（lsp_start 去 unused `app` 參數、部分 config command 去 unused `state`）——接受，前端 invoke 契約不變。
- #3：T4 偏差 3（which 候選目錄自定）——**否決**，plan T14 為準，已回派修正並復驗綠（見派工紀錄）。

## P4 對抗 review

**Reviewer 派工**
- 19:30 Round 1：全新 opus reviewer（未參與撰寫）。brief：R1–R8＋rubric 六項＋特別審視點（trace 保留 5 份實作與否／set_server 停舊實作與否／執行緒 Mutex 死鎖與併發 lsp_start 競態／Rust-TS serde 跨語言契約／D1–D3 裁決附上供異議）。回報待收。

**Round 1（reviewer 回報，19:30-19:40）**
- Findings：**F1**（lsp_service.rs:259-311）start() check-then-insert 非原子——併發同 (ws,lang) 雙 spawn、後者覆蓋前者、洩漏 process，破壞單一 server 保證；**F2**（:722-726）set_server 對無法 canonicalize 的 workspace 靜默寫入全域 defaults＋stop_matching(None) 停掉該語言全部 server（與 lsp_start :662 的 raw fallback 不一致）；**F3**（:565-582）trace 開檔失敗仍寫主 log 指標＋回 Ok（誤導性狀態）；**F4**（:484-496）watch_loop Crashed 分支缺 stopped 二次檢查（:499 重試分支有），併發 stop 時 Stopped 被覆寫成 Crashed＋spurious emit。
- 反證通過（摘）：framing 六邊界守住（length-based 抽取）；鎖序 servers→info 全程一致無死鎖；kill 後 watch_loop try_wait reap 無殭屍；HOME 缺失退 PATH 絕對路徑；R3 保留策略**已實作**（TRACE_KEEP=5＋TRACE_MAX_BYTES=50MB＋重啟 off）；R4 停舊**已實作**（stop_matching 雙向比對）。
- 裁決異議：無（D1/D2/D3 維持）。
- Rubric 六項全掃；Not reviewed：真 server 端到端（T15）、Windows 實測（trivial 指令測試策略為 plan 認可）。

**Findings 裁決**
- F1: 真 finding（違反 plan 明文單一 server 保證）→ 回 P3 派修（check-and-reserve 進單一臨界區）。
- F2: 真 finding（Some(workspace) 不得降級 None——比照 lsp_start raw fallback）→ 回 P3 派修。
- F3: 真 finding（低）→ 回 P3 派修（開檔失敗回 Err 或明確 disabled，不寫 enabled 指標）。
- F4: 真 finding（低）→ 回 P3 派修（Crashed 分支補 stopped 二次檢查，鏡像 :499）。

**F1–F4 修復（20:05 前後，回派 T4 implementer）**
- F1：check-and-reserve 單一臨界區（佔位 entry＋鎖外 spawn＋失敗回收；敗者回 Starting info）＋確定性併發測試 `concurrent_start_spawns_exactly_once`（TDD red：got 2 → 綠）。
- F2：抽 `apply_set_server` 純函式＋raw fallback（測試：不存在路徑寫 workspaces raw key、defaults 不變）。
- F3：set_trace 回 Result，開檔失敗 Err＋不寫指標（測試：同名檔擋目錄 → is_err）。
- F4：Crashed 分支補 stopped 二次檢查（鏡像 :499；結構性修復，crash_backoff 不退化——競態不可確定性驅動，finding 允許路徑）。
- 作者自報既有窄競態備查：reserve 窗口內 stop_workspace 抽走 entry → spawn child 短暫未追蹤至自行退出。
- **controller 復驗（親跑）**：cargo `124 passed; 1 ignored`（121→124 只增）；FMT CLEAN；三新測試逐條 ok。✅ 綠。

**Round 2 派工（20:07）**：全新 opus reviewer，整輪重審＋F1 修復專項反證（佔位 status 更新／失敗回收後重試／敗者 info 一致性／reserve 窗口 stop 行為）＋F4 終態一致性；窄競態備查項開放異議。回報待收。

**Round 2（全新 reviewer 回報，20:07-20:16）**
- Findings：**F-A** Crashed entry 永駐 map——lsp_start 對既有 entry 一律回快照，crash 後重試變靜默 no-op，唯 stop_workspace/set_server 能清（:307-309＋:494-511）；**F-B** 併發 start＋spawn 失敗——敗者拿 Starting 快照、map 已空、無 watch loop（:305-320 窄窗）；**F-C** which 無 Windows .exe/PATHEXT 處理，Windows 上全 Missing（:113-131，與 :145-149 的 cfg!(windows) 分支不一致）；**F-D** 健康 stderr 無條件寫 last_error、Crashed reason 取 startup banner（:654-667＋:502——與 plan 字面一致，診斷品質問題）。
- **裁決異議（成立）**：作者備查的 reserve 窗口競態被定性「短暫」不成立——stop 發生於 spawn_child 期間時 child 尚為 None 無從 kill，spawn 完成後 watch_loop 只在 child 退出後才查 stopped；長壽命 server 永不自退 → **永久孤兒 process＋執行緒**（rust-analyzer 級 1-2GB 不可回收）。必修：spawn 後補 stopped 重查（kill＋回 Stopped）。→ 記 **F-E**。
- 反證通過 11 條（摘）：F1 佔位 status 語意正確（無 Ready by design）；spawn 失敗回收後可重試；F4 三處 stopped 檢查全讓位；F3/F2 修復守住；serde↔TS 逐鍵一致（Tauri v2 camelCase 自動轉換與既有 search_workspace 一致）；fileGradeOf 全分支；執行緒測試無 flaky 味（determinism 逐條論證）。
- Rubric 六項全掃；Not reviewed：真 server 端到端（T15）、T6 前端 orchestration（未來 wave）、Windows 實機、全量 battery（reviewer 只跑 targeted——controller 五件套另行把關）。

**Findings 裁決（Round 2）**
- F-E（裁決異議升級）：真 finding、最高優先 → 回 P3 派修（spawn_child 成功後重查 shared.stopped：true 則 kill child＋回 Stopped info，不留孤兒）。
- F-A: 真 finding → 修（start() 見既有 entry 為 Crashed（終態、無活 process）→ remove＋重新 reserve＋spawn，重試語意恢復；auto-backoff 停止語意不變——重試需明確新 lsp_start）。
- F-B: 真 finding（窄）→ 與 F-A 統一修法：spawn 失敗不 remove，改保留 entry 標 Crashed（含 reason）——敗者後續 lsp_status 可見 Crashed，下次 start 走 F-A 重試路徑。
- F-C: 真 finding（低）→ 修（windows 候選補 command.exe/.cmd，抽純函式測試）。
- F-D: **defer**（實作忠實於 plan 字面「前 N 行存 last_startup_log/last_error」；tail-at-crash 診斷品質改善記 M3 backlog polish，P6 揭露）。

**F-E/F-A/F-B/F-C 修復（20:20-20:26，回派 T4 implementer）**
- F-E：spawn 成功後、啟 watcher 前重查 shared.stopped——true 則 kill+wait reap、設 Stopped、不啟 watch_loop（結構修復；fork/exec 微秒窗無法確定性驅動，沿 F4 前例附論證）。
- F-A：existing entry 為 Crashed → remove＋fresh reserve＋spawn（restart_count 歸零）；TDD red 在案。
- F-B：spawn 失敗不 remove，entry 標 Crashed 保留（併發敗者 lsp_status 可見；下次 start 走 F-A 重試）；TDD red 在案（以不存在 cwd 確定性觸發 spawn Err）。
- F-C：candidate_names_for(command, windows) 純函式——unix [command]；windows [command, .exe, .cmd]；雙平台 shape 測試。
- **controller 復驗（親跑）**：cargo `127 passed; 1 ignored`（124→127 只增）；check Finished 無 warning；FMT CLEAN。✅ 綠。（IDE panic.rs Debug 診斷與 cargo 實跑矛盾，判 stale。）

**Round 3 派工（20:27）**：第三位全新 opus reviewer，整輪重審＋crash-retry／spawn-fail-retained／post-spawn stopped 三個新語意的專項反證。回報待收。

**Round 3（第三位全新 reviewer 回報，20:27-20:37）**
- Findings：**F-R3-1**（lsp_service.rs:570-583）——F-E 的 post-spawn stopped 重查只補在 start()；watch_loop 退避重試的 spawn_child 成功點無同樣守衛，stop 撞上 respawn 窗口（child 賦值前 stop 殺到的是已 reap 的舊 child）→ 活 server＋reader/stderr/watch 三 thread 永久洩漏（inner loop 的 stopped 檢查僅在 child exit 後可達，健康 server 不自退）。屬 F-E 修復留下的新缺口。
- 裁決異議：無（八條已修＋D1–D3＋F-D defer 全數核可）。
- 反證通過 7 條（摘）：crash-retry 併發雙 remove 不可能（remove+insert 同一臨界區、mutex 序列化）；spawn-fail-retained entry 可被 stop 清走；start() 路徑 post-spawn 收斂正確；map 層終態機封閉（Stopped 永不駐 map）；serde 契約逐欄實證；fileGradeOf 邊界嚴格大於一致；send 於 Starting 窗口回 Err 合理。
- 觀察（非 finding）：cleanup_traces 5-keep/50MB 無專屬測試（plan 未要求）；$HOME 未設→相對路徑極端邊界（實務可忽略）。
- Rubric 六項全掃；Not reviewed：F-R3-1 無法不改 source 確定性重現（靜態分析呈報）、真 server 端到端（T15）、全量 battery（controller 把關）。

**F-R3-1 裁決**：真 finding（違反「關閉 workspace 停 process」保證，與 F-E 同 hazard 不同落點）→ 回 P3 派修（watch_loop 重試 spawn 成功後鏡像 F-E 區塊：重查 stopped → kill+reap＋set Stopped＋return）。

**F-R3-1 修復（20:40，回派 T4 implementer）**
- watch_loop 重試分支 spawn 成功後鏡像 F-E 守衛（:584-599：重查 stopped → kill+wait reap＋Stopped＋return）；結構修復＋鎖序論證（child→info，未持 servers）。
- **controller 復驗（親跑）**：cargo `127 passed; 1 ignored`；FMT CLEAN；守衛程式碼與註解 grep 確認在案（:585-599）。✅ 綠。

**Round 4 派工（20:42）**：第四位全新 opus reviewer，整輪重審＋雙 post-spawn 守衛狀態機全路徑窮舉專項。回報待收。

**Round 4（第四位全新 reviewer 回報，20:42-20:54）**
- Findings：**F-R4-1** parse_frames 對超大 Content-Length 無上界（:86/:89/:104）——溢位 panic（debug add-overflow／release slice-index，reviewer scratch 重現）→ reader thread unwind 死、child 仍活、watch_loop 不觸發 → server 永久假死無自癒；另 buf 無界成長。**F-R4-2** 全檔無 Drop/shutdown 兜底——app 退出（含 hard-quit 前的正常路徑）不殺 child，rust-analyzer 級 server 變孤兒（std Child drop 不 kill；askpass 已有 Drop 前例）。**F-R4-3**（低）併發同 key start 敗者拿 Starting 快照後 entry 隨即變 Crashed（F-B spawn-fail 路徑不 emit）——無更正訊號需自行 poll。
- 裁決異議：無（前三輪 9 修＋defer 全數複驗成立）。
- 反證通過 7 條（摘）：F-E／F-R3-1 雙守衛 × stop 時機四象限窮舉全成立（每個 child 都有對應 reaper）；五鎖死結搜尋無反轉；kill-without-wait 無 zombie；fileGradeOf 空字串邊界；stop_matching raw/canonical 橋接；F1 恰一次 spawn 實證。
- 觀察（非 finding，記錄）：crash-backoff silent respawn 沿用同 Channel、成功不 emit——前端是否偵測/重握手列 **T6 設計注意＋T15 gui-acceptance 驗證項**；LspManager 自建第二 LogSink 與 lib.rs managed sink 並行寫同檔（O_APPEND 實務安全）；lsp_config_set_server RMW 無鎖（UI 序列化下無虞）。
- R8 缺口（記錄）：F-E／F-R3-1 守衛本質無法自動化 race 測試（狀態機窮舉人工驗證），非 blocking。

**Findings 裁決（Round 4）**
- F-R4-1: 真 finding（協定層防禦缺失，觸發路徑現實：stdout banner／錯誤 binary／pipe 損毀）→ 回 P3 派修（Content-Length 上界＋checked 運算；超限視為 malformed 走既有 drain 路徑）。
- F-R4-2: 真 finding（資源洩漏兜底缺失）→ 回 P3 派修（impl Drop for LspManager：best-effort kill+wait 全部 child；graceful exit 有效、SIGKILL 無解為已知極限）。
- F-R4-3: 真 finding（低）→ 回 P3 派修（spawn-fail 設 Crashed 後補 emit "lsp:server-status"，與 crash-backoff 一致）。

**F-R4-1/2/3 修復（21:00-21:09，回派 T4 implementer）**
- F-R4-1：MAX_FRAME_BYTES=64MB＋checked_add＋超限走 malformed drain；兩測（overflow/oversize）TDD red 在案。
- F-R4-2：impl Drop for LspManager（stopped→kill+wait 全 child）＋**必要結構改造：worker threads 改持 Weak<LspManager>**（否則強 Arc 釘死 manager、Drop 永不可達）——沿 askpass Weak 前例；drop_manager_reaps_running_servers 測試（TDD red：process still alive → 綠）。
- F-R4-3：spawn-fail 設 Crashed 後補 emit；既有測試擴充斷言。
- **controller 復驗（親跑）**：cargo `130 passed; 1 ignored`（127→130 只增）；FMT CLEAN；四關鍵測試逐條 ok。✅ 綠。

**Round 5 派工（21:10）**：第五位全新 opus reviewer，整輪重審＋Weak 改造六攻擊面專項（upgrade 失敗行為／watch_loop tail 時序／Drop×respawn 競態／死鎖／正常運行等價性）。回報待收。

**Round 5（第五位全新 reviewer 回報，21:10-21:26）**
- **Weak 改造六攻擊面全部 held**：reader upgrade 失敗僅跳過 trace、EOF 自退無 panic；watch_loop upgrade 失敗僅發生於 Drop 後（child 已被收）；Drop×respawn 以 strong Arc 持有互斥不可能並發；trace/emit 生命週期安全；五鎖無 ABBA；正常運行與改造前等價（唯一差異＝teardown 真的會 kill child，即 F-R4-2 目標）。
- Findings：**F-R5-1**（:523-543＋:76-107）reader 累積 buffer 無上限＋O(n²) 重掃——MAX_FRAME_BYTES 只限「已宣告的 frame body」，無 `\r\n\r\n` 的非 framed 流（錯誤 binary usage 輸出／噴 garbage 的 server）使 buf 無界成長（reviewer scratch 實測 20MiB／10.84s O(n²)），F-R4-1 註解「never grow without limit」overclaim 被反證。
- 觀察（非 finding，記錄）：`std::process::exit` 路徑不跑 destructor（平台行為，Drop 兜底極限）；**canonical vs raw workspace keying**——config 用 canonical 解、map 用 raw key，前端對同一目錄傳兩種 raw 會開兩個 server → **T6 前端契約注意（brief 需載明：workspace 一律傳一致路徑）**。
- 裁決異議：無。Rubric 六項全掃。

**F-R5-1 裁決**：真 finding（resource exhaustion，觸發面現實）→ 回 P3 派修（raw buffer 上界＋非 framed 流 resync／丟棄＋避免每 chunk 全量重掃；well-framed 路徑不受影響）。

**F-R5-1 修復（21:30-21:35，回派 T4 implementer）**
- MAX_HEADER_BYTES=8KB＋三守衛（無 delim 超界 resync 保留末 3 bytes／超長 header drain 穿過 delim／既有 body 上界不變）＋DELIM 常數化＋註解校正不 overclaim；+2 測試（TDD red：131072 無界 → 綠；不變式斷言取代計時防 flaky）。
- **controller 復驗（親跑）**：cargo `132 passed; 1 ignored`（130→132 只增）；FMT CLEAN；parse_frames 9 測全綠。✅

**Round 6 派工（21:36）**：第六位全新 opus reviewer，整輪重審＋parse_frames 重寫六攻擊面專項（合法大 body 累積不誤觸 resync／病態卡 body 有界性／partial header 保留／delim 跨 resync 邊界 byte 級驗證／超長 header 殘餘連鎖／守衛優先序確定性）。回報待收。

**Round 6（第六位全新 reviewer 回報，21:36-21:45）**
- **0 findings**；裁決異議：無（3-byte 保留證為充要下界；64MB 上界為蓄意權衡）。
- parse_frames 六攻擊面全 held（reviewer 以 scratchpad 逐字複製實作獨立編譯驅動）：10MB 合法 body 累積不誤觸 resync 且 O(header)/chunk；病態卡 body 有界 ~64MB；恰 8192 partial header 保留（嚴格大於）；delim 跨 resync 邊界 byte 級重組成功；超長 header 殘餘遺失屬 malformed 固有（合法流永不觸發）；三守衛順序分支唯一確定、必終止（1000 連續空 delim 無 spin）。
- 額外掃描 held：鎖階層一致、stopped 守衛、content_length 各邊界。
- Not reviewed：真 server 端到端（T15）、T6+ 下游、實機資源上界。

**Dry pass**：✅ Round 6 整輪 0 條新 finding；六輪累計 13 findings 全修復＋3 defer/benign 全裁決；反證紀錄合計 40+ 條。**Gate G4 ✅**

## P5 驗收（controller 親跑，21:47）

- E1: T4——`cargo test lsp_service` 27 測全綠（含 framing 9／lifecycle／trace／Drop／併發）；lib.rs 9 handler（grep 證據在 P3）。**PASS**
- E2: T5——`bun run test -- src/lib/ipc.test.ts` → `43 passed (43)`（10 wrapper＋Channel＋fileGradeOf）。**PASS**
- E3: i18n——branchPopover `12 passed (12)`＋三檔接線 grep（P3 段）。**PASS**
- E4: 五件套終跑 →

  ```
  bun run test  → Tests 340 passed (340)，42 檔
  bunx tsc --noEmit → exit=0
  bun run build → ✓ built in 909ms（exit=0）
  cargo test    → test result: ok. 132 passed; 0 failed; 1 ignored
  cargo fmt --check → FMT CLEAN
  ```

  **PASS**（基線 325/103 → 340/132 只增不減）
- E5: dry pass 紀錄（P4 六輪全記錄）。**PASS**
- 與 baseline 比對：無新紅燈 ✅

**Gate G5**：✅

## P6 收尾

- 結果：**DONE**（wave 1 完成：T4 lsp_service＋T5 types/ipc＋i18n 最小字串表；六輪對抗 review、13 findings 修復、dry pass）
- 最終變更檔案：見 P3 變更檔案清單（8 檔）；git 零寫入、零新依賴、無 worktree。
- 需使用者確認的假設：無新增（i18n 形式已裁決）。
- 殘留事項（移交）：
  - F-D defer：stderr 前 N 行寫 last_error（plan 字面）——tail-at-crash 診斷品質改善記 backlog。
  - T6 契約注意（wave 2 brief 載明）：workspace 路徑前後端一律傳一致（canonical vs raw keying）；silent respawn 沿用 Channel——前端偵測/重握手設計＋T15 實機驗證。
  - 觀察備查：雙 LogSink 並行寫、config RMW 無鎖、process::exit 不跑 destructor、F-E/F-R3-1 守衛無自動化 race 測試（本質難測）。
  - **使用者指令**：M3 全部完成後最終 review 派 **fable** subagent（見 P1 追加指令）。
- 自我改進三問：閘門有效（review 六輪逐層挖出 13 條真 bug——check-then-insert、孤兒 process、framing 防禦三層）；無建議放寬；新學到——「review 修復引入的結構性改動（Weak 化、parse 重寫）必須成為下一輪專項攻擊面」已在本 run 落實為慣例。
- LEDGER 已追加：✅
- 下一步：wave 2（T6 client glue＋T13 Markdown preview；lead 先統一 bun add）。
