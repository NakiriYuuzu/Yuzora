# LOOP RUN — m3-closeout

- 日期：2026-07-05
- 狀態：DONE
- 任務原文：
  > M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示；唯一非 opus 例外）＋結案報告（docs/html 慣例）。

## P1 提問

**需求**
- R1：fable subagent 對 M3 整體做最終 review（plan Coverage :807-814／Verification :816-821 對照、15 tasks 落地抽驗、跨 wave 整合面、殘留清單完備性、五件套親跑覆核）。
- R2：findings（若有）照閉環裁決→派修（opus）→重驗。
- R3：M3 結案報告 `docs/html/yuzora-m3-lsp-closeout-2026-07-05.html`（opus subagent 撰寫；controller 只記帳）。
- R4：實機 checklist（m3-manual-checklist）為使用者配合項，不在本 run 內執行；報告與回覆中明列。

**已查**：wave 0-6 全 DONE（LEDGER）；P5 終驗 532/165+1 全綠＋smoke PASS。

**假設（非 BLOCKING）**：fable review 為 fresh-context 審計（不繼承對話），brief 給 plan/LEDGER/runs 路徑自行取證。

**Gate G1**：✅

## P2 規劃

1. 派 fable reviewer（subagent_type reviewer、model fable）→ controller 裁決。
2. findings 修復（若有，opus）→ 重驗。
3. 派 opus 撰寫結案報告 html → controller 親核內容與事實。
4. LEDGER 收尾；回報使用者（含實機 checklist 待辦）。

**Gate G2**：✅

## P3 執行

**fable 最終 reviewer（m3-final-fable）派出（14:0x）**：五審計面——plan coverage 逐 task 抽驗、跨 wave 三條端到端整合鏈（StatusBar→Settings 高亮／install→override resolve→事件終態／EditorPane gating→五態）、五件套親跑覆核、殘留清單完備性、自由裁量高風險抽查。等回報。

**fable 回報（14:3x）：M3 FINDINGS: 3（零 major）＋整體判定自動側閉合成立** — controller 親驗裁決（14:4x）
- 審計面結論：coverage 15 tasks 逐一 ✓（含 m1-m7 清債逐點核）；三鏈閉合；五件套親跑與宣稱一致（532/165+1/tsc 0/build ✓/fmt 0）；殘留除 M3F-1 矛盾外皆有去處；抽查三面（sanitize 鏈 FORBID 逐字一致＋contain:paint、install 輸入驗證白名單、wave5 R3 修復宣稱與碼一致）全 holds。
- **M3F-1〔minor〕checklist 第 5 項預期「切 profile 即時重啟」與已裁決 backlog「不重掛」矛盾 — 確認修（親驗 :102-103 文字在）**：加 caveat（標注 backlog、替代驗法：重開檔/重啟 app 後驗生效）＋H 節補列。→ w6-t15-fixtures。
- **M3F-2〔minor/latent〕npm/pip/venv 子程序無 timeout → hang 時 InflightGuard 永鎖＋前端進度永卡 — 確認修（親驗 :456-465 `.status()` 無限等待；git_service :87/:118 有現成 deadline poll+kill 模式）**：run_command 加 timeout（仿 git_service 模式）＋timeout kill 測試。fable 已核 W6A-F4 只裁決 download 側，此為真 gap 非重複。→ w6-t14-install。
- **M3F-3〔nit〕handleInstall setServerInfo 缺 workspace currency guard — 確認修（親驗 :633 無條件寫 vs lspManager :113/:126/:154 三處 guard 慣例）**：發起時 workspacePath 快照與 settle 時現值比對，相等才寫入（null==null 允許）＋測試。→ w5-t12-settings。
- 三修檔域零交集並行；修完 controller 重驗 → fresh reviewer 驗證輪 → 結案報告。

**M3F-1 修復回報（w6-t15-fixtures，15:0x）**：第 5 項 profile 小節改寫（預期＝寫 config＋停舊 server；驗證方式＝重開檔/重啟 app 後驗新 profile 生效；標 backlog 出處）＋H 節補「profile 切換不即時重掛」一列；grep 核無舊假宣稱殘留；implementer 自行核實 setProfile/stop_matching 程式碼現實。待 controller 親驗（與其餘兩修一次驗）。

**M3F-3 修復回報（w5-t12-settings，15:1x）**：wsAtRequest 快照＋settle 比對守衛（null===null 允許；快照兼作 install 呼叫參數）；+2 測 outcome-based（settle 前切 workspace→servers.python 仍 undefined 修前紅；同 workspace 照寫）。自證 23/23、tsc 0、全量 534 無 Errors。待整合驗。

**M3F-2 修復回報（w6-t14-install，15:2x）**：run_command 改 spawn＋deadline try_wait 輪詢＋逾時 kill+wait（逐字仿 git_service :118/:122-124）；常數 NPM_PIP 600s／VENV 120s（differentiate 有理由）；stdio inherit 維持（無 pipe deadlock）。TDD red 3（unimplemented）→green 3（含 `run_command_times_out_and_kills`：sleep 30＋300ms timeout 於 0.34s 回 Err 證明真 kill）。cargo 165→168、fmt 0。

### 三修整合 battery（controller 親跑，15:2x）
- vitest **534/534 無 Errors**（532→534 只增）／tsc **0**／build **✓ 1.08s**／cargo **168 passed／1 ignored**（165→168 只增）／fmt **0**／NUL（src＋src-tauri）**乾淨**
- spot-read 三修全部吻合裁決：run_command deadline 模式＋雙常數（:460-461/:466-476）；wsAtRequest 快照＋比對（SettingsDialog :627/:637-640，註解引 R2-5 慣例）；checklist 第 5 項 caveat＋H 節列（:100-105）。
→ 派 fresh opus reviewer 驗證三修（closeout 驗證輪），dry 後結案報告。

**驗證輪（m3c-r2-verify，15:3x 派出）**：三修落地驗證＋關鍵疑點——M3F-1 修後文字「重開檔（觸發 remount/ensureClient）生效」是否成立（fable 原 finding 稱 lspManager clients cache 無 per-language 清除→cached 死 client 可能在重開檔時被 reuse；若成立，checklist 驗證方式須再改為「切 workspace 或重啟 app」）；M3F-2 輪詢/kill/zombie/mutation；M3F-3 快照語意/guard mutation。

**驗證輪回報（15:5x）：M3F-2/M3F-3 落地正確（自跑 3 passed 0.34s／23 passed＋mutation 皆鎖）；M3F-1 有問題 → M3C-F1** — controller 裁決（16:0x）
- **M3C-F1〔minor〕「重開檔生效」為假宣稱 — 確認修（關鍵疑點命中）**：cache 鍵 (workspace, language)（lspManager:50）、ensureClient cache-hit 直回舊 client（:66-67）、EditorPane unmount 不清 cache（:218-231）、僅 stopWorkspace（workspace 切換）/重啟清除（:95/:117/:178 三處 delete 皆不在重開檔路徑）；四路反證（unmount/transport 吞錯/setConfig 不動 workspacePath/redetect 不清 cache）皆不倒。修：checklist :105 與 :201 兩處拿掉「重開該語言檔案」選項，改「app 重啟或切至別 workspace 再切回（觸發 stopWorkspace 清 cache）後生效；同 session 同 workspace 重開檔不生效（client cache 以 (workspace, language) 為鍵）」。→ w6-t15-fixtures。
- **run_command kill 不殺 process group（npm 孫程序孤兒）— hold 記殘留**：latent、600s 病態逾時才發生、與 git_service 既有 run_git pattern 一致；記 backlog（與 git_service 同批 process-group kill 工程）。
- 修完親驗 → mini fresh reviewer 驗兩處措辭 → dry → 結案報告。

**M3C-F1 修文回報＋controller 親驗 ✅（16:2x）**：兩處措辭修正（app 重啟或切 workspace 再切回生效；同 session 同 workspace 重開檔不生效＋cache 鍵原因）；implementer 主動同步清掉 H 節同根源「下次檔案 mount」假宣稱；grep 舊宣稱零殘留。親驗 :102-107/:202 吻合。
**最終驗證輪（m3c-r3-final，16:3x 派出）**：兩段文字逐宣稱（①-⑤）對碼核驗，重點「切至其他 workspace 再切回」的 stopWorkspace 參數語意（清的是哪邊的鍵）與「未裝→Missing」鏈。CLEAN 即收斂。

**最終驗證輪回報（16:4x）：CLEAN**——五宣稱逐一 ✓（①config 落盤＋stop 鏈、②無 remount、③重啟/round-trip 清 cache 語意（stopWorkspace 唯一呼叫點＝LspBridge cleanup、A→B 清 A、回 A miss）、④cache-hit 唯 stopWorkspace/重啟可破、⑤Missing 鏈至一鍵安裝按鈕）；三路反證守住（清錯邊/startClient deletes 只動 null entry/舊 server stale Ready 非 checklist 宣稱範圍）；tsc 0 佐證無碼漂移。
**review 循環收斂：fable 3 findings → 3 修 → 驗證輪 1 finding（M3C-F1）→ 修 → 最終輪 CLEAN。Gate G4 ✅**
→ 派 opus 撰寫結案報告。

## P4 對抗 review

（fable 最終 review 即本 run 的 review 階段）

**結案報告（m3c-report，16:4x）＋controller 親核 ✅**：docs/html/yuzora-m3-lsp-closeout-2026-07-05.html（56KB 自包含 6 tabs；唯一 http 命中為 SVG xmlns；NUL 乾淨）。撰寫者主動修正 controller brief 數據錯誤（wave3＝9 修非 11，11 為 cluster 數；總計 125 修 36 輪由此校正）——抽驗與 run files/LEDGER 一致。

## P5 驗收

- Review 循環：fable 3 findings（M3F-1/2/3）→ 3 修 → 驗證輪 1（M3C-F1）→ 修 → 最終輪 **CLEAN**。
- 三修後整合 battery（controller 親跑 15:2x）：vitest 534 無 Errors／tsc 0／build ✓／cargo 168+1／fmt 0／NUL 乾淨。
- 結案報告落地並親核（數據抽驗吻合）。
- **Gate G5 ✅**

## P6 收尾

- M3（wave 0-6＋closeout）全部 DONE；自動側完結。**最終 review 已依使用者指令由 fable 執行**（唯一非 opus 例外），其 3 findings 與後續 M3C-F1 全數修復收斂。
- 新殘留（本 run 產出）：run_command kill 不殺 process group（npm 孫程序孤兒；與 git_service 既有 pattern 同批 backlog）。
- 交付待辦（使用者配合）：m3-manual-checklist.md 實機驗收（12 主項＋歷史 A-H；前置＝fixture 產生器＋debug bundle 兩步＋gui-acceptance）。
- LEDGER：closeout → DONE。

- 狀態：**DONE**（2026-07-05 16:5x）
