# LOOP RUN — m3-wave2

- 日期：2026-07-03（收尾 2026-07-04）
- 狀態：DONE
- 任務原文：
  > （沿使用者指示「繼續進行下一個 wave，直到 m3 完結」；三約束：opus subagent／git 唯讀／無 worktree）
- **跨 run 指令備忘**：M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示，見 wave1 run P1）。
- **使用者裁決（2026-07-03，本 run 起跨 run 適用）**：「沒問題，全部進 ultracode，最後再由 fable-agents 進行review」——
  - P4 對抗 review 改用 **ultracode Workflow**：fan-out 多視角 fresh reviewer（全部 `model: opus`）＋findings 對抗驗證＋loop-until-dry；controller 保留裁決權、P5 親驗、記帳親寫（hybrid 模式，閉環骨架不變）。
  - 實作 fan-out（如 wave 3 T7/T8/T9）亦可經 Workflow 併行（檔案白名單零交集前提）。
  - Findings 修復仍回派原 implementer（SendMessage 續推，脈絡保留）。
  - M3 完結後最終 review：**fable agents**（可多個 fan-out，`model: fable`）——唯一非 opus 例外，使用者明示。

## P1 提問

**需求（可驗證陳述）**
- R1: T6 前端 client glue——src/lsp/{transport,lspManager,lspExtensions,workspace}.ts＋src/state/lspStore.ts＋src/workbench/LspBridge.tsx＋App.tsx 掛載，依 plan :410-487 契約（Transport 以安裝後 d.ts 為準）；YuzoraWorkspace（A1 裁決：背景載入＋LRU 20＋rename 背景寫回＋recentlySaved＋dirty 分頁鐵則）；大檔守衛（非 full 回 null）；deriveDisplayState 五態；format-on-save gating＋save flush 邏輯（A7: 預設 OFF）。
- R2: T13 Markdown preview——src/workbench/MarkdownPreview.tsx＋markdownPreview.test.tsx＋TabBar.tsx toggle 入口（A4 裁決：markdown-it＋sanitize；不動 EditorPane）。
- R3: 依賴由 lead 統一安裝（plan wave 2 序列化明文）：@codemirror/lsp-client、markdown-it（＋types）、DOMPurify（A4 白名單擇一）；@codemirror/lint 若非 transitive 補裝。
- R4: 五件套全綠不退化：vitest ≥340（42 檔）、tsc 0、build ✓、cargo ≥132＋1 ignored、fmt 乾淨。
- R5: P4 dry pass（每輪全新 opus reviewer）。

**已查資料**
- wave 1 DONE（LEDGER）：340/132 全綠；lsp_service 9 command 契約在案。
- plan T6（:410-487）／T13（:674-694）已讀；相依表 wave 2 note：bun add 序列化、T13 掛載點＝TabBar。
- wave 1 review 移交注意（本 run brief 必載）：(a) workspace 路徑一致性——前端對同一 workspace 一律傳同一字串（建議 canonical），lspStart/lspSend/lspStopWorkspace 一致；(b) silent respawn 沿用 Channel、成功不 emit——前端無從知悉需重 initialize，T6 記為 module doc 已知限制＋T15 實機驗證。
- LEARNINGS 已讀（bun 增量 add 不重解 peer deps——本次為全新依賴非 major 升級，風險低；裝後仍檢查 lockfile）。

**假設（非 BLOCKING）**
- A1: sanitizer 選 DOMPurify（A4 裁決允許「DOMPurify 或等效白名單擇一記 report」；DOMPurify 為業界標準、自帶 types）。
- A2: T6/T13 並行（原始碼零交集；依賴已由 lead 預裝，無 package.json 競寫）。

**BLOCKING 待問**：無。

**Gate G1**：✅

## P2 規劃

**Baseline battery**：wave 1 P5 終驗（21:47 親跑）＝本 run baseline：vitest 340/340（42 檔）、tsc 0、build ✓ 909ms、cargo 132＋1 ignored、fmt CLEAN。

**步驟**
1. lead 統一 bun add（@codemirror/lsp-client、markdown-it、@types/markdown-it、dompurify；查 @codemirror/lint transitive）→ verify: bun run test＋tsc＋build 仍綠。
2. 並行派工 opus implementer：T6、T13 → verify: 各回報後 controller 親跑對應測試。
3. controller 五件套整合 → 全綠不退化。
4. P4 全新 opus reviewer（輪替修復至 dry pass）。
5. P5 終驗＋P6 收尾（LEDGER、下接 wave 3）。

**預計變更檔案**
- lead: package.json、bun.lock
- T6: src/lsp/{transport,lspManager,lspExtensions,workspace}.ts（新）＋對應 test、src/state/lspStore.ts（新）＋test、src/workbench/LspBridge.tsx（新）、src/App.tsx（掛載）
- T13: src/workbench/MarkdownPreview.tsx（新）＋markdownPreview.test.tsx、src/workbench/TabBar.tsx（toggle 入口）

**不碰範圍（non-goals）**
- 不動 EditorPane.tsx（T10/wave 4）、lspExtensions 只組官方基礎＋placeholder（T7/T8/T9 於 wave 3 產出、T10 wire）、不動 StatusBar/SettingsDialog（wave 5）、不動 src-tauri/**、git 唯讀。

**風險**
- @codemirror/lsp-client API 名與 plan 假設不符 ／ 守門：T6 Step 1 核 d.ts、差異記 report（plan 風險表明列）。
- CM6 多版本共存（@codemirror/* 峰值依賴）／ 守門：裝後 tsc＋vitest 全量；bun.lock 查重複版本。
- jsdom 下 CM6 無 layout ／ 守門：沿 M1/M2 慣例斷言 DOM 結構與 state。

**驗收證據清單（實作前固定，只可加嚴）**
- E1: 依賴安裝後 vitest/tsc/build 綠（lead 步驟）。
- E2: T6 測試綠（transport 轉發／lazy 單一 client／大檔守衛 null／五態推導／workspace 四測：背景 didOpen、rename 寫回＋recentlySaved、dirty 分頁不覆寫、LRU didClose）。
- E3: T13 測試綠（渲染結構／script 移除／大檔降級）＋TabBar 入口。
- E4: 五件套終跑（≥340／0／✓／≥132＋1／CLEAN）。
- E5: P4 dry pass 紀錄。

**Gate G2**：✅

## P3 執行

**Lead 依賴安裝（21:13，plan wave 2 序列化明文）**
- `bun add @codemirror/lsp-client markdown-it @types/markdown-it dompurify` → lsp-client@6.2.5、markdown-it@14.3.0、@types/markdown-it@14.1.2、dompurify@3.4.11（17 packages）。
- @codemirror/lint@6.9.7 為 transitive（lockfile＋node_modules 確認）→ 免另裝；@codemirror/state 單一版本 6.7.0（無峰值重複）。
- verify（親跑）：vitest `340 passed (340)`、tsc exit=0、build ✓ 844ms。✅ E1 PASS。

**派工紀錄**
- 21:15 並行派工 2 個 opus implementer（agentType=implementer, model=opus）：
  - T6 client glue——brief：plan :410-487 契約＋d.ts 為準、YuzoraWorkspace A1 四測、大檔守衛、五態推導、LspBridge＋App 掛載、wave 1 移交注意（workspace 路徑一致性／silent respawn module doc）、禁 bun add。回報待收。
  - T13 Markdown preview——brief：markdown-it＋DOMPurify（A4）、TabBar toggle、sanitize `<script>` 斷言、大檔降級、不動 EditorPane/App、禁 bun add。
- 21:24 T13 回報收訖：MarkdownPreview.tsx（renderMarkdown html:true＋DOMPurify、isMarkdownPath、per-path zustand 開關、portal overlay 面板）＋TabBar toggle＋16 測試（TDD red：import 失敗 → 16 passed）。設計決定記錄：(a) overlay portal 而非 in-layout split——嚴守 scope 不動 EditorArea（後續要 split 屬新需求）；(b) 400ms 輪詢 live doc 即時更新（實效列 T15）；(c) toggle 每 md 分頁可見、overlay 僅 active 分頁渲染（最小侵入）。
  - **controller 復驗（親跑）**：markdownPreview＋TabBar 測試 `16 passed (16)`；tsc exit=0。✅ 綠。

- 21:33 T6 回報收訖：transport／workspace（YuzoraWorkspace：requestFile 背景載入＋LRU 20＋updateFile＋displayFile；pathToUri/uriToPath）／lspExtensions（官方基礎＋placeholder）／lspManager（ensureClient lazy 單一、lspExtensionsForFile 非 full 回 null、shouldFormatOnSave/flushPendingChanges 純邏輯）／lspStore（五態）／LspBridge＋App.tsx 掛載；+5 測試檔（TDD red 在案）。
  - d.ts 核對偏差（記錄）：LSPClient 能力屬性＝`serverCapabilities`；`languageServerSupport` 在 6.2.5 標 @deprecated（仍為官方打包入口，保留＋module doc 註記）；save flush primitive＝`LSPClient.sync()`；WorkspaceFileUpdate 未匯出（local FileUpdate 等形替代）；Workspace 掛鉤名 requestFile/updateFile/displayFile/syncFiles 確認。
  - 作者自報已知限制（P4 審視標的）：(1) 背景寫回不送 didChange——server 對該檔持舊文字至下次 didOpen；(2) displayFile 開分頁後 view 首回合可能 null（T10/T15 收尾）；(3) silent respawn client 不知情（Rust scope 外，T15）。
  - manager key 用 NUL 分隔（含空白路徑安全）；workspace 路徑一致性已寫入 module doc（wave 1 移交事項落實）。
  - **controller 復驗（親跑）**：T6 目標測試 `25 passed (25)`；tsc exit=0。✅
- 21:35 **五件套整合（controller 親跑）**：vitest `375 passed (375)`（48 檔）；tsc 0；build ✓ 909ms；cargo `132 passed; 1 ignored`；FMT CLEAN。✅ 基線 340/132 → 375/132 只增不減。

**變更檔案**
- lead: package.json、bun.lock（4 依賴）
- T6: src/lsp/{transport,workspace,lspExtensions,lspManager}.ts＋{transport,lspManager,workspace}.test.ts、src/state/lspStore.ts＋lspStore.test.ts、src/workbench/LspBridge.tsx＋LspBridge.test.tsx、src/App.tsx（掛 LspBridge）
- T13: src/workbench/MarkdownPreview.tsx＋markdownPreview.test.tsx、src/workbench/TabBar.tsx＋TabBar.test.tsx

**計畫修正**
- 無（d.ts 偏差屬 plan 明文「以 d.ts 為準並記 report」路徑；T13 overlay portal 屬 plan「split 或 toggle 擇最小侵入」範圍內決定）。

## P4 對抗 review

**Reviewer 派工（ultracode 模式首輪，使用者裁決）**
- 21:37 Round 1＝Workflow `wave2-adversarial-review`（run wf_f05ae9c6-109）：4 視角 fresh opus reviewer 並行（lsp-correctness／async-state／xss-ui／scope-tests，agentType=reviewer, model=opus）→ (file:line) 去重 → 每 unique finding 派 2 個 opus 反駁者（repro 角度＋spec 角度）→ 回 confirmed/split/refuted 供 controller 裁決。回報待收。

**Round 1 結果（Workflow wf_f05ae9c6-109，21:37-22:04，32 agents／4 lens＋28 驗證）**
- raw 17 → unique 14 → **confirmed 11／split 2／refuted 1**（每條經 repro＋spec 兩角度反駁驗證）。
- Confirmed（controller 裁決全數為真，回派修復）：
  - W1[major] workspace.ts:102 requestFile check-then-act race——併發同 uri 雙 didOpen＋重複 entry＋LRU 灌水（library find-references Promise.all 無去重，觸發常見）。
  - W2[major] workspace.ts:118 背景檔硬編 version 0 繞過 nextFileVersion——升級後首個 didChange 重用 v0（非單調）。
  - W3[minor] transport.ts:28 info promise 無人消費——lsp_start reject 成 unhandled rejection。
  - W4[minor] MarkdownPreview.tsx:71 400ms 輪詢對降級檔（可達 50MB）仍全文 stringify。
  - W5[minor] TabBar.tsx:26 關分頁不清 preview 開關（與 W8 同根）。
  - W6[major] workspace.ts:147 被編輯過的背景檔升級為 editor 永不 resync server——永久 desync，module doc 宣稱的緩解不實。
  - W7[minor] transport.ts:32 `void lspSend` 吞 rejection——server 亡後每鍵一個 unhandledrejection。
  - W8[minor] TabBar.tsx:31 preview 開關無界累積＋靜默重開。
  - W9[major] lspManager.ts:68 LSPClient 未配 sanitizeHTML——server 提供的 hover/completion markdown 經 innerHTML 直插（XSS）。
  - W10[major] MarkdownPreview.tsx:98 preview 連結點擊整個 webview 導航離開——editor 狀態全失。
  - W11[minor] lspManager.ts:3 匯入未宣告 transitive 依賴 vscode-languageserver-protocol 型別。
- Split 裁決：
  - S1 LspBridge stale crash emit（minor）→ **裁為真**：機制實在（listener deps [] 跨切換存活、無 workspace 鑑別欄），實害於 wave 5 StatusBar/Settings 消費時成立——本 milestone 內必然到來。修法＝契約補全：Rust LspServerInfo 加 `workspace` 欄（emit 帶 raw key）＋TS 型別同步＋LspBridge 過濾非當前 workspace 事件。
  - S2 ensureClient connect 搶先 spawn（major）→ **裁為誤報**：repro 反駁者以 wry/tauri 呼叫鏈證明同步 command 於 main thread inline 依序執行、不可交錯。殘餘顧慮（Missing/Crashed 下 connect 仍發 initialize、錯誤被吞）併入 W3 修法（ensureClient await info、missing/crashed 不 connect）。
- Refuted 維持：背景寫回 mark-before-save 為 ExternalChangeResolver 既載 data-safety 不變量、且背景檔無 tab 使 handleExternalChange 必 skip——雙反駁成立。觀察留檔：背景寫回失敗僅 void 吞（best-effort 慣例），列 polish 備查。

**修復派工（22:05，三路並行，各回原 implementer）**
- Batch A → T4 implementer：LspServerInfo +workspace 欄位（Rust 全構造點＋serde 測試）。
- Batch B → T6 implementer：W1/W2/W3(+S2 殘餘)/W6/W7/W9/W11＋types.ts workspace 欄＋LspBridge 過濾（S1 前端半）。
- Batch C → T13 implementer：W4/W5/W8/W10（opener 套件在：@tauri-apps/plugin-opener@^2＋capability）。

**修復回報與復驗（22:05-22:25）**
- Batch A（T4）：LspServerInfo +`pub workspace: String`（:66，三構造點填 raw key、emit 自然繼承）＋serde 測試斷言。**controller 復驗**：cargo 132＋1、FMT CLEAN、grep :66 在案。✅
- Batch C（T13）：W4 grade!==full 不建 interval；W5+W8 onClose closePreview＋module 層 workspace subscribe reset；W10 onClick capture 攔截＋openUrl 外開＋FORBID_ATTR target。TDD red 4 failed → 21 passed。**controller 復驗**：目標測試 21/21。✅
- Batch B（T6）：W1 per-uri in-flight map＋await 後 re-check；W2 nextFileVersion seed；W3+S2 ensureClient 快取 in-flight Promise＋await info（missing/crashed 不 connect、dispose、setServerInfo、回 null 可重試）；W6 背景 updateFile 後送 full-document didChange（版本單調）＋module doc 修正；W7 send catch 收斂；W9 sanitizeHTML=DOMPurify；W11 本地最小 ServerCapabilities 型別；S1 types.ts workspace 欄＋LspBridge 事件時讀 store 過濾＋fixture 補欄；附帶斷 lspStore↔lspManager import 循環（lspLanguageOf 移 types.ts）。TDD red 在案。
- **controller 五件套整合（親跑，22:25）**：vitest `388 passed (388)`（48 檔）；tsc 0；build ✓ 1.02s；cargo `132 passed; 1 ignored`；FMT CLEAN。✅ 375→388 只增不減。

**Round 2 派工（22:27）**：Workflow `wave2-adversarial-review-r2`（run wf_67d170b4-1d2）——4 個全新 fix-aware lens（fix-correctness／async-state-r2／security-r2（含 DOMPurify this 綁定、auxclick、替代導航向量）／scope-tests-r2）＋雙反駁驗證。回報待收。

**Round 2 結果（wf_67d170b4-1d2，22:27-22:53，26 agents）**
- raw 15 → unique 11 → **confirmed 9／split 0／refuted 2**。
- Confirmed（裁決全真，回派修復；R2-3/R2-6 同根合併）：
  - R2-1[major→實害 minor（T10 前 latent）] workspace.ts:131 loadBackground 對 openFile rejection 無防護——requestFile reject 短路 library Promise.all，find-references 全滅＋unhandled rejection（DefaultWorkspace 永不 reject 的契約被 override 破壞）。修：try/catch 回 null（優雅降級）。
  - R2-2[minor] MarkdownPreview.tsx:86 暫態超長行使 grade→veryLongLine 停輪詢且無恢復路徑——preview 永久卡降級。修：kind 為 full 時維持輪詢（content-derived 降級只影響渲染不停輪詢）。
  - R2-3[minor] TabBar.tsx:155-159 split 兩 group 各渲 fixed overlay 完全重疊互蓋。修：僅 focused/active group 渲染（或等效最小方案）。
  - R2-4[major] lspManager.ts:87 in-flight cache clobber（A→B→A）：舊 startClient 失敗清理刪掉新 session 的 clients[key] → 已 connect client 洩漏＋重複 lspStart。修：清理前身分比對（clients.get(key)===本 pending 才 delete）。
  - R2-5[minor] lspManager.ts:85 startClient 直寫 setServerInfo 繞過 S1 過濾——A 的 missing 寫進已切到 B 的 store。修：寫入前過濾 info.workspace===當前 workspacePath（與 LspBridge 同準則）。
  - R2-7[minor] MarkdownPreview.tsx:21 DOMPurify 預設放行 form/input——表單提交導航 webview。修：FORBID_TAGS 補 form 系。
  - R2-8[minor] lspManager.ts:108 in-flight 期間 stopWorkspace 後 handles.set 重塞——handles map 洩漏 entry。修：set 前查 key 仍屬本 pending，否則 dispose 不塞（與 R2-4 同守衛）。
  - R2-9[minor] MarkdownPreview.tsx:101 void openUrl 無 catch。修：.catch 收斂。
- Refuted 裁決：
  - stopWorkspace orphan handle——反駁成立（disconnect 經 pending.then 必達、handlers 清空無留存）。接受。
  - **rename 漏改從未載入檔——接受「wave 2 誤報」（T6 契約止於背景文件；流程 T10 才可達），但機制屬實（library doRename 對 getFile null 直接 continue、全程不 requestFile）→ 記為 T10 硬性需求：rename/WorkspaceEdit 套用前預載全部目標 URI（requestFile）或以 renameCompat 層攔截，wave 4 brief 必載。**

**修復派工 Round 2（22:55，兩路並行）**
- T6 implementer：R2-1／R2-4／R2-5／R2-8。
- T13 implementer：R2-2／R2-3／R2-7／R2-9。

**Round 2 修復回報與復驗（22:55-23:15）**
- T13：R2-2 輪詢條件改 kind-derived（content-derived veryLongLine 只影響渲染，自動恢復）；R2-3 僅 activeGroupIndex（workspaceStore 既有概念）的 group 渲染 overlay；R2-7 FORBID_TAGS form 系；R2-9 openUrl .catch。TDD red 3 failed → 25 passed。**controller 復驗**：25/25。✅
- T6：R2-1 loadBackground try/catch 回 null（恢復 requestFile 永不 reject 契約＋finally 清 in-flight 可重試）；R2-4/R2-8 `isCurrent = () => clients.get(key)===pending` 身分守衛（失敗 delete 前查＋connect 前超越守衛——被取代的 startup 不建 client 不 connect）；R2-5 寫 store 前過濾 info.workspace===當前 workspacePath。TDD red 4 failed → 21 passed（deferred() 精準重現競態）。
- **controller 五件套整合（親跑，23:15）**：vitest `396 passed (396)`（48 檔）；tsc 0；build ✓ 899ms；cargo `132 passed; 1 ignored`；FMT CLEAN。✅ 388→396 只增不減。

**Round 3 派工（23:17）**：Workflow `wave2-adversarial-review-r3`（run wf_719e166d-d80）——4 個全新 lens：fix-surfaces-r3（isCurrent 時序／dispose 責任鏈／index 漂移／FORBID_TAGS 繞過）、holistic-r3（模組契約全景：stopWorkspace→Rust process 停止鏈、雙副檔名表漂移）、test-quality-r3（30 條新測試逐條驗非恆真＋mock drift＋連跑穩定性）、ux-conventions-r3（鍵盤可達性／雙主題／unlisten 清理）。0 confirmed 即 dry pass。回報待收。

**Round 3 結果（wf_719e166d-d80，23:17-23:40，20 agents）**
- raw 8 → unique 8 → **confirmed 6／split 1／refuted 1**（收斂趨勢：raw 17→15→8；confirmed 11→9→6；major 4→2→1）。
- 裁決（全真回修）：
  - R3-1[minor] MarkdownPreview.tsx:102 `<area href>`/usemap 逃逸導航攔截（closest("a") 對 AREA 回 null；DOMPurify 預設放行 map/area）。修：FORBID_TAGS += map/area（＋usemap 屬性）。
  - R3-2[minor] workspace.ts:143 veryLongLine 背景載入守衛無覆蓋測試（刪掉該行測試仍全綠）。修：補測試。
  - R3-3[minor] lspManager.ts:82 lspStart-rejection retry 分支無測試。修：補測試。
  - R3-4[minor] lspManager.ts:146 stopWorkspace 的 lspStopWorkspace ipc 呼叫從未被斷言。修：補測試。
  - R3-5[major] MarkdownPreview.tsx:135 模式切換 overlay 洩漏——AppShell 對 EditorPanel 僅 CSS hidden，portal 逃逸至 body 蓋住 Git/DB/SSH 模式。修：render gate 加 mode 檢查。
  - R3-6[minor] MarkdownPreview.tsx:140 fixed bottom-0 蓋住 StatusBar。修：bottom 偏移至 StatusBar 高度上緣（px）。
  - R3-7[minor→split 裁真] MarkdownPreview.tsx:74 getDocument 無 .catch——openFile reject 時 preview 卡「載入中」＋unhandled rejection。修：.catch 顯示錯誤/空態（比照 W7/R2-9 慣例）。
- Refuted 接受：Rust workspace 欄構造點斷言——struct 必填欄位由編譯強制，serde 測試已覆蓋輸出鍵。

**修復派工 Round 3（23:42，兩路並行）**：T13←R3-1/5/6/7；T6←R3-2/3/4（純測試補強）。

**Round 3 修復回報與復驗（23:42-00:00）**
- T13：R3-5 render gate 加 `mode === "files"`（uiStore 既有 mode）；R3-1 FORBID_TAGS +map/area＋FORBID_ATTR +usemap；R3-6 bottom-[30px]（StatusBar h-[30px] 實高）；R3-7 loadError state＋.catch＋錯誤態文案。TDD red 4 failed → 29 passed。**controller 復驗**：29/29。✅
- T6：R3-2/3/4 三條純測試補強（production 行為皆符預期，無 STOP）；R3-2 附**變異驗證**（刪 :143 守衛行→僅新測試轉紅→還原），證明覆蓋真實。21→24 測試。
- **controller 五件套整合（親跑，00:00）**：vitest `403 passed (403)`（48 檔）；tsc 0；build ✓ 1.13s；cargo `132 passed; 1 ignored`；FMT CLEAN。✅ 396→403 只增不減。

**Round 4 結果（wf_39b22237-8fd，00:02-00:25，18 agents）**
- raw 7 → unique 7 → **confirmed 5／split 1／refuted 1**（趨勢：raw 17→15→8→7；confirmed 11→9→6→5）。
- 裁決（全真回修）：
  - R4-1[major] MarkdownPreview.tsx:146 renderMarkdown＋fileGradeOf 逐 render 未 memo——**每鍵放大**（markDirty 每鍵重建 groups → TabBar re-render → 全文重解＋重 sanitize；5MB 檔實測 11s/jsdom）。修：useMemo（keyed content）＋React.memo；效能實機驗證仍歸 T15。（與 R4-4 同根合併）
  - R4-2[major] lspManager.ts:121 initialize 完成僅寫 capabilities，**從未呼叫 setInitialized**——deriveDisplayState 的 "ready" 永不可達，wave 5 MVP「Pyright Ready」不可能渲染；且 Rust status 無 Ready variant，事件路徑也補不了。修：initializing.then 內 setInitialized(language, true)（加 workspace 當前性守衛，比照 R2-5）；catch 路徑不設。
  - R4-3[minor] MarkdownPreview.tsx:103 輪詢每 400ms 對至多 10MB doc 全量 toString 即使內容未變。修：doc 參考同一性（CM6 doc immutable，identity 比較 O(1)）先查再 toString。
  - R4-5[minor] MarkdownPreview.tsx:92 result 每 path 只取一次——外部 reload 跨 10MB 邊界時降級守衛 stale。修：最小方案由 implementer 擇定（輪詢時同步刷新 registry 快取引用或等效）＋record。
  - R4-6[minor→split 裁真] lspManager.ts:146 lspStopWorkspace ipc 無 .catch（同 W7/R2-9/R3-7 慣例類）。修：.catch＋測試。
- Refuted 接受：R3-6 測試斷言 class 字面——class 即樣式契約，jsdom 無法測真實視覺耦合，T15 覆蓋。

**修復派工 Round 4（00:27，兩路並行）**：T13←R4-1+4/R4-3/R4-5；T6←R4-2/R4-6。

**Round 4 修復回報與復驗（00:27-00:50）**
- T13：R4-1 memo(元件)＋useMemo(html/grade)＋__renderMarkdownCallCount 行為斷言；R4-3 lastDocRef identity 跳過；R4-5 輪詢 tick 內 getDocument 重讀比對 kind（registry 無同步 peek/事件——最小方案；tooLarge→full 不自動恢復為記錄在案的次要限制）。TDD red 3 failed → 32 passed。**controller 復驗**：tsc 0、32/32（IDE 診斷 stale 再確認）。✅
- T6：R4-2 initializing.then 內 setInitialized(language, true)＋workspace 當前性守衛（真握手驅動測試：擷取 initialize request id 回帶 result）；R4-6 lspStopWorkspace .catch（確定性 catch-spy 斷言）。雙批變異驗證 red 在案。14→17 測試。
- **controller 五件套整合（親跑，00:50）**：vitest `409 passed (409)`（48 檔）；tsc 0；build ✓ 991ms；cargo `132 passed; 1 ignored`；FMT CLEAN。✅ 403→409 只增不減。

**Round 5 結果（wf_1c37d3b8-d46，00:52-01:10，7 agents）**
- raw 2 → unique 2 → **confirmed 1／split 0／refuted 1**（趨勢：raw 17→15→8→7→2）。
- R5-1[major→雙反駁者皆調 minor，controller 採 minor] MarkdownPreview.tsx:118——R4-5 新增的輪詢 getDocument().then() 缺 .catch（外部刪檔＋reload 清快取後每 tick unhandled rejection；R3-7 同類回歸；init 路徑有 catch、輪詢路徑漏）。→ 回派一行修＋測試。
- Refuted 接受：R4-1 測試「無法驗 memo 包裹層」——等價變異（useMemo 已獨立守住不重解行為需求）；反駁者親跑雙 mutant 證明測試對真回歸（同刪 memo＋useMemo）確實轉紅。
- 觀察記 backlog（不動）：ExternalChangeBridge.tsx:16 `void reloadDocument().then()` 同類缺 .catch——M2 既有碼非 wave 2 引入，記 M3 殘留清單。

**Round 5 修復回報與復驗**
- T13：R5-1 輪詢 getDocument().then() 補 `.catch(() => {})`（MarkdownPreview.tsx:118-133）＋確定性 reject 測試。**controller 復驗**：vitest `410 passed (410)`（48 檔）、tsc 0。✅ 409→410 只增。

**Round 6 結果（wf_3161cc06-528，01:21-01:37，11 agents，875k tokens）**
- 3 lens：fresh-eyes-lsp（2 findings）／fresh-eyes-preview（1）／gate-audit（0，附反證紀錄：stopWorkspace 與 isCurrent 守衛間無 await 故無競窗、等多條 holds）。
- raw 4 → unique 4 → **confirmed 2／split 2／refuted 0**（趨勢：raw 17→15→8→7→2→4；confirmed 11→9→6→5→1→2）。
- 裁決：
  - R6-1[major→雙反駁者皆調 minor，controller 採 minor，裁真] workspace.ts:231 背景寫回 `void saveFile(...)` 無 `.catch`——wave 2 唯一漏網的 fire-and-forget IPC（同 W7/R2-9/R3-7/R4-6/R5-1 慣例類；M2 先例亦修過同型）。→ 回派 T6 一行修＋reject 測試。附帶觀察：EditorPane.tsx:55 主存檔 `void saveFile().then()` 同型缺 .catch——M1 既有碼非 wave 2 引入，記 M3 殘留清單（比照 ExternalChangeBridge.tsx:16）。
  - R6-2[minor，裁真] lspManager.ts:118 成功路徑丟棄 LspServerInfo——setServerInfo 只在 missing/crashed 分支呼叫，Rust 只 emit Crashed，健康 server 的 servers[language] 永遠 undefined → displayFor().serverId 空白，T11「Python · Pyright Ready」會渲染成「Python ·  Ready」。R4-2 同型前例（健康路徑 store 填充屬 T6 producer 職責）。→ 回派 T6：成功路徑 setServerInfo(info)＋workspace 當前性守衛（比照 R4-2）＋測試。
  - R6-3[minor，split→controller 裁真] MarkdownPreview.tsx:153 html useMemo 以 `kind==="full"` 為閘（非 grade）——veryLongLine 檔仍對至多 10MB buffer 全量 renderMarkdown 後丟棄，每個 content-changing tick 重複。裁決理由：refute-spec 的核心論點（改 grade 閘會破壞 R2-2 已裁決設計）**事實錯誤**——poll 獨立以 kind 為閘，改 html 閘不影響輪詢；長行刪除後 grade 回 full 即重渲染，自動恢復完整保留，顯示行為零變化、純省 CPU。→ 回派 T13 一行修＋測試（veryLongLine 期間 renderMarkdown 不被呼叫）。
  - R6-4[split→controller 裁 wave 2 誤報；**記 T10 硬性需求 #2**] workspace.ts:190 closeFile 無 per-view refcount——split pane 同檔雙 view 時首關 pane 即 didClose＋untrack，倖存 pane 的 LSP 靜默死亡。wave 2 不可達（assembleLspExtensions 無 production caller）；且 viewRegistry 單 view 設計下光補 refcount 亦不足以支撐雙 pane。比照 Round 2 rename 預載前例：wave 4 brief 必載——**LSP plugin mount 政策：同 path 只掛一份 plugin，或 per-view refcount＋multi-view registry，二擇一在 T10 裁定**。

**修復派工 Round 6（兩路並行）**：T6←R6-1/R6-2；T13←R6-3。

**Round 6 修復回報與復驗**
- T6：R6-1 workspace.ts:231 補 `.catch(() => {})`＋catch-spy 測試；R6-2 成功路徑 setServerInfo(info)＋workspace 當前性守衛（`info.workspace === workspacePath`）＋兩測（成功入 store／pending 切走不寫）。TDD red 2 failed → green 在案。
- T13：R6-3 html useMemo 閘門 `kind==="full"` → `grade==="full"`（deps `[grade, content]`），輪詢閘未動（R2-2 恢復路徑保留）；測試斷言 veryLongLine 期間計數器不增＋長行移除後恢復渲染。TDD red（20→22 計數）→ green 在案。
- **controller 五件套復驗（親跑）**：vitest `414 passed (414)`（48 檔，410→414 只增）；tsc exit 0；build ✓ 897ms（exit 0）；cargo `132 passed; 0 failed; 1 ignored`；FMT CLEAN。✅

**Round 7 結果（wf_e8314173-44a，01:55-02:11，7 agents，686k tokens）**
- 3 lens：fix-audit-r6（0 findings，R6 三修變異審計通過）／fresh-eyes-crosslayer（1）／gate-audit（1）。
- raw 2 → unique 2 → **confirmed 1／split 0／refuted 1**（趨勢：raw 17→15→8→7→2→4→2；confirmed 11→9→6→5→1→2→1）。
- 裁決：
  - R7-1[minor，裁真] workspace.ts:130 uriToPath（decodeURIComponent）位於 loadBackground try（:137）之外——server 對未開檔回傳含裸 `%` 的未編碼 file URI（如 `100%done.md`）時 URIError 直接讓 requestFile reject；library find-references 以 Promise.all fan-out 且無 per-item catch → 整批 references 全滅＋unhandled rejection，正是 :131-135「requestFile must never reject」註解宣稱防住的結果。displayFile:240 同型可達；updateFile:229 反駁者證不可達（malformed URI 永不可能成 tracked file）。雙反駁者皆 node repro 證實、無法反駁；觸發窄（主流 server 皆 percent-encode）故 minor。→ 回派 T6：uriToPath 全函數化（decode 失敗 fallback 原始字串）＋測試。
  - R7-2[refuted 接受] run file P3 變更清單「漏」types.ts/ipc.test.ts——P3 清單為 21:35 phase snapshot；兩檔 mtime 22:12/22:15 屬 Batch A/S1 P4 修復窗，且 run file :119/:125/:131 已記錄三處（ipc.test.ts fixture 補欄為 compiler-enforced 連帶），非漏帳、無清單外檔案。

**修復派工 Round 7**：T6←R7-1。

**Round 7 修復回報與復驗**
- T6：R7-1 uriToPath 全函數化（decodeURIComponent 包 try/catch，失敗 fallback 去 scheme 原始 body；workspace.ts:20-30）＋兩測（裸 % URI → requestFile resolve null 降級；%E4%BD%A0 合法 decode 不變）。TDD red（URIError reject）→ green 在案。updateFile 同型路徑經證不可達，未動。
- **controller 復驗（親跑）**：vitest `416 passed (416)`（48 檔，414→416 只增）；tsc exit 0；cargo `132 passed; 1 ignored`；FMT CLEAN。✅

**Round 8 結果（wf_d6d47397-627，02:26-02:44，9 agents，749k tokens）**
- 3 lens：fix-audit-r7（2：R8-1/R8-2）／adversarial-inputs（1：R8-3）／gate-audit（0）。
- raw 3 → unique 3 → **confirmed 0／split 2／refuted 1**（趨勢：confirmed 11→9→6→5→1→2→1→0）。
- 裁決：
  - R8-1[major→minor，split→controller 裁真部分修] lspManager.ts:33-35 sanitizeHtml 用 DOMPurify 預設——保留 form/input/button/map/area/usemap，而同 webview 的 T13 preview 已 FORBID 同清單＋onClick 攔截；LSP tooltip（hover/completion/signature 的 innerHTML sink）render 同等不可信的 server markdown 卻無硬化。spec 反駁「wave 2 不可達（extension 無 production caller，R6-4 先例）」屬實，但 sanitizeHtml 是 wave 2 自身程式碼（W9），FORBID 對齊是一行修，現在修比帶需求到 T10 便宜且消除靜默缺口；嚴重度採 repro 反駁者 minor（威脅者本已是有完整本機執行權的 subprocess，導航逃逸屬 defense-in-depth）。→ 回派 T6：FORBID 清單與 MarkdownPreview.tsx:31-34 完全對齊＋測試。**tooltip anchor 點擊攔截記 T10 硬性需求 #3**（sanitizer 層做不到——T10 editor 掛載層需比照 MarkdownPreview capture onClick→openUrl 外開，或裁定 tooltip 禁 anchor）。
  - R8-2[minor，split→controller 裁真] workspace.test.ts:129-139 R7-1 測試只釘 totality 未釘 fallback 值——repro 反駁者親測變異 `return body`→`return ""` 全 suite 存活。本 run 變異驗證紀律一貫要求測試對真回歸轉紅。→ 回派 T6：補 `expect(openFile).toHaveBeenCalledWith("/ws/100%done.md")` 級斷言釘住 fallback。
  - R8-3[refuted 接受] lspStore crash→restart stale initialized=true 假 "ready"——雙反駁證不可達：mid-session crash 經 LspBridge 只寫 setServerInfo、不清 lspManager clients 快取，重開檔命中 cached client，`setServerInfo("starting")` 不會發生；initialized=true 與可刪快取來自互斥路徑。此即已備查之 silent-respawn 項（T6 module doc＋T15 驗）。
- gate-audit 0 findings：五件套親跑全綠、vitest 連跑兩次穩定、無殘留 debug 碼、依賴清單與 plan 授權相符。

**修復派工 Round 8**：T6←R8-1/R8-2。

**Round 8 修復回報與復驗**
- T6：R8-1 sanitizeHtml 加 FORBID_TAGS/FORBID_ATTR（與 MarkdownPreview.tsx:31-34 逐字相同，字面複製未抽共用）＋form/map/area/usemap/target 移除測試（合法 code/href 保留）；R8-2 R7-1(a) 補 `toHaveBeenCalledWith("/ws/100%done.md")` 釘 fallback 值＋變異驗證（fallback→"" 轉紅、還原綠）兩段輸出在案。TDD red→green 在案。
- **controller 復驗（親跑）**：vitest `417 passed (417)`（48 檔，416→417 只增）；tsc exit 0；cargo `132 passed; 1 ignored`；FMT CLEAN。✅

**Round 9 結果（wf_77db7556-e2c，02:57-03:15，9 agents，780k tokens）**
- 3 lens：fix-audit-r8（3：R9-1/R9-2/R9-3）／fresh-eyes-final（0）／gate-audit（0）。
- raw 3 → unique 3 → **confirmed 3／split 0／refuted 0**（趨勢：confirmed 11→9→6→5→1→2→1→0→3——R8-1 審計 lens 對 FORBID 清單追問「預設還放行什麼」而挖出全新向量）。
- 裁決（全真回修）：
  - R9-1[major，裁真] MarkdownPreview.tsx:31-34 FORBID_TAGS 漏 `style`——untrusted .md 含 `<style>#root{display:none}</style>` 經 sanitize 存活（實測 DOMPurify 3.4.11：style 跟在任何內容後即保留），經 dangerouslySetInnerHTML(:184)＋createPortal(document.body)(:224) 注入後 CSS **全域生效**（實測 getComputedStyle(#app).display==="none"；元件自身 MarkdownPreviewProse 正是靠此機制上樣式，反證無 scoping）；csp:null 無後盾。全 app 藏匿／defacement／clickjacking，開個 preview 即可達。反駁者五路反駁全敗。一位反駁者依 run 校準主張 minor（純視覺、關 preview 即回復）；controller 採 major——wave 2 即可達且損及全 app 完整性，worst case（連關閉鈕一起藏）需重啟。→ 回派 T13：FORBID_TAGS 補 "style" ＋測試。
  - R9-2[major→minor，裁真] lspManager.ts:39-41 tooltip sanitizer 同漏 `style`（W9 威脅模型明載的 compromised server 正是此向量）。wave 2 latent（tooltip 未接），但 R8-1 先例拘束：sanitizer 是 wave 2 自身程式碼，現在補比帶到 T10 便宜。→ 回派 T6：FORBID_TAGS 補 "style"（與 T13 保持逐字同步）＋測試。
  - R9-3[minor，裁真] 兩測試檔皆無 `dialog` 斷言——變異（FORBID 清單刪 dialog）全 suite 存活（反駁者實測 live mutant）。→ 兩路各補 dialog 移除斷言。
- fresh-eyes-final 與 gate-audit 皆 0 findings（附反證紀錄；plan Task 6/13 驗收條目逐條對照無漏交付）。

**修復派工 Round 9（兩路並行）**：T13←R9-1/R9-3(preview 側)；T6←R9-2/R9-3(lsp 側)。

**Round 9 修復回報與復驗**
- T13：R9-1 FORBID_TAGS 補 "style"（MarkdownPreview.tsx:35）＋style 移除測試（TDD red 在案）；R9-3 dialog 斷言＋變異驗證（刪 dialog 轉紅、還原綠）兩段輸出在案。
- T6：R9-2 FORBID_TAGS 補 "style"（lspManager.ts:41）＋style 移除測試（TDD red 在案）；R9-3 dialog 斷言＋變異驗證兩段輸出在案。
- **controller 復驗（親跑）**：兩份 FORBID 清單 grep 比對**逐字一致**（9 tags＋2 attrs）；vitest `418 passed (418)`（48 檔，417→418 只增）；tsc exit 0；cargo `132 passed; 1 ignored`；FMT CLEAN。✅

**Round 10 結果（wf_3ab46a49-761，03:26-03:45，9 agents，781k tokens）**
- 3 lens：dompurify-surface-exhaust（2：R10-1/R10-2）／fresh-eyes-nonsanitizer（1：R10-3）／gate-final（0 新，dup 到 R10-2）。
- raw 4 → unique 3 → **confirmed 2／split 1／refuted 0**。
- **決定性窮舉證據**：dompurify-surface-exhaust lens 對整個 DOMPurify 預設放行面逐一 repro（base/iframe/frame/object/embed/portal/link rel/meta refresh/a 協定 js:vbscript:data:blob:/ping/svg animate·foreignObject·use·image/input formaction/遠端媒體 poster·autoplay·srcset），**除 style 屬性外全部 hold**（預設已 strip 或屬既定放行的 img 遠端資源類）。fresh-eyes-nonsanitizer＋gate-final 對非 sanitizer 面（併發 A→B→A／stopWorkspace 洩 process／requestFile double-didOpen／LRU off-by-one／deriveDisplayState crashed>ready 排序／400ms poll storm／LspBridge stale-workspace 事件）做 5＋15 條反證嘗試全 hold。→ sanitizer 線至此挖乾。
- 裁決：
  - R10-2[major，裁真] MarkdownPreview.tsx:34 FORBID_ATTR 缺 `style` 屬性——R9 修的是 style **標籤**，這是同 root cause 的**屬性通道**姊妹缺口。惡意 .md 放 `<a href="https://evil" style="position:fixed;inset:0;z-index:2147483647;opacity:0">`：fixed+inset:0 覆蓋整個 app（實測 reviewer 排除 transform containing-block），透明變體配 onPreviewClick(:142-149) 把任一點擊變外開攻擊者 URL（clickjacking），不透明變體遮蔽偽裝編輯器；`background:url(remote)` render 即發 beacon（csp:null）。preview 已 wired 可達 → major。→ 回派 T13。
  - R10-1[major→minor，裁真] lspManager.ts:40 同缺口（tooltip latent）——依 R8-1/R9-2 先例（sanitizer wave 2 自身碼、現在補比帶 T10 便宜、威脅者本有完整本機權故 defense-in-depth）採 minor。→ 回派 T6，FORBID_ATTR 與 T13 逐字同步。
  - R10-3[minor，split→controller 裁 wave 2 誤報，記 T10 硬性需求 #4] workspace.ts:189 openFile 背景檔提升未 reconcile resident stale doc/version vs mounting editor view——背景窗內磁碟變更會使 editor 種入的 fresh 內容與 workspace resident doc 不一致。裁決：與 R6-4 同構（openFile 僅 library LSPPlugin ctor 呼叫，wave 2 無 production caller，T10 才 wire），一位反駁者證不可達；歸 T10 mount 政策一併處理（版本/內容 reconcile）。

**修復派工 Round 10（兩路並行）**：T13←R10-2；T6←R10-1。

**Round 10 修復回報與復驗**
- T13：R10-2 FORBID_ATTR 補 "style"（MarkdownPreview.tsx:35）＋style 屬性移除測試（合法內容不受影響斷言）。TDD red→green 在案。
- T6：R10-1 FORBID_ATTR 補 "style"（lspManager.ts:39）＋style 屬性移除測試。TDD red→green 在案。
- **controller 復驗（親跑）**：兩份清單 `uniq -c` 各出現 2 次且完全相同——FORBID_ATTR=[target,usemap,style]、FORBID_TAGS=[form,input,button,select,textarea,dialog,map,area,style] **逐字一致**；vitest `419 passed (419)`（48 檔，418→419 只增）；tsc exit 0；build ✓ 979ms；cargo `132 passed; 1 ignored`；FMT CLEAN。✅

**Round 11 結果（wf_beb48ef3-e1f，03:58-04:15，7 agents，626k tokens）**
- 3 lens：sanitizer-final-confirm（1：R11-1）／fresh-eyes-total（1：R11-2）／gate-final（0 新）。
- raw 2 → unique 2 → **confirmed 1／split 1／refuted 0**。dryPass=false。
- **反省**：Round 10「sanitizer 線挖乾」宣稱過早——那輪窮舉的是 DOMPurify **標籤/協定放行面**，漏了 `class` 屬性配合**專案自身全域 Tailwind bundle** 這條「應用層」通道。教訓：不可信 HTML 注入 body portal + 全域 CSS 框架，黑名單逐屬性補是在跟 CSS 賽跑；根因是 CSS 定位模型允許 position:fixed 逃逸容器。
- 裁決：
  - R11-1[major，裁真] MarkdownPreview.tsx:35 FORBID_ATTR 缺 `class`——DOMPurify 放行 class，`<div class="fixed inset-0 z-50 opacity-0">` 用已編譯的 Tailwind utility 重建 R10 封的全螢幕 overlay（雙反駁者親跑 `bun run build`＋grep dist：.fixed/.inset-0/.z-50/.opacity-0 全在 bundle；preview portal 祖先鏈無 transform/filter/contain 故 fixed 以 viewport 為準逃逸）。preview 已 wired 可達 → major。孿生 lspManager.ts:40 同缺口（tooltip latent，minor）。**修法定為雙管**：(a) 兩 sanitizer FORBID_ATTR 補 "class"（可 vitest 驗；MarkdownPreviewProse 全用標籤選擇器、md 無高亮，故移除 class 對顯示零副作用——已讀碼確認）；(b) 根因斷絕：`.markdown-preview-body` 建立 containing block（contain:paint 或 transform），使任何 position:fixed 子元素困在 preview 內容區、無法覆蓋 editor——一次中和 style/class/未來任何 CSS 定位通道（此層實機效果歸 T15 gui-acceptance）。→ T13（a+b）／T6（a 孿生同步）。
  - R11-2[minor，split→controller 裁真] TabBar.tsx:101/111 preview toggle 對每個 md tab 渲染且 aria-pressed 讀 per-path，但 panel render（:157-161）額外 gate 在 activePath——非 active tab 按 toggle 顯示 pressed/accent 態卻無 panel（jsdom repro 證實）。→ T13：消除 aria-pressed 說謊（依現有 store API 擇最小方案：點非 active tab 眼睛先 activate 再開 preview／或 toggle 與 panel gate 對齊），回報說明選項。
- gate-final 0 新 findings：五件套親跑全綠、連跑穩定、plan Task 6/13 驗收條目逐條盤點無漏交付。

**修復派工 Round 11**：T13←R11-1(a+b)/R11-2；T6←R11-1(a 孿生)。

**Round 11 修復回報與復驗**
- T13：R11-1(a) FORBID_ATTR 補 "class"（MarkdownPreview.tsx）＋class 移除測試（合法內容渲染斷言）；R11-1(b) MarkdownPreviewProse 補 `.markdown-preview-body{contain:paint}` 建立 containing block＋註解（退路 transform:translateZ(0)）；R11-2 採「toggle onClick 先 setActiveTab(groupIndex, tab.path) 再 togglePreview」——被點 tab 成 active，aria-pressed 與可見 panel 天然對齊（沿用既有 setActiveTab API，最小侵入，符合「點眼睛即聚焦」直覺）＋一致性測試。TDD red（class 存活／aria-pressed true 無 panel）→ green 在案。
- T6：R11-1(a 孿生) FORBID_ATTR 補 "class"（lspManager.ts）＋class 移除測試。TDD red→green 在案。
- **controller 復驗（親跑）**：兩份清單 `uniq -c` 各 2 且相同——FORBID_ATTR=[target,usemap,style,class]、FORBID_TAGS=[form,input,button,select,textarea,dialog,map,area,style] **逐字一致**；contain:paint 在 MarkdownPreview.tsx:244；vitest `421 passed (421)`（48 檔，419→421 只增）；tsc exit 0；build ✓ 865ms；cargo `132 passed; 1 ignored`；FMT CLEAN。✅
- **T15 實機驗收項（新增）**：R11-1(b) containing block 阻斷 position:fixed 逃逸＋prose 顯示/捲動無副作用——jsdom 測不到 layout，須 gui-acceptance 真 webview 驗。

**Round 12 結果（wf_45d51382-e82，部分完成）**
- 3 lens 只完成 2：**`containing-block-attack` 因 session limit（reset 4am Asia/Taipei）失敗未跑**——這是本輪最關鍵 lens（對抗驗證 contain:paint 根因防禦）。workflow 回報 dryPass:true 僅代表「已完成 agents 中 0 findings」，**不足以宣稱 dry pass**（證據先於宣稱：關鍵 lens 未完成）。
- 完成的 2 lens 皆 0 findings、反證全 hold：
  - toggle-activate-sideeffects（11 條 hold）：R11-2 setActiveTab 用該 TabBar 自身 groupIndex（不切錯 group／不竊焦點）；已 active tab toggle 僅一次多餘 re-render 無後果；切 tab/切 workspace/關 tab 的 openPaths per-path 持久＋panel gate 綁 activePath 清理正確；stopPropagation 與 setActiveTab 無競態；split 僅 active group 渲染單 panel（TabBar.test.tsx:178 斷言長度 1）。
  - gate-final（15 條 hold）：byte-diff 兩 sanitizer FORBID 清單逐字一致；R11 class-removal／toggle-consistency 測試經變異證明對真回歸轉紅；順帶重掃 deriveDisplayState（Rust 僅 emit Crashed）／requestFile（uriToPath total）／LRU（evictIfNeeded 精確 trim）全 hold；plan Task 6/13 驗收無漏交付。
- **待補**：session 額度恢復後補跑 containing-block-attack lens（Round 12b）才能確認 dry pass。
- **Round 12b 補跑 blocker（記錄）**：單一 opus reviewer 補跑 containing-block-attack lens 連續 3 次因基礎設施失敗——(1) workflow 內 session limit（reset 4am Asia/Taipei）；(2) 單 agent connection closed mid-response（剛啟動即斷）；(3) resume 後 stall 600s 無進度。三者皆非任務問題，屬 opus 額度/串流退化。依反亂槍打鳥規則暫停硬重試，靠每小時 :07 cron 心跳＋4am 額度 reset 自動重試。基線五件套於此期間 controller 親驗維持全綠（vitest 421/421、tsc 0、build ✓、cargo 132+1、fmt clean）。
- **spec 事實補記（controller，非取代 review）**：contain:paint 依 CSS Containment Module L2，paint containment 使該元素成為其 absolutely/fixed-positioned 後代的 containing block——故 spec 層面 R11-1(b) 對「fixed 逃逸」的斷絕成立；實機 layout 效果仍歸 T15 gui-acceptance。此僅為佐證，dry pass 仍待 fresh opus reviewer 完成 containing-block-attack lens 對抗嘗試。

**Round 12b 補跑結果（單一 fresh opus reviewer af559321，第 4 次派工成功）**
- containing-block-attack lens：**NO NEW FINDING**。6 條反證全 hold：(1) 無 style/class 元素無法取得非 static 定位（全域 CSS grep 無任何 position:fixed|sticky|absolute 規則、無 tag/id/data-*/name/srcset 定位選擇器）；(2) 即使假設某規則賦 fixed，注入點皆 .markdown-preview-body 後代、被 contain:paint 的 body 捕獲為 containing block（CSS Containment L2）；(3) root-cause 宣稱成立（效果掛 app 自有容器、與注入屬性無關，未來即使 style/class 重開仍捕獲）；(4) 無 prose regression（paint containment 非 size containment，不改寬度/img max-width/捲動；pre overflow-x 為獨立 scroll container）；(5) 殘留屬性 id/data-*/name/srcset 無對應全域定位選擇器、DOM-clobbering 由 DOMPurify 預設 SANITIZE_DOM 處理；(6) 非渲染分支（error/downgrade/empty/loading）皆純文字無 dangerouslySetInnerHTML。
- 結論：contain:paint **能**斷 position:fixed 逃逸（spec 層），WebKit 引擎實作正確性歸 T15 gui-acceptance（非新缺口）。
- 附註：lspManager FORBID 與 MarkdownPreview 逐字一致再確認；lspManager tooltip 無 contain:paint 屬 T10 surface。

**Dry pass**：✅ **達成**（Round 12 完整三 lens：toggle-sideeffects 0／gate-final 0／containing-block-attack 0；累計 12 輪對抗 review 修 47 項，confirmed 趨勢 11→9→6→5→1→2→1→0→3→2→1→0/0）。

## P5 驗收

Controller 親跑五件套終驗（Round 12b 完成後）：
- **E1**（依賴後綠）：達成於 P3 lead 步驟（vitest 340／tsc 0／build ✓）。✅
- **E2**（T6 測試綠）：transport 轉發／lazy 單一 client／大檔守衛 null／五態推導／workspace 四測皆在 12 輪修復中強化並綠（併入全量 421）。✅
- **E3**（T13 測試綠＋TabBar 入口）：渲染結構／sanitize（script+style tag+style attr+class attr 全移除）／大檔降級／TabBar toggle 一致性皆綠。✅
- **E4**（五件套終跑，標準 ≥340／0／✓／≥132+1／CLEAN）：**vitest 421 passed (421)（48 檔）／tsc exit 0／build ✓ 1.05s／cargo 132 passed；1 ignored／FMT CLEAN**。基線 340/132 → 421/132 只增不減。✅
- **E5**（P4 dry pass 紀錄）：Round 12 完整三 lens 0 未被推翻 finding。✅

**Gate G5**：✅ 全部 E 有證據且 PASS。

殘留（非阻塞，交付後追蹤）：
- **T10 硬性需求**（wave 4 brief 必載）：#1 rename/WorkspaceEdit 套用前預載全部目標 URI；#2 split-pane per-view refcount／LSP plugin mount 政策（同 path 單 plugin 或 multi-view registry）；#3 tooltip anchor 點擊攔截（sanitizer 層做不到，editor 掛載層攔）；#4 背景檔提升 stale doc/version reconcile。
- **T15 gui-acceptance 實機驗收項**：markdown preview 即時更新/效能；contain:paint 在 WKWebView 阻斷 fixed 逃逸＋prose 無 layout 副作用；silent respawn 前端重握手；tooLarge→full 不自動恢復。
- **M1/M2 backlog**：ExternalChangeBridge.tsx:16 與 EditorPane.tsx:55 的 `void ...()` 缺 .catch（既有碼，非 wave 2 引入）。

## P6 收尾

- 狀態：**DONE**。
- 最終變更檔案：lead（package.json、bun.lock）；T6（src/lsp/{transport,workspace,lspExtensions,lspManager}.ts＋3 test、src/state/lspStore.ts＋test、src/workbench/LspBridge.tsx＋test、src/App.tsx、src/lib/types.ts）；T13（src/workbench/MarkdownPreview.tsx＋test、src/workbench/TabBar.tsx＋test）；Rust（src-tauri/src/lsp_service.rs LspServerInfo.workspace 欄）。
- 證據摘要：12 輪對抗 review（ultracode workflow fan-out＋雙反駁驗證，全 opus）修 47 項；五件套終驗 421/421・tsc 0・build ✓・cargo 132+1・fmt clean；全部 TDD red→green＋關鍵修復變異驗證在案。
- 需使用者確認的假設：A1 sanitizer=DOMPurify（A4 允許擇一）；A2 T6/T13 並行零交集。
- 下接：**wave 3**（T7 codeActions／T8 semanticTokens／T9 diagnosticsPull＋renameCompat，三並行、獨立新檔、不動 lspExtensions.ts 由 T10 wire）。

**自我改進三問**
- 哪個閘門沒攔到問題？→ P4 前 10 輪 sanitizer 面反覆冒新 CSS 注入通道（style tag→style attr→class attr），因早期 lens 只窮舉 DOMPurify 標籤/協定放行面、未含「應用層 class 配全域 Tailwind bundle」。教訓 [tighten]：不可信 HTML 注入有全域 CSS 框架的 portal 時，注入面窮舉須含 class/id 等應用層屬性，且優先做 CSS 定位模型根因防禦（containing block）而非逐屬性黑名單。→ 追加 LEARNINGS。
- 哪條規則浪費時間？→ 無建議放寬。
- 學到的陷阱：opus 額度/串流退化時派工連續失敗（session limit／connection closed／stall），單 agent 比 workflow 易過（輕量、可 SendMessage 續推）；lens brief 加「優先讀碼推理、repro 單發短指令」可避 stall。→ 追加 LEARNINGS [trap]。

## P5 驗收

- （待填）

## P6 收尾

- （待填）
