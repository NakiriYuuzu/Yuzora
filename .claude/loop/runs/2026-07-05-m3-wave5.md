# LOOP RUN — m3-wave5

- 日期：2026-07-05
- 狀態：DONE
- 任務原文：
  > （沿使用者指示「繼續進行下一個 wave，直到 m3 完結」；三約束：opus subagent／git 唯讀／無 worktree；2026-07-04 改令：P4 不用 ultracode Workflow，改純 opus subagent）
- **跨 run 指令備忘**：M3 全部完成後最終 review 派 **fable** subagent（使用者 2026-07-03 指示）。

## P1 提問

**需求（可驗證陳述）**
- R1 (T12 LSP section)：SettingsDialog `SECTIONS` +lsp 項＋`LspSection()`（仿 GitSection :164 模式）——四語言卡（active server profile 切換 `lspConfigSetServer(workspace|null, lang, id)`／resolved path／Ready-Missing＋install_hint／last startup log／last error）；guided install＋一鍵安裝按鈕（`lspInstallServer` wrapper mock 測試、listen `lsp:install-progress` 顯示 phase＋進度；npm 系無 npm→disabled＋「需先安裝 Node.js」、pylsp 無 python3→disabled＋「需先安裝 Python」；失敗顯錯＋manual hint。handler wave 6 T14 落地，本 task UI＋wrapper 串接）；stale entry 列表＋清除（`lspConfigClearStale`）／重綁；JSON-RPC trace toggle 預設 off（`lspSetTrace`，不持久化）＋顯示 trace 檔路徑；format-on-save 開關預設 OFF（localStorage，consume EditorPane 的 `FORMAT_ON_SAVE_STORAGE_KEY` export）。plan :651-687。
- R2 (T12 prop)：SettingsDialog +initialSection/target prop（section 現為內部 state :289，需可外部指定 initial section＋language）。
- R3 (T12 API)：`openSettings(section?, language?)` 全域入口——**uiStore action＋AppShell 訂閱**（或等效最小、擇一記 report；現況 settingsOpen 為 AppShell 區域 state :47、StatusBar :247 無 props）。API 合約測試：`openSettings("lsp","python")` 開 dialog 顯示 LSP section 對應語言卡。
- R4 (T11 StatusBar)：右側 placeholder（:125 `{languageFromPath} · Syntax only`）→ 真實態：讀 `useLspStore.displayFor(activePath, grade)` 顯示 `{Lang} · {serverDisplay} {態}`（Ready／Failed tooltip lastError／Missing／Syntax only（大檔/fallback）／Starting）；未開啟檔→`未開啟檔案`；非四語言→`{Lang} · Syntax only`；**Missing/Failed 點擊 → openSettings("lsp", language)**（消費 R3 API；本 task 只動 StatusBar.tsx＋statusBar.test.tsx）。plan :622-648。
- R5：五件套不退化：vitest ≥492 全綠**無 Errors 行**、tsc 0、build ✓、cargo 132+1、fmt clean。
- R6：P4 dry pass（純 opus subagent fresh reviewer，loop-until-dry）。

**已查資料（本 session 親核）**
- plan T11（:622-648）/T12（:651-687）全讀；序列裁定 T12 先 T11 後（:104 相依表）。
- 現況：uiStore.ts＋test 存在；AppShell.tsx:47 settingsOpen 區域 state、:137 handleOpenSettings、:177/:253 props 消費、:247 `<StatusBar />` 無 props；SettingsDialog SECTIONS :29、GitSection :164、section state :289、section render :494；StatusBar.tsx:125 placeholder。
- T5 wrappers 就緒（ipc.ts :192-214）：lspConfigSetServer/lspConfigStale/lspConfigClearStale/lspSetTrace/lspInstallServer（其餘 lsp* wrapper 由 implementer 全查）。
- EditorPane.tsx:24 `FORMAT_ON_SAVE_STORAGE_KEY` export（wave 4 T10 留給 T12）。
- lspStore.ts:33/:51 `displayFor(path, grade)`＋deriveDisplayState 就緒。
- A7 裁決：format-on-save 預設 OFF、開關放 Settings LSP 區；A8：trace 獨立檔、重啟恢復 off；A9/A9'：七個 curated 皆一鍵安裝。
- wave 4 殘留接手：tabSize:4 寫死屬 T12 範疇外（plan 未列 format 細節設定；記殘留不擴 scope）。

**假設（非 BLOCKING）**
- A1：**序列派工**（T12 完成親驗後才派 T11）——T11 production import uiStore.openSettings，並行會使 T11 的 tsc 檢查阻塞；T11 極小（單元件右段）。
- A2：openSettings 採 uiStore action 方案（plan 建議項）；SettingsDialog prop 形狀由 implementer 依既有 props 慣例定、記 report。
- A3：per-language profile 清單等資料以**現有** wrapper/store 可得者為準；缺欄位不得自加 Rust handler（wave 6 T14 才動 Rust）→ 停下回報。
- A4：trace 檔路徑若現有 API 取不到 → 顯示說明文字並記 report（不發明 API）。

**BLOCKING 待問**：無。

**Gate G1**：✅

## P2 規劃

**Baseline battery**（wave 4 P5 終驗＝本 run baseline，controller 親跑 2026-07-05 01:3x，之後零程式變更）：
```
vitest：57 files / 492 passed（無 Errors 行）
tsc --noEmit：EXIT=0
build：✓ built in 1.03s
cargo test：132 passed / 1 ignored
cargo fmt --check：EXIT=0
```

**步驟**
1. 派 T12 opus implementer（TDD red→green）→ verify：controller 親跑其測試＋tsc。
2. T12 綠後派 T11 opus implementer（消費 openSettings API）→ verify 同上。
3. controller 五件套整合 → 全綠無 Errors 不退化。
4. P4 純 opus fresh reviewer fan-out（lens 分工）→ 裁決 → 回派修 → loop-until-dry。
5. P5 終驗＋P6 收尾（LEDGER，下接 wave 6）。

**預計變更檔案**
- T12：src/app/workbench/SettingsDialog.tsx、src/app/AppShell.tsx、src/state/uiStore.ts＋uiStore.test.ts、Settings 相關測試檔（既有或新建 settingsDialog 系測試）。
- T11：src/app/workbench/StatusBar.tsx、src/app/workbench/statusBar.test.tsx。

**不碰範圍（non-goals）**
- 不動 src-tauri/**（T14 wave 6）、不動 EditorPane/lspExtensions/lspManager/symbols/SymbolPicker（wave 4 定稿）、不動 ipc.ts/types.ts（wrapper 已齊）、git 唯讀。

**風險**
- SettingsDialog 為 M1 大檔（500 行級），LSP section 併入可能撞既有 section 測試／守門：既有 settings 測試不退化＋新測隔離。
- openSettings 全域 state 與 AppShell 區域 state 併存的雙源問題／守門：uiStore 為單一真相、AppShell 訂閱改造後 handleOpenSettings 收斂、reviewer 專驗。
- install-progress listener 洩漏／守門：unlisten cleanup 測試。

**驗收證據清單（實作前固定，只可加嚴）**
- E1：T12 測試綠（四語言卡／profile 切換呼叫 lspConfigSetServer／stale 清除／format-on-save toggle localStorage／openSettings("lsp","python") API 合約／trace toggle 呼叫 lspSetTrace／一鍵安裝呼叫 lspInstallServer＋無 npm disabled）。
- E2：T11 測試綠（Ready 顯示 server＋Ready／Missing 點擊觸發 openSettings spy／大檔 grade→Syntax only／非四語言無 server 名／未開啟檔案）。
- E3：五件套終跑（vitest ≥492 無 Errors／tsc 0／build ✓／cargo 132+1／fmt clean）。
- E4：P4 dry pass 紀錄（每 finding 有裁決）。

**Gate G2**：✅

## P3 執行

**T12 回報＋controller 親驗 ✅（02:0x）**
- 變更檔：uiStore.ts（settingsOpen/settingsSection/settingsLanguage＋openSettings/setSettingsOpen，settings 單一真相）＋uiStore.test（+3）；SettingsDialog.tsx（SECTIONS +lsp、LspSection＋LspLanguageCard、initialSection/initialLanguage props＋open/target 同步 effect）；AppShell.tsx（settingsOpen 改訂閱 uiStore、handleOpenSettings→openSettings()、傳 initial props）；settingsDialog.lsp.test.tsx（新，9 測）。
- TDD red（11 失敗）→green。controller 親跑：兩檔 **14/14**、全量 **504/504 無 Errors**（492→504 只增）、tsc 0。
- 選型（controller 核可）：openSettings 寬鬆 string＋SettingsDialog 內部驗證；prop 非法值忽略保留記憶；重綁＝lspConfigGet mapping→逐項 SetServer→ClearStale（無 workspace 退清除 only）；trace state 在 section 內（dialog 關閉即 reset，較 spec 嚴，接受）；format-on-save import FORMAT_ON_SAVE_STORAGE_KEY。
- **資料缺口裁決**：
  - ①per-language profile 清單無前端 API → implementer 正確停下。**裁決：curated 7 為使用者 A9 裁決固定的靜態資料，允許前端靜態表**（附 lsp_adapters.rs source-of-truth 註解）→ follow-up 補 profile 切換 UI（T12 驗收標準含 profile 切換，不可缺）。
  - ②trace 檔路徑無 getter → 接受說明文字 fallback；記殘留（getter 屬後續）。
  - ③npm/python 偵測無 API → 接受安裝失敗訊息 fallback（brief 明列）；pre-disable 記殘留 → wave 6 T14（偵測隨 install handler 落地）。
  - ④resolve-only probe 無 API →「重新偵測」限已啟動者；接受，T14 install 後 emit server-status 補償。
- **計畫外發現裁決**：Editor pane 既有 placeholder（假 Format on save 預設 true＋Language servers 假清單）與新 LSP section 重複且誤導——implementer 因 panels.test.tsx:90（白名單外）正確還原。**裁決：follow-up 移除 placeholder＋更新 panels.test**（真控制項已在 LSP section）。localStorage polyfill 歸 setup.ts＝三處重複記殘留（consolidation）。i18n inline 沿 GitSection 慣例＝接受。

**Follow-up（T12b）派 w5-t12-settings**：①profile 切換 UI（靜態 curated 表＋lspConfigSetServer，workspace vs 全域二選）；②移除 Editor pane placeholder＋panels.test 更新。
**T11 並行派工**（檔案零交集：StatusBar/statusBar.test vs SettingsDialog/panels.test）。

**T12b 回報＋controller 親驗 ✅（02:2x）**
- `LSP_PROFILES` 靜態表（id 逐一核對 lsp_adapters.rs::all()：vtsls/typescript-language-server、pyright/pylsp、rust-analyzer、marksman/markdown-oxide；註解指向 source of truth＋A9）；section 級 scope toggle（此工作區/全域，無 workspace 時強制 global）；每卡 radiogroup（rust 單一 disabled）；active 高亮 workspace=覆寫值（未設不高亮＝繼承）、global=defaults 回退 adapter default。
- placeholder 移除＋隨之 orphan 的 formatOnSave state／LANGUAGE_SERVERS 常數自清；panels.test :90 改斷言「語言伺服器列不再於 Editor pane」＋Editor 仍有 minimap toggle（一測換一測淨 0）。
- TDD red（3 失敗）→green。controller 親跑：兩檔 **26/26**、全量 **507/507 無 Errors**（504→507 只增）、tsc 0。缺口①正式關閉。
- T12＋T12b 完成；等 T11 回報。

**T11 回報＋controller 親驗 ✅（02:4x）**
- StatusBar.tsx 右段改 displayFor 驅動五態（Ready 綠/Failed 紅＋title stderr/Missing 琥珀/Starting/Syntax only 皆 status token）；Missing/Failed 綁 openSettings("lsp", language)；statusBar.test 17 測（Ready/Missing 點擊/Failed title/大檔/非LSP/未開啟；git 段原封）。
- 選型（controller 核可）：grade 走 getDocument 快取命中＋useState（active 檔已開零 IPC；未動 lspStore fallback）；非四語言跳過讀取；grade 未載入樂觀 "full"（暖快取不可見過渡）；servers+initialized slice 訂閱＋useMemo 防 getSnapshot 迴圈；serverId 原樣顯示（目錄名映射另案，接受）。
- TDD red（3 失敗）→green。controller 親跑：statusBar 17/17、全量 **511/511 無 Errors**、tsc 0。

### 整合 battery（controller 親跑，02:4x，T12/T12b/T11 全落地）
- vitest：**58 files / 511 passed 無 Errors**（baseline 492→511 只增）
- tsc：**0**／build：**✓ 975ms**／cargo：**132+1**／fmt：**0**
→ P3 完成，進 P4。

## P4 對抗 review

**Round 1（2 fresh opus reviewer 並行，02:5x 派出）**
- Lens A（w5r1-settings）：SettingsDialog LSP section／uiStore 單一真相改造回歸／install-progress listener 生命週期／scope-radio 語意／重綁部分失敗／async reject 窮舉（歷史教訓重點）／placeholder 移除乾淨度／LSP_PROFILES id 逐一核對 lsp_adapters.rs。
- Lens B（w5r1-statusbar）：displayFor 五態映射核對／grade async 競態（activePath 切換 stale resolve、getDocument reject）／slice+useMemo reactivity／點擊鏈端到端（language 字串兩端比對）／a11y／測試 mutation。
- 等回報 → controller 逐條裁決 → 回派修 → 全新 reviewer loop-until-dry。

**Lens A 回報（6 findings＋11 反證 hold；LSP_PROFILES id 逐一核對吻合）— controller 親驗裁決（03:0x）**
- **A-F1〔major〕install 終態事件 race 卡死安裝鈕 — 確認修**。親驗：phase enum 含 done/error（types.ts:90）、listener（:570-572）無條件寫入、finally（:617）清除——終態事件晚於 finally 即永久 disabled＋真錯誤區被 `!installing` 吃掉。修：listener 對 done/error 設 null（終態＝清除、與 finally 冪等），其他 phase 才寫入。
- **A-F2〔major〕dialog 開著＋手動切換後，same-target openSettings no-op — 確認修**。親驗：openSettings 寫相同 primitive→zustand Object.is 短路→props 不變→effect 不 re-fire（:813-818 註解只對「關閉後重開」成立）。修：uiStore 加 `settingsNonce`（每次 openSettings ++），AppShell 傳 prop，sync effect deps 加 nonce。
- **A-F3〔major〕lspSetTrace 無 catch＋樂觀 setTrace；unlisten 鏈無 catch — 確認修**。親驗 :670-673/:573-575。修：toggleTrace catch→revert trace state；unlisten `.then(fn=>fn()).catch(()=>{})`。
- **A-F4〔minor〕trace UI 於 LspSection remount 後與 Rust desync — 確認修**：trace state 移入 uiStore（in-memory、不持久化——app 重啟自然 off、remount 存活），toggle 讀寫 store。
- **A-F5〔minor〕targetLanguage 高亮於手動重入殘留 — 確認修**：手動 nav 點擊時清 targetLanguage（或消費後一次性清除，擇最小）。
- **A-F6〔minor/test-gap〕listener cleanup 與 initialLanguage 高亮無 mutation 覆蓋 — 補測**：unmount 後 listeners 清空斷言；initialLanguage 高亮（accent/scroll 目標）斷言。
- 反證 hold ×11（id 核對、scope null、重綁部分失敗續跑、format key 同源、無參保留記憶、非法 section、並行安裝 keyed、orphan 清理乾淨、target 殘留重開路徑不存在等）。
- 全部落在 w5-t12-settings 檔域；等 Lens B 到齊一批派。

**Lens B（w5r1-statusbar）基礎設施死亡（03:1x）**：idle_notification `failed: You've hit your session limit · resets 4:10am`，review 內容未送達。依 LEARNINGS（連續基礎設施失敗停硬重試）未即時重派；額度 reset 後 10:3x 補派**全新** fresh reviewer w5r1b-statusbar（同 Lens B brief；附「A-F2 same-target openSettings 已裁決」排除條款避免重複回報）。程式碼自 02:4x battery 後零變更（Lens A 修復刻意 hold），Round 1 兩 lens 審同一 snapshot 成立。

**Lens B（w5r1b-statusbar）回報（6 findings＋nit＋7 反證 hold；NUL 檢查清白；自跑 17/17＋tsc 0）— controller 親驗裁決（10:4x）**
- **B-F1〔test-gap〕Starting 態零斷言 — 確認補測**。親驗：17 測無一渲染 Starting（Ready 測 initialized:true、大檔測被 grade 擋）；lspStore.ts:24-25 與 StatusBar label/color 的 starting 分支 mutation 存活。修：補一測 servers:{python: starting}+initialized:{} → `/Python · Pyright Starting/`、非 button。
- **B-F2〔latent-bug〕stopped fallthrough → 死 server 顯示綠 Ready — 確認修（display 半），lifecycle 半記殘留**。親驗全鏈：types.ts:67 有 `{status:"stopped"}`；deriveDisplayState（lspStore.ts:19-25）無 stopped 分支→落 `initialized?"ready"`；Rust `lsp_config_set_server`（lsp_service.rs:916）確實 stop_matching→Stopped 且**不 emit**（:610/:630 註解明示）；前端 setProfile（SettingsDialog.tsx:595-603）不重啟；redetect（:626-628）會把 Stopped info 寫入 store；`setInitialized(lang,false)` 全 codebase 不存在（僅 lspManager.ts:155 設 true、reset 全清）→ 「profile 切換＋重新偵測」即重現 stopped+initialized=true→Ready。修（display）：deriveDisplayState 加明確分支 `stopped → "syntaxOnly"`（誠實呈現當下能力：無活 LSP；維持 plan 五態 vocabulary，不加第六態）＋lspStore.test 補該分支。**殘留（lifecycle，記 backlog/T14）**：set_server stop 不 emit（Rust 註解「frontend restarts」未兌現）、initialized 無 per-language reset、profile 切換不重掛 editor client→生效時機＝下次 mount；屬 T14 restart/install 故事，wave 5 不動 src-tauri。
- **B-F3〔test-gap〕Missing title(installHint) 無斷言 — 確認補測**。親驗：missing 測（statusBar.test.tsx:91-108）設了 installHint 從未斷言 title；StatusBar.tsx:165-167 分支 mutation 存活（對照 Failed 測 :126 有斷言）。修：補 `expect(btn.getAttribute("title")).toContain("npm i -g pyright")`。
- **B-F4〔minor〕冷快取 1-frame 樂觀 "full" 閃現 — hold（接受的設計）**。T11 選型時已核可（暖快取不可見；冷快取自我修正），程式碼有註解。記 T15 實機觀察項，不修。
- **B-F5〔minor〕getDocument reject → 樂觀 full 而非降級 — 確認修**。親驗 StatusBar.tsx:82-84 catch→setGrade(null)→memo 當 "full"：workspace 切換 clearAll 後殘 tab 指向已刪路徑→openFile reject→對打不開的檔顯示完整 LSP 態。修：catch 改設非 full 之 FileGrade（如 "limited"＋註解「讀取失敗→以 syntax-only 呈現」；deriveDisplayState 對任何非 full 皆 syntaxOnly）；loading 仍為 null=樂觀 full。implementer 若有型別更誠實等價方案可改採（C2 慣例）。
- **B-F6〔latent-bug〕同路徑 reload 後 grade 陳舊 — 確認修**。親驗：effect deps 僅 [activePath]（StatusBar.tsx:88）；reloadDocument（ExternalChangeBridge/Resolver）同路徑 drop+refetch 可變 grade（外部改出 20k 長行→veryLongLine）但 effect 不重跑；EditorArea:68 以 documentGeneration keyed remount、StatusBar 沒有。修：render 時讀 `documentGeneration(activePath)` 入 effect deps——reload 流程必經 workspaceStore 變更（markExternallyModified）觸發 re-render，render-time 讀數即新 generation；註解記明此耦合。補測：mock documentRegistry 加 documentGeneration，reload 情境 grade 翻新。
- **B-nit cursor-pointer — 順手修**：Failed/Missing button className 加 `cursor-pointer`。
- 反證 hold ×7（stale-overwrite 有 disposed 閉包擋、unmount cleanup、lastError 空值、missing-title narrowing、無 getSnapshot 迴圈、測試隔離、非 LSP/未開檔分支）；檢點 3（reactivity）/檢點 4（vocabulary 四語逐一比對含 .js→typescript）宣告乾淨。
- **Round 1 合計**：Lens A 6（全修）＋Lens B 5 修 1 hold＋nit。派修分工（檔域零交集）：A-F1..A-F6 → w5-t12-settings（SettingsDialog/uiStore/AppShell）；B-F1/B-F2a/B-F3/B-F5/B-F6/nit → w5-t11-statusbar（StatusBar/statusBar.test/lspStore+test）。

**B 批修復回報（w5-t11-statusbar，10:5x）— 待 controller 重驗（A 批落地後一次整合驗）**
- 六項全落地，TDD red→green（red：stopped→syntaxOnly／reject 降級／generation 翻新 3 失敗；B-F1/B-F3 屬補覆蓋加測即綠）。變更僅白名單四檔。
- B-F5 選型：catch→setGrade("limited")（enum-proxy＋註解），評估過獨立 sentinel 因零行為差選簡；符合 C2 慣例授權。
- B-F6 耦合親查：ExternalChangeBridge:14-16／ExternalChangeResolver:209-213 皆 reloadDocument（bump generation）＋markExternallyModified（動 groups）配對 → StatusBar 訂閱 groups 必重渲染。mock factory 補 documentGeneration。
- 自證：兩檔 29/29 無 act warning、tsc 0。
- 其回報全量 vitest 有 6 失敗全在 settingsDialog.lsp.test.tsx——A 批（w5-t12-settings）in-flight 中間態（TDD 紅測已寫、store/props 實作未落地；IDE diagnostics 同步顯示 settingsNonce/traceEnabled/openNonce 未存在），非 B 批影響（其單獨跑該檔同樣 6 敗、SettingsDialog 有自己的 status 映射不用 deriveDisplayState）。等 A 批回報後一次整合驗。

**A 批修復回報（w5-t12-settings，11:0x）**：A-F1..A-F6 全落地，TDD red 8 失敗（A-F3 red 時伴 unhandled rejection＝正是 bug）→green；A-F6① 修前即 pass（既有 cleanup 正確，coverage 補測）；A-F5 以 selectSection（setSection＋清 targetLanguage）實作；A-F6② 用卡片 `data-highlighted` 穩定屬性。變更五檔皆檔域內。自證：兩檔 26/26、tsc 0、全量 524。

### R1 修復後整合 battery（controller 親跑，11:0x）
- vitest 全量：**58 files / 524 passed 無 Errors 行**（511→524 只增：statusBar +3、lspStore +1、uiStore +2、settingsDialog +7）
- tsc：**EXIT=0**／build：**✓ 988ms EXIT=0**／cargo：**132 passed / 1 ignored**／fmt：**EXIT=0**
- NUL 掃描（全 src *.ts/tsx，perl raw）：**乾淨**
- controller spot-read 五核心修復點皆確認落地：deriveDisplayState stopped→syntaxOnly（含註解）；uiStore settingsNonce ++／traceEnabled；install listener 終態→null＋unlisten .then().catch；toggleTrace async 樂觀＋catch revert（讀 store prev）；StatusBar documentGeneration render-read＋deps [activePath, generation]。
→ 12 修全落地，進 Round 2（全新 reviewers）。

**Round 2（2 全新 opus reviewer 並行，11:1x 派出）**
- w5r2-settings：驗證 A-F1..A-F6 落地（終態/finally 全交錯、LspSection unmount 期間安裝、A-F3 快速連點 revert race、A-F5 誤清初始高亮順序、A-F6 mutation）＋全新掃描（sync effect 四 deps 交互、scope 切換寫錯 scope、redetect/rebind 錯誤路徑、a11y、i18n）。
- w5r2-statusbar：驗證 B 批落地（stopped 分支位置與 Settings 端映射語意一致性、"limited" enum-proxy 下游消費者、B-F6 耦合反例——不經 workspaceStore 的 generation bump 路徑、新測 mutation 與中間態假綠）＋全新掃描。
- 兩者皆附已裁決排除清單＋NUL 檢查。等回報 → 裁決 → dry 判定或續派修。

**w5r2-settings 回報（4 findings＋13 反證 hold；NUL 通過；A-F1..A-F6 全判落地正確）— controller 裁決（11:2x）**
- 外部事實（reviewer 查證）：lib.rs 未註冊 lsp_install_server、全 repo 零 `lsp:install-progress` emit——A-F1 listener 為 T14 前瞻碼，race 修復對未來事件正確但契約無 producer 可驗。
- **R2A-F1〔minor/latent〕toggleTrace 樂觀 revert 缺 generation guard — 確認修（controller 已親讀 :686-694 核實三擊情境）**：click1(true) late reject 的 revert 可蓋掉 click3(true) 的最新意圖 → UI=off/backend=on desync，且 trace 已入 store 不隨 remount 自癒。修：request generation（ref 計數器，僅最新請求可 revert）。A-F3 修復引入的新 race，本輪收斂。
- **R2A-F2〔minor/前瞻〕A-F1 自癒依賴「終態事件必最後發」契約 — hold，記 T14 brief**：T14 落地時鎖「install 每路徑必以 done/error 收尾」契約測試。
- **R2A-F3〔nit/前瞻〕終態 error 事件 message 被丟 — hold，記 T14 brief**：T14 契約規定「emit error 必伴 command Err」（handleInstall catch 已顯示 String(e)），listener 不加前瞻碼。
- **R2A-F4〔nit〕LspSection unmount 期間 in-flight install 進度遺失＋重入 double-install 風險 — hold，記 T14 brief**：install handler 需 per-language in-flight guard（重複呼叫 no-op/回同進度）；前端 section-local 進度接受（部分 by-design）。
- 派修：R2A-F1 → w5-t12-settings（generation guard＋測試）。T14 brief 三契約（終態收尾／error 伴 Err／in-flight guard）記入 wave 6 待辦。

**w5r2-statusbar 回報（1 finding＋11 反證 hold；NUL 通過；B 批 5/6 判落地正確、B-F6 有反例）— controller 親驗裁決（11:3x）**
- **R2B-F1〔minor/latent〕B-F6「reload 必伴 markExternallyModified」耦合在被 reload 檔已刪除時破裂 — 確認修（controller 親讀三處核實）**：documentRegistry.ts:39-43 reloadDocument 同步 bump generation 後 getDocument→openFile 對已刪檔 reject；ExternalChangeBridge.tsx:16 與 ExternalChangeResolver.tsx:208-216（takeDiskReload）皆 `.then`-only——reject 時 (a) markExternallyModified 跳過→workspaceStore 不變→StatusBar 不重渲染→殘留 stale Ready（B-F5/B-F6 修復皆未啟動）；(b) **unhandled rejection ×2**（LEARNINGS reject 源窮舉教訓再現）；(c) Resolver 對話框卡住不關（markDirty/close 全跳過）。reviewer 誠實限定：非 regression（舊碼同樣 stale）、下次任意 re-render 自癒；但 unhandled rejection 與 resolver 卡死為實質缺陷。修：兩 caller 的 reload 鏈必 settle——Bridge 成敗皆 markExternallyModified(path,false) 且鏈以 catch 收尾；Resolver takeDiskReload reject 分支必 settle（closeResolver＋清 externallyModified＋log；dirty 依「buffer 與磁碟不一致則保留」原則，上下文有更佳等價可改採並回報）；StatusBar.tsx:70-73 耦合註解改述「由 Bridge/Resolver settle 保證」；補測（reject 時 markExternallyModified 仍被呼叫／無 unhandled rejection／resolver 關閉）。
- 反證 hold ×11（stopped 兩端語意互補非矛盾、limited 無下游消費者、reject 測非假綠（missing 非 ready）、displayFor memo、immutability、mock 隔離、token 五鍵齊、cleanup race、generation null 邊界、openSettings 不傳空字串、B-F5 retry 卡 limited 與 R2B-F1 同源）。
- Not reviewed 記錄：Rust watcher 對純刪除是否 emit 未讀原碼（rename/replace 同破口佐證觸發面存在）；設計 §6 色票指派屬實機驗收。
- **R2 dry 判定：非 dry**——2 個確認修（R2A-F1、R2B-F1）。派修分工（檔域零交集）：R2A-F1 → w5-t12-settings（SettingsDialog）；R2B-F1 → w5-t11-statusbar（白名單擴充：ExternalChangeBridge/ExternalChangeResolver/StatusBar 註解＋測試）。修完 Round 3 全新 reviewers。

**R2A-F1 修復回報（w5-t12-settings，11:4x）— 待 controller 整合驗**：traceGenRef（useRef 單調遞增），catch 僅 `gen === traceGenRef.current` 才 revert。**實作者正確攔截 brief 測試設計缺陷**（「連續兩次 toggle」交替語意下 prev1===click2 目標值→pre-fix 也綠、無鑑別力；改採 brief 自身三擊反例為 repro）——stop-and-report 協定再次運作。TDD red（`expected true, got false`）→green；自證兩檔 27/27、tsc 0、全量 525。變更僅二檔。

**R2B-F1 修復回報（w5-t11-statusbar，11:3x）**：Bridge `.then(settle).catch(settle)`（成敗皆 markExternallyModified(path,false)＋註解）；Resolver takeDiskReload 加 `.catch`（close-and-settle：清 external＋log `reload-missing`＋closeResolver，**保留 dirty**——buffer 與磁碟不一致，可再存檔重建）；StatusBar 耦合註解改述 settle 保證。新建 externalChangeBridge.test.tsx（2 測）＋externalChangeResolver.test +1（reject-mid-flight settle）。TDD red 2 失敗→green 32/32。語意抉擇記錄：close-and-settle 優於 re-degrade("deleted")（更簡且結果正確）；unhandledRejection guard 因 TS2591（無 @types/node）改行為斷言（僅 settle 會在 reject 路徑呼叫 markExternallyModified，vitest clean run 為輔證）——合理等價。

### R2 修復後整合 battery（controller 親跑，11:3x）
- vitest 全量：**59 files / 528 passed 無 Errors 行**（524→528 只增：settingsDialog +1、bridge +2、resolver +1）
- tsc：**EXIT=0**／build：**✓ 942ms**／cargo：**132 passed / 1 ignored**／fmt：**EXIT=0**／NUL 掃描：**乾淨**
- controller spot-read 三修復點皆吻合裁決（Bridge settle 雙路＋trailing catch；Resolver catch 分支 close-and-settle＋dirty 保留；StatusBar 註解條件化→settle 保證）。
→ R2 兩修落地，進 Round 3（全新 reviewer，dry-pass 候選輪）。

**Round 3（1 全新 opus reviewer w5r3-final，11:4x 派出）**：驗證 R2A-F1（generation guard 交錯枚舉＋反向錯誤——最新請求 reject 仍須 revert＋mutation）與 R2B-F1（settle 對已關 tab 的 markExternallyModified no-op 疑點——追 workspaceStore 實作、catch 吞非 ENOENT 錯誤的語意、closeResolver unmount 交互、新測 mutation）＋兩修檔案回歸快掃。零新 finding 即 dry pass 進 P5。

**w5r3-final 回報（1 finding major＋大量反證 hold）— controller 親驗裁決（11:5x）：非 dry**
- R2A-F1 判定落地正確（兩測夾住 guard 兩側：單擊 reject pin「最新必 revert」、三擊 pin「stale 不得 clobber」；拔 guard／反相 guard 皆紅）。
- R2B-F1 判定有問題 → **R3-F1〔major〕reload-missing settle 觸發 keyed remount 抹掉未存 buffer，「re-save 重建」承諾落空**。controller 親驗五宣稱全成立：workspaceStore.markExternallyModified（:96-104）無條件 map 重建 groups reference（path 不存在也翻）→ EditorArea:68 `key=path:documentGeneration` remount → 舊 pane cleanup `updateBuffer(path, buffer, mount 時捕捉的 gen)`（EditorPane:139/:223）被 documentRegistry:25 gen guard no-op → buffer 沒寫回；新 pane getDocument reject（EditorPane:152 無 catch＝wave4 F1 殘留）→ 空白 pane＋unhandled rejection。dirty=true 但無 view 可存。
- **controller 根因判定（比 reviewer 深一層）**：eager bump（documentRegistry.ts:40-41 同步 delete＋bump 才 fetch）本身即 time bomb——bump 後 fetch 失敗，任何 re-render（git poll／LSP 事件）都會引爆同一鏈；R2B-F1 settle 只是把引爆提前到「立即」。reviewer 方案 (a)（不清 externallyModified 僅 closeResolver）僅拆近端引信、時 bomb 仍在，不採。
- **裁決修法（根因）**：reloadDocument 改為 fetch 成功後才 bump（`registry.delete → await getDocument → generations.set(+1) → return`）。效果枚舉（controller 推演）：失敗→gen 不變→settle 的 re-render 不 remount→pane 存活 buffer 完好 dirty 保留→Cmd-S saveFile(view content) 重建（catch 已清 externallyModified，maybeInterceptSave 不攔）✅；成功→await resume 與 set 同 microtask 原子（無 updateBuffer 插入窗口），且消掉「fetch 期間他源 re-render 提早 remount 進 registry miss」的舊競態，嚴格更好✅；Bridge clean-delete→不 remount，pane 續活（優於空白 pane）、StatusBar gen 不變 grade 不重推導顯示 Ready（pane 真活著且 LSP 掛 buffer，語意可接受）✅。
- 隨修：StatusBar:66-76 註解末句（deleted → Syntax only）隨新語意改述；documentRegistry.test pin 兩行為（成功 bump +1／reject gen 不變）；resolver reject 測補 gen 不變斷言。EditorPane:152 無 catch 維持 F1 殘留不擴 scope。
- 派修 → w5-t11-statusbar（白名單：documentRegistry.ts＋test、StatusBar.tsx 註解、externalChange 兩測試檔、statusBar.test 如需）。修完 Round 4 全新 reviewer。

**R3-F1 修復回報（w5-t11-statusbar，12:0x）＋controller 整合驗**：
- reloadDocument bump-after-success 落地（controller spot-read 確認：註解精確涵蓋「僅真有新內容才 remount／await-resume 原子／消 fetch 期間 remount 舊競態」）；StatusBar 註解隨新語意改述正確。
- 測試：documentRegistry.test pin 成功 gen+1／失敗 gen 不變（TDD red 3 失敗——eager bump 下 reject 也 +1——→green）；**Bridge 測 mock 策略改真 reloadDocument＋mock ipc.openFile**（原全 mock 使 gen 斷言空洞——正確抉擇，gen 斷言成為真實證據）；resolver reject 測補 gen 不變斷言。既有 registry 測試（:37/:60）僅驗端態兩序皆過、只改 prose 註解；statusBar.test 未動（成功路徑語意不變）。
- 附帶效益記錄：reload-path 的 EditorPane:152 reject 觸發面消失（失敗不再 remount）；:152 對其他 reject 源仍缺 catch，F1 殘留維持。
- **整合 battery（controller 親跑，12:0x）**：vitest 全量 **59 files / 530 passed 無 Errors**（528→530 只增）；tsc 0；build ✓ 1.00s；cargo 132+1；fmt 0；NUL 乾淨。
→ 進 Round 4（全新 reviewer，dry-pass 候選輪之二）。

**Round 4（1 全新 opus reviewer w5r4-dry，12:0x 派出）**：驗證 R3-F1 根因修——bump-after-success 原子性反例、registry 長期空缺期間各讀者行為、getDocument 無 dedup 是否被新時序加劇、generation 消費者逐一核（EditorArea remount 時序等價性／EditorPane 捕捉 gen 語意／clearAll「generations 保留不清」workspace 重疊情境是否被破壞／StatusBar 註解一致性）、新測 mutation 鑑別力＋gen 全域 Map 測試間洩漏（絕對 vs 相對斷言）。零新 finding 即 dry pass 進 P5。

**w5r4-dry 回報（12:1x）：零新 finding，dry pass**。根因修判定落地正確且**結構性正確非時序僥倖**（key 與 gen 同源 → 「該 remount 時 updateBuffer 必 no-op／不該 remount 時 buffer 必存活」為不變式；clobber window 內 unmount 觸發條件與 gen-guard 拒絕條件完全耦合，結構上不可達）。4 個 generation 消費者逐一 holds（EditorArea remount 時序等價——settle 的 then/catch 在 bump 後才跑；EditorPane 兩向 pin；clearAll workspace 重疊走 null-activePath 卸載與本修正交；StatusBar 註解逐句屬實）；mutation 兩向被抓（bump 前移→3 測紅、拔 bump→2 測紅）；singleton 洩漏因相對斷言穩健；mock 邊界正確；R2 settle 語意無退化。附帶觀察（非 finding、pre-existing）：documentRegistry.ts:34 clearAll 註解措辭不精確（remount 靠 unmount 非 gen 遞增）——記殘留 nit。
**P4 收斂：R1（12 修）→R2（2 修）→R3（1 major 根因修）→R4 dry。Gate G4 ✅**

## P5 驗收

**最終五件套（controller 親跑，12:18，dry pass 後零程式變更）**：
```
vitest：59 files / 530 passed（無 Errors 行）    ← baseline 492 → 511（P3）→ 530（P4），只增
tsc --noEmit：EXIT=0
build：✓ built in 1.06s
cargo test：132 passed / 1 ignored
cargo fmt --check：EXIT=0
NUL 掃描（全 src ts/tsx）：乾淨
```
**驗收證據對照（P2 固定清單）**：E1 ✅（settingsDialog.lsp 27 測＋uiStore 7 測：四語言卡／profile 切換／stale 清除／format-on-save／openSettings 合約／trace／install＋終態 race／nonce／高亮）；E2 ✅（statusBar 20 測：五態全覆蓋含 Starting／Missing 點擊＋title／Failed title／大檔／非LSP／未開檔／reject 降級／generation 翻新）；E3 ✅（上表）；E4 ✅（P4 四輪每 finding 皆有裁決紀錄）。
**jsdom 測不到項 → T15 實機清單**：Missing/Failed 點擊開 Settings 對應卡高亮＋捲動、install 進度條視覺、trace toggle 實效、冷快取 1-frame 閃現、五態 status token 色票、resolver reload-missing 實機流。
**Gate G5 ✅**

## P6 收尾

- **本 wave 落地**：T12（Settings LSP section：四語言卡＋profile 切換＋scope toggle＋stale 清除/重綁＋install UI＋trace＋format-on-save＋openSettings 全域 API）＋T12b（LSP_PROFILES 靜態表＋placeholder 移除）＋T11（StatusBar displayFor 五態＋點擊鏈）＋P4 四輪 15 修（含 R3-F1 根因修：reloadDocument bump-after-success 保全未存 buffer）。
- **殘留（前端，記 backlog）**：EditorPane:152 getDocument 無 catch（F1，維持）；失敗 reload 後 StatusBar 顯示 Ready＋Bridge clean-delete pane 續活顯示舊內容（裁決接受的語意）；documentRegistry.ts:34 clearAll 註解措辭（nit）；trace 檔路徑 getter；serverId 顯示名映射；localStorage polyfill 歸 setup.ts；palette i18n 整併。
- **殘留（→ wave 6 T14 brief 必含）**：①install 事件契約——每路徑必以 done/error 收尾（契約測試）；②emit error 必伴 command Err；③install handler per-language in-flight guard（防 double-install）；④npm/python 偵測（pre-disable）；⑤lifecycle——set_server stop 不 emit／initialized 無 per-language reset／profile 切換不重掛 client（「frontend restarts」註解未兌現）；⑥T14 落地後 install 成功 emit server-status 補償 redetect。
- **LEDGER**：wave5 → DONE。**LEARNINGS**：+3（eager bump time bomb／修根因不修引信／mock 邊界）。
- **下接 wave 6**：T14 七 server 一鍵安裝（src-tauri lsp_download.rs＋第 10 個 handler＋emit lsp:install-progress）＋T15 checklist/fixtures/closeout（gui-acceptance 實機清單已累積於各 wave）。**M3 完結後最終 review 派 fable subagent（使用者指令，跨 run 備忘）。**

- 狀態：**DONE**（2026-07-05 12:2x）
