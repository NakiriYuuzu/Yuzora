# LOOP RUN — m3-wave0

- 日期：2026-07-03
- 狀態：DONE
- 任務原文：
  > 開始執行 m3
  > 1. subagent opus only
  > 2. 不使用 git add, commit
  > 3. 不使用 worktree

## P1 提問

**需求（可驗證陳述）**
- R1: lib.rs 預註冊完成——4 行 `pub mod`（lsp_adapters/lsp_config/lsp_service/lsp_download）＋4 個空 stub .rs，`cargo check` 通過（plan 明文由 lead 親做，使用者已核准）。
- R2: T1 M2 清債——m1–m7 逐條修復且各有對應測試；代表 Minor（askpass Drop unlink、BranchPopover notice 重置＋placeholder i18n、SettingsDialog onBlur clamp＋role=group、T15 陳舊選取、T18 revealLine 下界 clamp＋250ms 舊結果殘留）完成；linked worktree 明確 defer；其餘 ~30 條 Minor 逐條過帳（已修/續 defer＋理由）。
- R3: T2 lsp_adapters.rs——curated registry（四語言七 adapter＋language_for_path），plan Interfaces 形狀一致，查表測試通過。
- R4: T3 lsp_config.rs——~/.yuzora/lsp.json load/save/resolve/stale/clear，plan Interfaces 形狀一致，壞檔不 panic，round-trip 測試通過。
- R5: 五件套全綠且基線不退化：vitest ≥172（29 檔）、tsc 0、build ✓、cargo ≥51＋1 ignored、fmt 乾淨；新增測試只增不減。
- R6: P4 對抗 review dry pass（全新 opus reviewer）。

**已查資料**
- LEDGER：m3-lsp-kickoff／m3-plan-adjudication／m3-session-prompts 全 DONE；plan 定稿、A1–A9＋A9' 不得重議；尚無 m3 實作 run → 本 run 從 wave 0 起。
- prompts 檔（docs/superpowers/plans/2026-07-03-yuzora-m3-session-prompts.md）：本 session＝Prompt A（wave 0 首發）；lib.rs 預註冊由 controller 親做；五件套指令與基線數字在案。
- M3 plan（docs/superpowers/plans/2026-07-03-yuzora-m3-lsp.md）：Global Constraints／File Structure／相依表／T1（:109-155）／T2（:159-201）／T3（:205-243）已讀。
- LEARNINGS 已讀：/clear 斷點風險（長輪派工前讓中間結果落盤）、px-only、fixtures/out 限制、run file 當下建立。

**假設（非 BLOCKING，已選定預設值）**
- A1: T1/T2/T3 並行派工（依據：plan 相依表「三者原始碼零交集併行」＋Prompt A「可並行派工」；cargo/bun 有檔案鎖，同目錄並行驗證安全）。
- A2: 使用者三約束＝鐵律 v1.1 原樣（opus only／git 唯讀／無 worktree），無額外偏離（依據：字面一致）。

**BLOCKING 待問**：無（Prompt A 即使用者既定指示；wave 0 範圍由定稿 plan 固定）。

**Gate G1**：✅

## P2 規劃

**Baseline battery**（實際輸出尾段；含 `git status --short` 快照，唯讀；2026-07-03 16:18 實跑）

```
git status --short：D .gitignore ＋全數 untracked（單 commit repo，與 kickoff 一致）
bun run test  → Test Files 40 passed (40) / Tests 305 passed (305)
bunx tsc --noEmit → exit=0
bun run build → ✓ built in 711ms（chunk >500kB warning 既有）
cargo test    → test result: ok. 78 passed; 0 failed; 1 ignored（unit）＋ doc-tests 0
cargo fmt --check → ❌ RED：git_log.rs 多處 diff（M2 遺留）
```

**Baseline 事實修正（相對 plan 記載）**
- 實測基線 vitest 305/305（40 檔）、cargo 78＋1 ignored——高於 plan 所載 172/51（plan 數字過時）；本 run「不可退化」以實測 305/78 起算。
- `cargo fmt --check` baseline 即紅（git_log.rs，M2 遺留）→ 計畫修正 #1：併入 T1 brief（跑 `cargo fmt` 清 git_log.rs），屬 M2 清債範疇、機械性零風險。

**步驟**
1. Baseline 五件套＋git status 快照 → verify: 上列指令 → 預期: vitest 172/172、tsc 0、build ✓、cargo 51/51＋1 ignored、fmt 乾淨。
2. lead 親做 lib.rs 預註冊（4 mod＋4 空 stub）→ verify: `cargo check --manifest-path src-tauri/Cargo.toml` → 預期: 0 error。
3. 並行派工 opus implementer：T1（M2 清債）、T2（lsp_adapters）、T3（lsp_config），brief 含白名單／non-goals／鐵律轉述／verify → verify: 各 subagent 回報後 controller 親跑對應測試。
4. controller 親跑五件套整合驗證 → 預期: 全綠、基線不退化。
5. P4：全新 opus reviewer 對抗 review（rubric 全項）→ 裁決 → 有 finding 回派修＋整輪重跑 → 預期: dry pass。
6. P5 驗收：五件套終跑＋E 逐項銷帳。
7. P6 收尾：run file／LEDGER／回覆（含「下個 session 從 wave 1 接」）。

**預計變更檔案**
- src-tauri/src/lib.rs（4 mod 行，lead）＋ lsp_adapters.rs／lsp_config.rs／lsp_service.rs／lsp_download.rs（stub→T2/T3 實作其二）
- T1：src-tauri/src/search_service.rs、askpass.rs；src/state/gitStore.ts、src/editor/viewRegistry.ts、src/editor/EditorPane.tsx、src/workbench/ExternalChangeResolver.tsx、src/workbench/search/SearchResults.tsx、src/app/workbench/FilesNavContent.tsx、src/app/workbench/GitNavContent.tsx、src/workbench/git/BranchPopover.tsx、src/app/workbench/SettingsDialog.tsx＋對應測試檔
- 記帳：本 run file、LEDGER

**不碰範圍（non-goals）**
- 不動 T4+ 範圍（lsp_service/lsp_download 僅空 stub）、不裝任何依賴（wave 0 無新依賴）、不動 spikes/**、docs/design/**、不執行任何 git 寫入、不用 worktree。

**風險**
- 可能弄壞：EditorPane（m4 呼叫端＋revealLine clamp）與搜尋/git 面板既有行為 ／ 守門測試：既有 vitest 172 全量重跑＋baseline 比對。
- lib.rs stub 與 T2/T3 實作銜接：stub 為空檔，T2/T3 整檔覆寫 ／ 守門：cargo check 於預註冊後與 task 後各跑一次。

**驗收證據清單（實作前固定，只可加嚴）**
- E1: lib.rs 預註冊後 `cargo check` 輸出（0 error）。
- E2: T1 report——m1–m7＋代表 Minor 逐條過帳＋linked worktree defer＋其餘 Minor 清單；對應新測試在全量輸出中可見。
- E3: T2/T3 cargo test 輸出（lsp_adapters／lsp_config 測試通過）。
- E4: 五件套終跑輸出尾段（vitest ≥172／tsc 0／build ✓／cargo ≥51＋1 ignored／fmt 乾淨）。
- E5: P4 dry pass 紀錄（反證 ≥3、findings 全裁決）。

**Gate G2**：✅（baseline 已貼；fmt 紅燈已記為計畫修正 #1）

## P3 執行

**Wave 0 預註冊（lead 親做，plan 明文使用者核准）**
- 建 4 空 stub：src-tauri/src/{lsp_adapters,lsp_config,lsp_download,lsp_service}.rs（0 bytes）✅
- lib.rs +4 行 `pub mod`（字母序插於 logging 後）✅
- verify: `cargo check --manifest-path src-tauri/Cargo.toml` → `Finished dev profile ... in 2.03s`（0 error）✅ → E1 PASS

**派工紀錄**
- 16:20 並行派工 3 個 opus implementer（agentType=implementer, model=opus）：
  - T1 M2 清債——brief：R1–R10（m1–m7＋代表 Minor＋T18×2＋fmt 清債 #1＋過帳）、白名單 12 檔＋測試、TDD、五件套 verify。回報待收。
  - T2 lsp_adapters——brief：plan :159-201 Interfaces 逐字對齊、七 adapter、TDD、不碰 lib.rs。**異常**：首輪 0 tool use、7 秒即結束、回報為無關樣板 → 16:21 SendMessage 續推重啟（派工失敗第 1 次，記錄在案）。
    - 回報：9 測試（TDD red→green 證據齊）、七 adapter＋install_hint 照清單、大小寫不敏感副檔名、未碰 lib.rs。dotfile `.md` 判為 markdown（回報在案，屬邊界瑣事）。
    - **controller 復驗**：`cargo test lsp_adapters` → `9 passed; 0 failed`（親跑）✅ 綠。
  - T3 lsp_config——brief：plan :205-243 Interfaces 逐字對齊、tempdir 測試不碰真 ~/.yuzora、TDD、不碰 lib.rs。
    - 回報：14 測試（TDD 14 fail→14 pass）、8 函式契約逐字、stale=`!Path::exists`、全程 tempdir。
    - **controller 復驗**：`cargo test lsp_config` → `14 passed; 0 failed`（親跑）✅ 綠。
- 16:26 lead 補修自建 stub：lsp_download.rs／lsp_service.rs 0-byte 觸發 rustfmt「缺換行」diff → 各寫入單一換行，`rustfmt --check` 兩檔 CLEAN（親跑）。fmt 殘餘僅 git_log.rs（T1 處理中）。

- 16:47 T1 回報收訖：m1–m7 全修＋代表 Minor 6 項＋fmt 清債＋R10 過帳（m1–m7 表、linked worktree defer、其餘 34 條 Minor 逐條已修/續 defer）。TDD red→green 證據齊（Rust panic 重現、前端 18 failed→綠）。
  - 回報要點需 P4 檢視：(a) askpass accept loop Arc→Weak（讓 Drop 可達的必要 lifecycle 改動）；(b) GitNavContent stage/unstage 呼叫既有 selectGitFile（有 guard，僅選中路徑相符時）；(c) BranchPopover placeholder 改中文「分支名稱…」（repo 無 i18n 框架，取「與檔內中文文案一致」字面義）。
  - **controller 復驗（五件套親跑，16:47-16:49）**：vitest `42 passed / 321 passed`；tsc exit=0；build `✓ built in 705ms`（exit=0）；cargo `103 passed; 0 failed; 1 ignored`；`cargo fmt --check` → FMT CLEAN。✅ 全綠；基線 305/78→321/103 只增不減。IDE 停滯診斷（viewRegistry.test.ts 參數數）與 tsc 全量結果矛盾，以 tsc 為準判定為 stale。

**變更檔案**
- src-tauri/src/lib.rs — +4 pub mod（lead 預註冊）
- src-tauri/src/lsp_download.rs / lsp_service.rs — 空 stub（lead；僅單一換行）
- src-tauri/src/lsp_adapters.rs — T2 curated registry（七 adapter＋language_for_path＋9 測試）
- src-tauri/src/lsp_config.rs — T3 lsp.json load/save/resolve/stale（8 函式＋14 測試）
- src-tauri/src/search_service.rs — m1 find_col lowercase 同串計 col＋測試
- src-tauri/src/askpass.rs — 代表 Minor T5 Drop unlink socket（accept loop 改持 Weak）＋測試
- src-tauri/src/git_log.rs — R9 僅 cargo fmt（15 處既有 diff 清理）
- src/state/gitStore.ts — m2 ready gate、m3 pendingRerun（有界）
- src/editor/viewRegistry.ts — m4 unregisterView(path, view?) 比對
- src/editor/EditorPane.tsx — m4 呼叫端帶 view＋T18 revealLine 下界 clamp
- src/workbench/ExternalChangeResolver.tsx — m5 退化採用磁碟版補 markDirty(false)
- src/app/workbench/FilesNavContent.tsx — m6 清空取消（generation）＋T18 切 query 先清舊結果
- src/app/workbench/GitNavContent.tsx — m7 discardAll 排除 conflicted＋T15 選取跟隨
- src/workbench/git/BranchPopover.tsx — T14 notice 重置＋placeholder「分支名稱…」
- src/app/workbench/SettingsDialog.tsx — T19 onBlur clamp（draft）＋role=group
- 測試：新增 src/editor/viewRegistry.test.ts、src/editor/EditorPane.test.tsx；追加 gitStore.test.ts、externalChangeResolver.test.tsx、gitNavContent.test.tsx、filesNavContent.test.tsx、branchPopover.test.tsx、src/app/panels/panels.test.tsx＋Rust 同檔測試

**計畫修正**
- #1：`cargo fmt --check` baseline 紅（git_log.rs，M2 遺留）→ 併入 T1 brief 以 `cargo fmt` 清理（M2 清債範疇；五件套要求 fmt 乾淨，屬必要改動）。

## P4 對抗 review

**Reviewer 派工**
- 16:50 Round 1：全新 opus reviewer（agentType=reviewer, model=opus，未參與撰寫）。brief：R1–R5＋變更檔案全清單＋六項 rubric＋特別審視點（askpass Arc→Weak／selectGitFile 耦合／pendingRerun／m4 呼叫端全覆蓋／m6+T18 effect 競態／dotfile+canonicalize 邊界）；不給作者自辯；要求 ≥3 反證、禁 looks good。回報待收。

**Round 1（reviewer 回報，16:50-17:01）**
- Findings：F1 find_col col 在 lowercased 字元空間（İ 前綴時偏移，search_service.rs:45-49）；F2 m2 gate 未在 debounce/rerun 路徑重查（gitStore.ts:182-195，ready→非 ready 300ms 窗口仍寫 lastError）；F3 askpass accept thread Drop 後滯留 incoming()（benign cleanup）。
- 反證通過 11 條（摘）：m4 全 tree 無殘留單參數呼叫端（EditorPane.tsx:81 唯一）；m4 分割 group 註冊順序以 value-identity guard 擋覆蓋；m3 pendingRerun 先重置再補跑、無限補跑不可能；m7 conflicted 兩陣列皆排除；T15 僅選中相符才跟隨、null no-op；lsp_config 壞檔/優先序/tempdir 全過；canonicalize 與 stale symlink 一致；lsp_adapters 與 plan 逐字對齊；stub 恰為 1 byte 換行＋lib.rs 僅 4 行；askpass Weak 下 roundtrip/cache/背景測試全過；dotfile `.md`→markdown 無害。
- Rubric：邊界✅ 時序✅ state✅ 主題/i18n✅ scope✅（16 檔全掃、每行對應 R）孤兒✅
- Not reviewed（reviewer 誠實申報）：repo 無 baseline commit 可 diff，「未弱化既有測試」與「git_log.rs 純 fmt」以現況全綠＋通讀無語意變更佐證；Windows 未實編（askpass 改動限 #[cfg(unix)]）；GUI 行為屬 gui-acceptance。

**Findings 裁決**
- F1: 真 finding（col 邊界偏移確實存在且測試固化），**但實作逐字忠實於 plan :120 處方與本 run R2 字面（「col 以同一份 lowercased 字串計」）**——屬 plan 既定限制，非實作偏離；影響僅 case-insensitive＋İ 類展開字元前綴時高亮偏一格（cosmetic）。裁決：**不回 P3，記 Minor defer 入 M3 殘留**，P6 向使用者揭露。
- F2: **真 finding → 回 P3 派修**。m2 的目的即消除非 ready 噪音，300ms debounce 窗口與 rerun 路徑繞過 gate 是實質缺口；修法小（callback/rerun 內重查 ready）、風險低。
- F3: 接受為 benign cleanup——Drop 語意（unlink socket）正確達成；thread 滯留為 accept-loop 結構固有、非本次引入（改動前 thread 同樣永生且 Drop 根本不可達）；修復需非阻塞 accept/自連喚醒，超出 T1 範圍。裁決：**不修，記錄在案**。

**F2 修復（17:03-17:05，回派 T1 implementer——角色固定合規）**
- gitStore.ts：debounced callback 執行前重查 ready（非 ready → 清 pendingRerun/inflight、resolve、return）；m3 rerun 觸發前同樣 gate。gitStore.test.ts +2 測試（TDD red 證據在案：2 failed | 15 passed → 17 passed）。
- **controller 復驗（親跑）**：`bun run test` → `323 passed (323)`（321→323 只增）；`bunx tsc --noEmit` exit=0。✅ 綠。

**Round 2 派工（17:06）**：全新 opus reviewer（未參與撰寫與前輪）。整輪重審全部變更＋F2 修復特別反證（inflight/pendingRerun 殘態、ready 恢復後行為）；F1/F3 裁決附上供異議。回報待收。

**Round 2（全新 reviewer 回報，17:06-17:18）**
- 親跑基線全綠確認（cargo 103/fmt 0/vitest 323/tsc 0）；stub 以 xxd 實測各恰 0x0a 1 byte。
- Findings：**F-1（latent-bug）** gitStore.ts:196-199——F2 只 gate 了 callback 進入與 rerun 前，`await gitStatus` 之後的 `set({status})`/`set({lastError})` 無 post-await ready 重查。重現：ready 下 in-flight → 切到非 repo workspace（detect 清 status）→ 舊 gitStatus resolve → 舊 repo status 復活且不會被覆蓋（detect :160 僅 ready 才 refresh）→ ConflictBanner 在非 repo workspace 浮出舊 merge banner／lastError 噪音。
- 裁決異議：無（F1/F3 裁決成立）。
- 反證通過 6 條（摘）：conflicted-only discardAll 為乾淨 no-op（Rust 空守衛 git_service.rs:313,318）；module-scope 旗標跨測試不污染；m4 後掛者先 unmount 屬單槽結構固有非本次引入；revealLine 雙向 clamp；language_for_path 隱藏檔/無副檔名安全；lsp_config 壞檔不 panic。
- Rubric 六項全掃；Not reviewed：git diff baseline 不存在（untracked repo）、GUI/真 server 屬 gui-acceptance、F-1 未以測試實跑（reviewer 不得改檔）。

**F-1 裁決**：真 finding → 回 P3 派修（post-await 兩分支補 ready 重查；與 m2/F2 同一噪音類別，修法小）。

**F-1 修復（17:19-17:20，回派 T1 implementer）**
- gitStore.ts：post-await 兩寫入分支各加 `if (get().environment?.status === "ready")` guard；finally 不動（inflight/promise 收尾不變）。gitStore.test.ts +2（TDD red：2 failed | 17 passed → 19 passed）。
- **controller 復驗（親跑）**：`bun run test` → `325 passed (325)`（323→325 只增）；tsc exit=0。✅ 綠。

**Round 3 派工（17:21）**：第三位全新 opus reviewer，整輪重審＋ready-gate 完整鏈專項反證（非 ready 丟棄後 promise resolve？ready 恢復正常？runOp/detect 併發？）。

**Round 3（回報，17:21-17:32）**
- **0 findings**；裁決異議：無（F1 另核 col 僅資訊顯示、revealLine 不吃 col → 確為 cosmetic；F3 同意 benign）。
- 反證 8 條全通過（摘）：非 ready 丟棄後 promise 必 resolve（:172/:192/:209），detect 的 Promise.all 不 hang；pendingRerun 每 cycle 至多一次非遞迴；F-1 修復兩測試斷言 stale resolve/reject 不寫入；m1/m4/m6/m7/T18 各以 檔案:行 佐證。
- 觀察（非 finding，記錄）：refreshQuiet（gitStore.ts:230-233）無 ready gate——既有設計（帶註解）、非本次變更範圍、觸發需多重巧合，reviewer 判 speculative 不打回。列 M3 殘留觀察。
- Rubric 六項全掃；Not reviewed：git_log.rs fmt 前後語意等價無 baseline 可 diff（以 fmt --check 0＋103 tests 間接佐證）、gui-acceptance 範疇、lsp_service wiring（後續 wave）。

**Dry pass**：✅ Round 3 整輪 0 條（未被裁決推翻的）新 finding；反證紀錄 8 條（≥3）；F1/F2/F3/F-1 全數有裁決紀錄。**Gate G4 ✅**

## P5 驗收（controller 親跑，17:32）

- E1: lib.rs 預註冊 `cargo check` →

  ```
  Compiling yuzora v0.1.0 … Finished `dev` profile … in 2.03s
  ```

  **PASS**
- E2: T1 report 過帳（m1–m7 全修表＋代表 Minor 6 項＋linked worktree defer＋34 條 Minor 逐條已修/續 defer）——收錄於本檔附錄；新測試（find_col İ、askpass Drop、m2/m3/F2/F-1、m4×2、m5、m6、m7、T15、T18×2、T14、T19）全部出現在全量綠燈中。**PASS**
- E3: T2/T3 `cargo test` →

  ```
  lsp_adapters: test result: ok. 9 passed; 0 failed（controller 親跑）
  lsp_config:   test result: ok. 14 passed; 0 failed（controller 親跑）
  ```

  **PASS**
- E4: 五件套終跑（17:32 親跑）→

  ```
  bun run test  → Tests 325 passed (325)，42 檔
  bunx tsc --noEmit → exit=0
  bun run build → ✓ built in 756ms（exit=0）
  cargo test    → test result: ok. 103 passed; 0 failed; 1 ignored
  cargo fmt --check → FMT CLEAN
  ```

  **PASS**（基線 305/78＋fmt 紅 → 325/103＋fmt 乾淨；只增不減，fmt 紅燈已清）
- E5: P4 dry pass（Round 3 全輪 0 finding；反證 8 條；F1/F2/F3/F-1 全裁決）。**PASS**
- 與 baseline 比對：無新紅燈 ✅（唯一 baseline 紅燈 fmt 已由 T1 修復轉綠）

**Gate G5**：✅

## P6 收尾

- 結果：**DONE**（wave 0 完成：lib.rs 預註冊＋T1 M2 清債＋T2 lsp_adapters＋T3 lsp_config；三輪對抗 review dry pass）
- 最終變更檔案：見 P3「變更檔案」清單（Rust 8 檔＋前端 8 檔＋測試 10 檔；git 零寫入、零新依賴、無 worktree）
- 需使用者確認的假設：
  - A-p1: BranchPopover「New branch」placeholder 改為中文「分支名稱…」（repo 無 i18n 框架，取「與檔內中文文案一致」字面義）——若想用英文請告知，僅此一處。
  - A-p2: 派工模式採 wave 0 三 task 並行（plan 明文可並行；驗證各自復驗＋整合五件套，無互踩證據）。
- 殘留事項（移交後續 wave／backlog）：
  - F1 defer：find_col col 於 İ 類展開字元前綴＋case-insensitive 時偏一格（cosmetic，plan :120 處方之既定限制）。
  - F3 benign：askpass accept thread 於 Drop 後滯留 incoming() 至 process 退出（socket unlink 已正確）。
  - R3 觀察：refreshQuiet/autofetch 無 ready gate（既有設計、speculative、非本次範圍）。
  - linked worktree（M2 T4/T7）：續 defer 至未來 worktree 支援里程碑。
  - 34 條 M2 Minor 過帳：1 已修（selectGitFile staged 路徑間接覆蓋）、1 既修於 M2（vitest exclude）、其餘續 defer（附錄逐條）。
  - 下一步：**wave 1（T4 lsp_service＋T5 types+ipc）**。
- 自我改進三問：
  - 閘門漏洞：有——派工可能「空轉完成」（0 tool use＋樣板回報）；SendMessage 續推即復原。已追加 LEARNINGS `[trap]`。
  - 浪費規則：無建議放寬。
  - 新陷阱／慣例：plan 記載基線會過時（P2 一律實測）＋0-byte stub 觸發 rustfmt 缺換行——已追加 LEARNINGS。
- LEDGER 已追加：✅（RUNNING → DONE）

## 附錄：T1 M2 backlog 過帳（condensed，全文見 T1 implementer 回報）

**m1–m7＋代表 Minor：全部已修**（m1 find_col／m2 ready gate／m3 pendingRerun／m4 viewRegistry+呼叫端／m5 markDirty(false)／m6 清空取消／m7 conflicted 排除；T5 askpass Drop unlink（accept loop 改 Weak）／T14 notice 重置＋placeholder／T19 onBlur clamp＋role=group／T15 選取跟隨／T18 revealLine clamp＋T18 舊結果先清）＋P4 追加 F2/F-1（ready-gate 完整鏈）。

**續 defer（各附一句理由，出自 T1 report）**：watcher DebouncedEvent shape（升級時整合測試會抓）；watcher 非 UTF-8 忽略（brief 原意）；uiStore 型別註記（cosmetic）；design-ref 三條 doc-only（--frost 變數/拭取 typo/StatusBar 12/1）；search walker 順序假設＋trim 依賴＋同行多 hit 首 col（無正確性風險）；git 空 repo "(initial)"（歸 worktree/empty-repo 後續）；detect reason 措辭／LogSink per-call／timeout alias 測試（polish）；client read_to_string 隱含契約；git_detect 原子性（極低機率）；refresh(paths) in-flight 合併語意＋refreshQuiet 介面＋quiet 繞 single-flight（皆 brief 既定）；localStorage polyfill 位置；advance 冪等 guard；isText includes；EmptyState icon；bare fetch；grade_bytes 空 bytes；busy=fetch 無直測；rail badge 尺寸；add/del stats 未渲染；detect card 無 loading 態；rebuild 極端邊界×2；gen-git-fixtures 環境×2。
**已修/已結**：selectGitFile(staged) 直測（T15 測試間接覆蓋）；vitest snapshot 掃描（M2 已修，仍在）。
